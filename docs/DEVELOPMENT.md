# Development

Use this guide for setup and checks, [STATUS](STATUS.md) to resume work, and the relevant [ARCHITECTURE](ARCHITECTURE.md) section for design constraints. Product usage belongs in [README](../README.md).

## Build and run

Windows x64, Visual Studio's Desktop development with C++ workload, Windows SDK, LLVM/libclang, Git, and PowerShell are required. Rust and components are pinned by [rust-toolchain.toml](../rust-toolchain.toml); dependencies by [Cargo.lock](../Cargo.lock).

From Developer PowerShell at the repository root:

```powershell
$ffmpegDir = .\scripts\setup-ffmpeg.ps1
$env:FFMPEG_DIR = $ffmpegDir
$env:LIBCLANG_PATH = Join-Path $env:ProgramFiles 'LLVM\bin'
$env:PATH = (Join-Path $ffmpegDir 'bin') + ';' + $env:PATH
cargo run -p towavue-app -- 'path/to/media.mp4'
```

For normal-speed use or performance comparisons, add `--release`: `cargo run -p towavue-app --release -- 'path/to/media.mp4'` (omit the final `--` and path to open Welcome). Plain `cargo run` uses unoptimized Debug code, including Rust image decoding and UI processing; Debug latency is not representative of the Release application.

The setup script retrieves the checksum-pinned BtbN development build. **That build is not a distribution candidate:** its transitive FFTW linkage was rejected. For the native Windows rebuild and scoped ZVBI candidate, use the packaging references below. Do not introduce WSL.

To use an already prepared compatible FFmpeg prefix, set `FFMPEG_DIR` to its root and add its `bin` to this shell's PATH instead. Use `CARGO_TARGET_DIR` to isolate incompatible native build variants; do not put personal absolute paths in tracked documentation.

Native Windows build scripts compile the four fixed blit/UI HLSL shaders with the existing D3DCompiler API and embed the generated `OUT_DIR` bytecode; no `fxc.exe` lookup, downloaded shader binary or extra deployed asset is needed. Sources, targets and optimization flags remain explicit in each crate's build script ([Microsoft build-time compilation](https://learn.microsoft.com/en-us/windows/win32/direct3dhlsl/dx-graphics-hlsl-part1)). When changing them, run `renderer::built_blit_shaders_match_runtime_compilation` in the runtime library and `sampling_tests::built_egui_shaders_match_runtime_compilation` in `egui-directx11`, plus affected GPU regressions. Bytecode equality compares against the historical runtime compiler on the test host, not a cross-Windows compiler-version guarantee. Video-edit raster compilation still uses D3DCompiler at runtime.

The app build uses the installed Windows SDK's `rc.exe` to embed `assets/towavue.ico` through `assets/windows.rc`, retaining the Common Controls manifest. SDK discovery reuses the already locked `find-msvc-tools` package; no downloaded tool or deployed ICO is needed. Run the app's `window_icon::tests::` filter after changing the asset/resource, and check the resulting EXE's icon group as well as native window/taskbar appearance. Resource loading alone is not visual confirmation of Explorer's icon cache. See the [ICON resource format](https://learn.microsoft.com/en-us/windows/win32/menurc/icon-resource).

Helper lookup is deliberate: colocated ffmpeg.exe/ffprobe.exe take precedence; only a development layout with neither colocated helper uses FFMPEG_DIR/bin. A partially installed pair fails rather than borrowing another version from PATH.

## Verification by impact

| Change | Expected checks |
|---|---|
| Documentation only | Review diff, local links/anchors and claims; no Rust build, codec fixtures, installer tests, or native UI session |
| Local code change | Relevant regressions, rustfmt, affected-package Clippy; broaden if shared behavior is affected |
| Cross-crate/runtime contracts, dependencies, broad checkpoint | Full workspace checks below; Release when optimized behavior or an executable is relevant |
| UI/input/rendering | Relevant layout/input tests and targeted visible verification when needed; distinguish injected/offscreen tests from actual OS input |
| Performance/device behavior | Comparable representative measurements; record fixture, build, conditions, limits |
| Packaging | Affected packaging checks; installation, signing, and publication require their own scope |

```powershell
.\scripts\generate-m1-fixtures.ps1
cargo fmt --all --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --all-targets
cargo build -p towavue-app --release
```

Use unoptimized Debug tests for the large app harness and Release all-target Clippy. If the app test PDB reaches MSVC LNK1140, use the command-line-only override `cargo --config 'profile.test.package.towavue-app.debug=1' test --workspace --all-targets --locked --offline -- --test-threads=1` (plus explicitly documented exclusions). This reduces only app test debug information; it changes neither optimization nor tracked dependency/toolchain settings. Preserve the failed log and matching native failure executable/PDB before rebuilding. Do not substitute a huge Release app test harness or claim Debug timing as product performance.

Generate fixtures only when missing or their inputs change. Reuse completed checks for an unchanged tree; after a failure fix, rerun affected checks rather than restarting unrelated tests. Poll an existing build/process to completion; a tool timeout is not process failure. Do not run all ignored tests indiscriminately: some need hardware, local media, or long measurements. Skips remain unverified capabilities.

For saved Shell ordering, `shell::tests::saved_order_tests::hidden_views_read_saved_shell_sort_without_overwriting_it` creates a unique mixed-media fixture, saves four sort configurations through native APIs, verifies fresh hidden reads, and checks that observer changes are not persisted. It shows no Explorer window and changes no user-folder settings. The opt-in `reference_hidden_order_reports_settling` requires `TOWAVUE_SHELL_REFERENCE_DIR` and optionally `TOWAVUE_SHELL_EXPECTED_FIRST`; run with `--ignored --nocapture --test-threads=1` to read the same hidden view at 0/20/100/500/2000 ms after EnumDone. It reports stability, not an assertion that an externally expected order is satisfied. Use `reference_first_requests_preserve_shell_order` to assert the expected first item across fresh/reused providers.

For Shell apartment diagnostics, add `--features towavue-runtime-windows/shell-lifecycle-verification` to a targeted app test. `SHELL_APARTMENT` stderr lines report PID, native thread ID, elapsed microseconds, phase and the current [CoGetApartmentType](https://learn.microsoft.com/en-us/windows/win32/api/combaseapi/nf-combaseapi-cogetapartmenttype) result (`hr` is the query result, not the Shell call). Type 3 is the main STA; type 0 is another STA. The trace neither initializes COM nor retains its objects and emits no paths, but logging changes timing. It covers Shell worker initialization/closure and path parsing, not every COM user or process shutdown. The feature is off by default. Preserve a native failure's matching executable/PDB/dump before rebuilding; compare lifecycle evidence rather than rerunning until green.

For the isolated `audio_playback::tests::audio_buttons_fit_small_windows_and_accessibility_invokes_shared_commands` and `tab_transfer::tests::image_transfer_rebinds_current_pixels_and_preserves_animation_edits_and_view` controls, use the app-level `--features shell-lifecycle-verification` and set `TOWAVUE_SHELL_TEST_LIFETIME` to `drain` or `anchor`. Both wait up to ten seconds for instrumented provider work/apartment cleanup after the existing test body; `anchor` additionally holds a verified main STA with a message-aware loop, without parsing paths or prewarming Shell factories, then verifies its cleanup. No variable skips these diagnostic controls; ordinary isolated app tests still retire tracked native Shell threads after local owners drop. These controls require an isolated process with no earlier providers; their diagnostic completion alone is not an OS-thread/DLL-shutdown guarantee. Do not enable an anchor across ordinary tests merely to hide a crash or use this blocking verification wait in the UI.

For the final-window Shell exit gate, run the app filter `native_last_window_waits_for_shell_retirement_before_exiting` with `--ignored --nocapture --test-threads=1` and the app test debug override above. It creates one hidden native window, verifies that production AboutToWait closes its owner without exiting while a controlled Shell worker remains, then verifies event-loop exit after the native worker finishes. D3D11 is required; this is automated native lifetime evidence, not physical input or rendered-pixel approval. The ordinary `shell::shutdown::` runtime test additionally holds a TLS destructor to distinguish actual native thread exit from worker-body completion.

[CI](../.github/workflows/ci.yml) retains full Windows checks; this documentation change does not alter CI policy. There is no requirement to repeat those checks locally for prose-only edits.

## Locate code and tests

For file-operation groundwork, `cargo test -p towavue-runtime-windows --lib --locked --offline file_operation:: -- --test-threads=1` covers owned rename/case-only/collision/stale-source fixtures and the real Shell callback that rejects permanent deletion. The explicit `native_recycle_removes_only_the_selected_owned_file` opt-in recycles one uniquely owned tiny fixture; run with `--ignored --nocapture`. For `cross_volume_move_preserves_the_file_and_reports_completion`, set `TOWAVUE_FILE_MOVE_OTHER_VOLUME` to a writable existing folder on a different volume, then run it explicitly with `--ignored --nocapture`; the test creates/removes only its unique child directory and asserts the volumes differ. Neither check operates on user media. `dialog::` covers cancelled confirmation semantics. The app `file_operations::` filter covers confirmation cancellation, unavailable native ownership, timeline exclusion, failure rollback, duplicate-tab replacement and preference parsing. Explicitly run the ignored app test `native_recycle_completion_persists_confirmation_choice_and_does_not_close_last_window` with `--ignored --nocapture --test-threads=1` to recycle two uniquely owned tiny bitmaps through the production host and Shell, verify an empty retained window, and reload/use its isolated opt-out record. Its native dialog response is injected; it is not physical Delete input or visible dialog evidence.

For thumbnail context menus, run the app filters `thumbnail_menu::`, `file_operations::`, `filmstrip::`, `recent_tests::` and `tab_menu::`. Menu controls capture exact actions without launching Explorer or consuming the native clipboard. The inactive relocation control renames/moves owned files through the real host and waits for refreshed Shell membership while checking the current document stays selected. Explicitly run `inactive_recycling_keeps_current_media_and_refreshes_filmstrip_membership` with `--ignored --nocapture --test-threads=1` to recycle one owned unopened fixture, preserving the displayed document and its edits. That opt-in suppresses the native prompt only in its isolated host and does not write the preference file.

For source-save groundwork, run `cargo test -p towavue-runtime-windows --lib --locked --offline source_save:: -- --test-threads=1`. These controls create uniquely owned BMP/WAV/MP4 fixtures, exercise real preparation/native replacement and retained-original reuse, and verify cancellation, stale sources, encoder failures, reader locks, named streams and cleanup. The partial-publication recovery control constructs the documented failure state; it does not induce an actual native partial-replacement error. No user media or app Ctrl+S interaction is involved. The `retained_input` runtime filter additionally checks original-input lifetime through image decode/export and paused video decoder restarts. App filters `source_backing::` and `frame_export::` exercise real native replacement on owned fixtures, then simulate the save handoff to check reload/Undo/Redo, transfer, derivative/frame output, logical source protection and cleanup. The frame control uses a hidden native D3D11 window; these are automated ownership checks, not physical Save/shortcut evidence. The `window_host::source_save::` app filter now exercises actual Save dispatch and host publication: repeated image saves across different document baselines, Undo, edits arriving during preparation, cancellation, reader-drain gating, stale versions, close-guard continuation, audio channel/normalization output and recovery rejection. Its video control uses one hidden native D3D11 window and two hosted reader sessions sharing that device, checking retained original geometry and independent paused positions. The ambiguity control injects a typed recovery result; it does not force a native partial replacement. These are owned-fixture automated controls, not physical Ctrl+S or visible dialog approval.

For deleted-source runtime groundwork, the `source_save::` runtime filter includes retained-copy ownership, read-only-source/named-stream handling, stale/write-locked sources, missing-target preparation, exact unedited restoration, cancellation, collisions and candidate tampering. Injected failed-recycle completion states verify preserved recovery bytes when the original is missing/replaced and normal cleanup when it remains intact; this does not induce a native Shell failure. Generated image/audio/video controls recreate edits and decode/seek the retained input after removal; they do not exercise the app's deleted-document state. Explicitly run `native_recycle_retains_a_document_and_recreation_preserves_other_files` with `--ignored --nocapture --test-threads=1` to recycle and recreate one owned bitmap, preserving a neighbor and checking final-reader cleanup. It uses real Shell enumeration/recycling and native publication, without a confirmation dialog or user media. The app must opt into the retaining service and recreation job before this is a user-visible deletion feature.

For loaded source identity, the `source_version` filter in the runtime/app tests covers cache hits and same-length/same-mtime replacement, mutation during decode, WARP playback restart, stale UI completion, retained/transferred image documents and unavailable snapshots. `window_host::file_operations::tests::relocation_updates_all_tab_owners_without_discarding_edits_and_collision_rolls_back` additionally checks version propagation through real same-volume rename/move, collision rollback and invalidation after external modification. The image cache controls use an injected one-pixel decoder with real filesystem identities, and the app image completions are injected. The playback restart control uses a generated video and actual WARP decoding. Run the existing `native_unopened_cards_seek_paused_play_and_keep_activation_position` and `native_host_routes_workers_and_keeps_other_windows_alive_after_close` opt-ins explicitly with `--ignored --nocapture --test-threads=1` to assert loaded versions through real active/background playback, next-track navigation and hosted rename round trips. These are not physical-input or complete Ctrl+S controls.

For the Keyboard Shortcuts tab, `cargo test -p towavue-app --bin towavue --locked --offline keyboard_settings -- --test-threads=1` covers recording, modal saves, retained media and utility-tab transfer. `shortcuts::` includes v8/v9 migration, explicit removals and stale/locked/malformed save protection; `keyboard_shortcut_updates_reach_all_hosted_windows_and_cancel_old_prefixes` covers host propagation. The ignored `keyboard_settings_ui_reaches_gpu_at_three_densities` requires hidden hardware D3D11 and renders list/search/edit and command/file/folder picker states at 100%, 125% and 200%. Set `TOWAVUE_KEYBOARD_RENDER_DIR` to retain its generated RGBA readbacks; filenames contain dimensions. Run it explicitly with `--ignored --nocapture --test-threads=1`. These are isolated automated/native-render checks, not physical keyboard or visible-window approval.

For UI font changes, run `cargo test -p towavue-app -p towavue-runtime-windows --lib --bins --locked --offline fonts:: -- --nocapture --test-threads=1` with the app test debug override above. Candidate-loader controls use owned temporary files. Installed-font checks compare representative glyph atlas entries against replacement glyphs in both text families, retain baseline Latin/Japanese metrics and verify shared bytes across two contexts. Uninstalled optional families have no coverage claim; inspect the reported count and use the multilingual reference scene below for native rendering review.

For custom modal layout, the app filter `chrome::modal::` verifies fixed white body-sized headings and wrapped actions, real body wheel movement, full available-height use and natural short-content sizing at 100/125/200%, including a 240x150 client. Run the affected resize/rotation, audio/metadata export, keyboard settings and guard/focus regressions too: sizing alone does not prove input, IME or transactional behavior. The media reference below adds real production image resize/rotation/error modals, checking backdrop coverage outside their placement bounds before GPU readback.

For media reference readbacks, run the app filter `media_reference_layouts_reach_the_gpu` with `--ignored --nocapture --test-threads=1` and the app test debug override above. Set `TOWAVUE_REFERENCE_RENDER_DIR` to retain 42 `scene-WIDTHxHEIGHT.rgba` files (tightly packed RGBA8) from a 1228x708 logical client at 100/125/200%. It covers image, multilingual filenames, reading hints, ordinary/guarded export status, image resize/rotation/error modals, Gallery/filmstrip thumbnail context menus and compact/editing audio/video, using generated images, muted paused WAV/MPEG-4 sessions and seeded Shell/clock/waveform state. The native first video frame is composed below the UI; checks cover media/list/compact-seek pixels and timeline space, reading before Present. Hardware D3D11 and the pinned FFmpeg fixture tool are required; failure cannot silently skip. This is a repeatable visual-review aid, not native-caption, physical-input, real-clock/seek/waveform or desktop-compositor evidence.

For the fullscreen black-background check, run the app test `native_resize_coalesces_transitions_and_draws_interactive_changes`. Set `TOWAVUE_FULLSCREEN_BACKGROUND_VISIBLE=1` only for its opt-in visible run (`--nocapture --test-threads=1`), then clear that variable before other tests. It creates a nonactivating owned window and checks normal/maximized fullscreen round trips. Verification reads opaque black pixels before Present and counts only successful Present/DwmFlush pairs; post-Present flip-discard contents are not a screenshot. Logged wait time excludes the opt-in readback, while total transition time includes it; neither is an uninstrumented benchmark or physical flicker approval. The default hidden run retains the coalescing and interactive-resize checks without black submissions.

For the standard/custom native-caption reference, run `cargo test -p towavue-runtime-windows --lib --locked --offline compare_native_reference_geometry -- --ignored --nocapture`. This opt-in briefly shows owned windows on each monitor, compares standard/custom side and bottom insets and visible right-edge gaps, and checks client/button geometry through repeated normal/maximized fullscreen round trips. Creation requests nonactivation, but winit can replace that extended style during placement; foreground activation is reported separately. The test does not capture rendered pixels or physical input; inspect STATUS before repeating unchanged trials.

For system time/settings notifications, run the runtime library `caption::` tests. Set `TOWAVUE_TIME_SETTINGS_VISIBLE=1` for the bounded paint-delivery control: it briefly shows the owned window without activation, clears existing paint, and sends synthetic time/settings notifications. The default remains hidden and checks notification retention/coalescing. The app regression `native_time_notification_refreshes_same_source_and_rejects_old_results` verifies asynchronous cache refresh, stale-result rejection and held-image/close behavior. Neither test changes the OS clock or time zone; actual settings-screen operation is separate.

For video-resume process/transfer verification, run `cargo test -p towavue-app --bin towavue --locked --offline hosted_resume_process_restart_and_transfer -- --ignored --nocapture --test-threads=1`. It launches four separate test-executable processes using the production WindowHost, visible owned windows, isolated APPDATA/LOCALAPPDATA and a generated MPEG-4 video. Scripted pending/live transfers, normal closes, exact restored positions, clearing before close followed by a fresh zero-position reopen, and unchanged source bytes/mtime are checked; hardware D3D11 is required. This is not operator input, visual approval or an installed-executable test. The existing `native_host_routes_workers_and_keeps_other_windows_alive_after_close` opt-in also checks resume ownership through hidden active/background/detached transfers and shared-device recovery. Its synchronous opening and file-relocation helpers capture real resume and file-operation callbacks and route them explicitly; other notifications retain normal event-loop delivery and controlled Shell snapshots. The relocation exercise uses injected native-picker results on owned fixtures, not physical dialog interaction.

For Go to File's bounded search/cancellation checks, run `cargo test -p towavue-runtime-windows --lib file_search:: -- --test-threads=1`. Add `--include-ignored` only when directory symlink creation is permitted: the extra test creates owned escape/cycle links and checks exclusion plus an explicitly selected linked root. It changes no OS settings and reads no private media. Palette/keyboard/virtual-row regressions are under `palette::tests` and `recent_tests` in the app.

For software seek conversion, run `cargo test -p towavue-runtime-windows --lib --release --locked --offline deferred_worker_preserves_bounds_eof_counts_and_cancelled_input_reuse -- --test-threads=1`. The opt-ins `deferred_worker_reports_first_frame_and_cleanup_cost` and `deferred_session_reports_rapid_seek_replacement_cost` additionally require `--ignored --nocapture`: they generate bounded MPEG4/B-frame fixtures and compare historical/default/default/historical. The session control uses one WARP device, paused software fallback and 12 scheduled seeks at 33 ms, not physical keys or GPU presentation. Run timing comparisons alone; only the session control changes a test-global default to reach playback threads. Synchronous comparisons and cancellation counters are thread-local, so ordinary guards can run in parallel. Source/frame checks are outside timing. Consult STATUS before repeating unchanged measurements. The earlier `preroll_conversion_reports_first_frame_cost` is only a synchronous cost cutout, not worker latency.

For the read-only 1080p reference control, set `TOWAVUE_SEEK_REFERENCE_SOURCE` to one explicitly authorized video, then run `reference_video_reports_native_seek_path_and_software_preroll_equality` with the same Release/ignored/nocapture flags. It performs 12 completed hardware-first seeks and two forced-software exact-pixel comparisons in memory; no decoded pixels are saved or displayed. This is not rapid-input or GPU-presentation evidence. The sibling `reference_hardware_seek_reports_stage_cost` compares a synchronous seek/init/decode cutout against real workers at four positions. `reference_session_reports_hundred_hardware_seek_replacements` instead sends 100 forward and 100 reverse commands, 33 ms apart, through a windowless hardware PlaybackSession with normal WASAPI Shared initialization, muted from construction and paused before timing. Both require the same explicit source/flags and inspect no pixels; the session checks intermediate timestamp bounds and an independently decoded final timestamp, not physical/UI/GPU delivery. Consult STATUS before repeating.

For waveform/foreground contention, run `waveform_jobs_report_foreground_image_and_thumbnail_overlap` in the Release runtime library with `--ignored --nocapture --test-threads=1`. It generates900s stereo PCM and16 distinct4096x2304 PNG paths, then compares quiet/overview/detail/detail/overview/quiet using existing image, preview and waveform workers with fresh caches. Completion intervals, actual task overlap, full pixel/envelope equality and source stamps are checked; no UI/GPU, physical input, cold-disk or whole-process-memory measurement. Run without concurrent builds and consult STATUS before repeating.

| Area | Entry points |
|---|---|
| Commands, time, history, geometry | `crates/towavue-core/src/` |
| Window, media, graphics, Shell, export | `crates/towavue-runtime-windows/src/` |
| UI, workers, tab state | `crates/towavue-app/src/` |
| Native image renderer patch | `vendor/egui-directx11/`, [patch notes](../vendor/egui-directx11/TOWAVUE-PATCH.md) |
| PNG row-hook patch | `vendor/png/`, [source, scope and checks](../vendor/png/TOWAVUE-PATCH.md); preserve the pinned version and upstream unsafe prohibition |
| Generated media and specialized checks | `scripts/`, `tests/`; find the relevant test with rg |

Tests are colocated with implementations or under the crate's tests directory. Search for the behavior/test before creating another fixture or diagnostic.

For full-folder thumbnail preparation, explicitly run `cargo test -p towavue-app --bin towavue --locked --offline filmstrip::tests::large_folder_preparation_retains_pixels_without_ui_uploads -- --exact --ignored --nocapture --test-threads=1`. It generates 96 owned 4096x2304 PNGs, starts with an empty preview cache, checks pause/resume, bounded UI textures, all-file disk reuse through a fresh cache and a foreground original decode. Reported completion-handler/ready times exclude fixture generation and later source/cache verification; they are diagnostic for the selected build, not physical navigation, GPU presentation, OS-cold, peak-memory or comparative Release benchmarks. The ordinary test uses the same checks with 32x24 inputs. Fixtures are removed on success and retained on failure.

For source-waveform cost, run `cargo run -p towavue-runtime-windows --example waveform-cost --release --features waveform-verification --locked --offline`. It generates owned one-second/three-minute PCM and AAC, alternates native/historical CLI overview order using independent empty preview caches, and separately times edited-timeline refinement and warm cache hits. Both overview paths include identical PNG/cache handling and must match full pixels; input length/mtime and repeated results are checked. System file caches are warm; no UI/GPU, cold-disk or peak-process-memory claim is implied. The CLI backend exists only with this verification feature. Fixture files are removed on success and retained on failure.

For persisted-image queue latency, explicitly run `cargo test -p towavue-runtime-windows --lib --release --locked --offline persisted_image_queue_reports_mixed_and_uncached_cost -- --ignored --nocapture --test-threads=1`. It compares 64 generated image requests with either one uncached 4096x2304 PNG followed by 63 persisted thumbnails, or 64 uncached 32x24 PNGs, using fresh memory per sample. Request-to-observed-completion timings exclude fixture/cache preparation and full pixel/source checks; files are OS-warm, and the all-miss case retains an unrelated old cache entry. Compare preserved before/after binaries without concurrent builds. This is worker queue delivery, not UI/GPU, cold storage, physical input or an IrfanView benchmark.

Use the same command with `singleton_preview_queue_reports_populated_cache_cost` for one generated request with 0/512/2048/8192 unrelated disk entries, both persisted and uncached. `populated_disk_key_snapshots_report_allocation_reuse_cost` isolates filename collection/normalization at 512/2048/8192 entries with a same-binary historical control. Both are opt-in Release tests with warm files and untimed fixture/equality checks; neither measures UI presentation. The singleton test's uncached path still includes ordinary persistence/pruning costs.

For isolated image-color experiments, `cargo run -p towavue-app --example image-color-cost --release --locked --offline` uses the same conversion source as the app and checks every output pixel against egui. It generates its own inputs by default; an explicit `TOWAVUE_COLOR_REFERENCE_PATH` instead selects one read-only static file, checking its length/mtime and emitting no paths or pixels. Conversion timings exclude decode, GPU work and equality checks; they do not replace full navigation measurements. Alternative comparison strategies are verification-only; the default app entry uses packed conversion. This small target avoids rebuilding the application test binary for each conversion experiment.

For first-call decoder instrumentation, build `cargo build -p towavue-runtime-windows --example decode-call --release --locked --offline`, then run the example with one explicit static-image path. It invokes the production decoder once, with no window, preview cache or warm-up. Exported debugger boundaries `towavue_decode_begin` and `towavue_decode_end` surround decoding; on Windows x64 the end marker receives width, height, a borrowed RGBA pointer and byte length in RCX/RDX/R8/R9. The frame remains owned while stopped at that marker; never retain the pointer after continuing. Normal execution checks source length/mtime and reports dimensions only. A debugger that terminates at the marker must check source identity externally. Instrument both comparison processes with the same timing/heap policy, validate source/output identity outside timing, and distinguish decoder calls from original presentation or physical navigation. The marker ABI belongs only to this example, not the installed application. Optional `TOWAVUE_DECODE_REPEATS=1..1000` repeats decoding on the same main thread for process-scoped sampling; default is one. Repeated/suspended-thread samples are not first-call latency, CPU-active time or an app-worker profile; retain no pixel/stack data and never attach a sampler to an unrelated process.

For windowless device-start diagnosis, build `cargo build -p towavue-runtime-windows --example device-start-cost --release --locked --offline`. Run with `video` (ordinary BGRA/video flags), `graphics` (BGRA only), or `adapter` (ordinary flags, explicitly discover adapter zero), one fresh process per sample. The latter separates factory creation, enumeration and device creation; total `create_ms` includes discovery and accounting overhead. `DEVICE_CPU` brackets the device API with process/calling-thread kernel and user counters: quantization and worker overlap mean wall-minus-CPU is not wait time. It then times context protection and checks adapter/feature level, video interfaces and decoder-profile identity. Compare alternating batches after builds finish. No media/window or production-policy changes. Capability enumeration is not decoding compatibility, a cold-driver guarantee or app startup/presentation timing.

For actual-app first-original submission, build `cargo build -p towavue-app --bin towavue --release --features presentation-verification --locked --offline`. Launch a separate copy of its EXE/PDB with isolated `APPDATA` and `LOCALAPPDATA` and one explicit generated image; executable identity isolates the launch server. The ordinary event loop emits `PRESENT_STAGE` and one `ORIGINAL_SUBMITTED` record after a non-preview image mesh passes the existing clipped presentation gate and `present_surface` succeeds. The clock starts at main entry, excluding process creation/loader work; output is deferred until after the recorded submission. Phase IDs are documented in the runtime's `presentation_verification.rs`. No pixels, paths or screenshots are logged. Native self-timing must run without a debugger: attached debugger pauses contaminate these timestamps and can change running intervals too. Stable debugger symbols `towavue_presentation_stage` and `towavue_original_submitted` support separate diagnosis; the latter receives original width/height and client width/height in RCX/RDX/R8/R9 on Windows x64, followed by two f32 display dimensions in stack arguments 5/6 (RSP+0x28/+0x30 at entry). These computed physical dimensions describe the unedited initial image before clipping, not a pixel readback. Set `TOWAVUE_PRESENTATION_ACTUAL_SIZE=1` for a 100% fresh-image-load control, including navigation; any presence selects Actual instead of Fit only in this verification build. This does not override saved-tab restoration. Remove it for normal Fit, and check reported geometry/client sizes rather than assuming them. This is not OS-delivered zoom input or a permanent preference. Do not call successful Present compositor completion or physical scanout. Validate source stamps/dimensions and record client/zoom/cache conditions; never compare this native clock directly with an IrfanView debugger clock. This diagnostic feature also enables the existing selected-path image-loader trace. `INITIAL_DECODE` reports foreground/prefetch original-decode starts and dropped events, not independent thumbnail/preview-worker decodes. Set `TOWAVUE_SKIP_INITIAL_PREFETCH=1` only in this build for the historical serialized-startup control; any presence disables initial prefetch, so remove the variable for normal behavior. The ordinary build always enables initial-image prefetch and ignores this variable. Tracing/markers are absent by default, not a product ABI or telemetry service. Terminate only the diagnostic process you started after collecting output; do not close an existing user window.

For additional-window timing, the same feature emits `IMAGE_REQUEST_SUBMITTED` once per presented image request, scoped to each Application rather than the thread-local initial-process markers. It reports request→submission, request→acceptance and preparation durations; selected-path cache/publication intervals require a Requested event in the current image generation and start there; an unmatched selected-path trace reports `-1` instead of attributing the seed's timings to a different image. Publication is mailbox insertion before notification, not UI acceptance. Show/focus durations describe that window's launch activation; `-1` means unavailable. `INITIAL_DECODE` is emitted alongside that record only when the trace contains a current-generation request, never on later redraws; its counts remain selected-path trace totals, not per-step totals or all preview work. Use single-image opens with selected-path tracing for attribution. Forwarding a CLI path opens a new HWND, not a tab or arrow step. Set `TOWAVUE_SKIP_READY_LAUNCH_IMAGE=1` to suppress ready-original submission immediately after show/focus; any presence selects the historical first-redraw control. The normal build always uses ready-launch submission and ignores this variable. Run `cargo test -p towavue-app --bin towavue --locked --offline window_host::launch_tests::ready_launch_image_submits_once_without_waiting_for_pending_or_failed_loads -- --exact --ignored --nocapture --test-threads=1` for its independent hidden-D3D11 regression with owned images; it does not require the broad video-host trial.

For non-stopping first-original readiness, the same feature exposes `TOWAVUE_FIRST_ORIGINAL_SIZE`: one aligned u64, high/low 32 bits = width/height, latched at the first validated UI completion before texture preparation. It publishes no pointers/pixels and is not a submission marker. Resolve its RVA from the matching local EXE/PDB before timing, then use read-only [ReadProcessMemory](https://learn.microsoft.com/en-us/windows/win32/api/memoryapi/nf-memoryapi-readprocessmemory) on the owned child; no debugger attachment or remote calls. Time externally from process creation and retain the last zero/first nonzero observation bracket, source stamps and expected geometry. Do not label module-list availability DLL-initialization completion. The hash-pinned IrfanView main-DIB/header counterpart and current probe limitations are in STATUS/local evidence. Run `cargo test -p towavue-runtime-windows --lib --release --features presentation-verification --locked --offline presentation_verification::original_ready_marker_keeps_the_first_complete_dimensions -- --exact --test-threads=1`. This first-process marker is not a per-request navigation timer; default builds contain neither it nor the instrumentation.

For cache-PNG ownership cost, explicitly run `cargo test -p towavue-runtime-windows --lib --release --locked --offline preview::sharing_tests::owned_cache_png_reports_decode_cost -- --exact --ignored --nocapture --test-threads=1`. It compares historical copied/current owned RGBA decoding from generated encoded memory at thumbnail and sheet sizes, with complete pixel equality outside timing. It does not measure disk/cache lookup, original-media decoding or UI/GPU. For native producer publication, use the sibling `native_media_publication_reports_png_round_trip_cost` opt-in: the existing generated H264/AAC fixture feeds single-frame, sheet and waveform paths with independent empty caches; compare historical re-decode/current transferred pixels, including encoding/persistence, and verify full output, stored bytes, fresh-memory disk reuse and source stamps outside timing. Its test-only thread-local control is not a product option. Run ordinary preview/waveform regressions for conversion, fallback, cache repair, cancellation and persisted pixels; do not repeat timing without a changed path or new causal question.

For preview cache transfer, run `cargo test -p towavue-runtime-windows --lib --release --locked --offline preview::sharing_tests::preview_cache_hits_report_copy_and_contention_cost -- --exact --ignored --nocapture --test-threads=1`. Six generated resident keys serve 64 retained results through one/four synchronized callers. It records batch latency, request p95 and pointer-deduplicated returned RGBA lengths; thread creation/readiness and complete pixel checks are untimed. No file/decoder/UI/GPU work or process-memory measurement. Compare preserved before/after Release binaries without concurrent compilers; do not confuse shared returned bytes with additional-to-cache memory. Run lifetime/eviction/COW and native allocation-transfer tests, plus the native generation opt-in when the ownership path changes.

For preview UI acceptance, run `cargo test -p towavue-app --bin towavue --release --locked --offline tab_preview::tests::preview_acceptance_reports_packed_color_cost -- --exact --ignored --nocapture --test-threads=1`. The real tab single-frame/sheet completion handlers receive generated 240×160 and 960×640 opaque/mixed-alpha pixels; historical/packed/packed/historical batches check all queued texture pixels outside timing. Context creation, initial built-in texture registration and source cloning are untimed; acceptance, conversion and texture registration are timed. No worker/decode/cache, native event loop, GPU or scanout measurement. Run without concurrent compilers. The scalar control exists only in tests; ordinary builds always share the packed original-image kernel.

For uncached duration/filmstrip cost, run `cargo test -p towavue-runtime-windows --lib --release --locked --offline native_duration_ -- --include-ignored --nocapture --test-threads=1` without concurrent builds. The test-only CLI control compares fresh duration probes and whole `PreviewCache::filmstrip` requests in CLI/native/native/CLI order, checking exact durations/pixels and source stamps outside timing. Includes generation/persistence, not actual-app input/UI/GPU or OS-cold access. Ordinary preview tests retain timestamp-origin and helper-location/cancellation coverage; duration probing itself no longer needs FFprobe.

For indexed video-preview continuation, explicitly run `cargo test -p towavue-runtime-windows --lib --release --locked --offline preview::video_sheet::generation_tests::indexed_preview_reuse_reports_long_and_short_gop_cost -- --exact --ignored --nocapture --test-threads=1`. Generated 1080p MPEG4/MP4 fixtures use short/long GOPs with B frames. Independent-seek/continuation/continuation/independent-seek batches measure open/probe/decode/filter and all 16 reduced frames; complete pixel comparisons and source-byte/mtime checks are untimed. Test-only counters record seeks and decoded frames. Warm files, no concurrent compiler; excludes PNG/cache handling, UI/GPU and whole-process memory. Run ordinary video-sheet regressions for frame selection, reverse/duplicates/EOF, format fallbacks and cancellation.

For JPEG preview input allocation, run `cargo test -p towavue-runtime-windows --lib --release --locked --offline image::jpeg_preview::tests::reserved_preview_input_reports_whole_thumbnail_cost -- --exact --ignored --nocapture --test-threads=1`. It generates five noisy/plain JPEGs, or reads one bounded source specified by `TOWAVUE_JPEG_PREVIEW_INPUT_SOURCE`. Nine-sample historical/reserved/reserved/historical batches include file open/read, decode, resize and orientation; full pixel/source checks are untimed. Separate stdout metrics from FFmpeg stderr warnings. Warm files, no concurrent compiler; not persistence, worker/UI/GPU, OS-cold or peak-memory evidence.

For the reduced WIC thumbnail worker, ordinary `default_worker_uses_reduced_wic_jpeg_and_persists_its_result` checks the default path. Explicitly run `worker_service_preserves_guards_orientation_and_apartment_lifetime` with `--ignored --test-threads=1` using the existing Pillow JPEG fixtures. The Release-only `wic_existing_worker_reports_complete_preview_delivery` also requires `--features png-decoder-verification`, `--ignored --nocapture --test-threads=1`, and either `TOWAVUE_WIC_WORKER_FIXTURES` (nine baseline 4:4:4 JPEGs) or `TOWAVUE_WIC_WORKER_REFERENCE_DIR` (direct `.jpg` size-rank selection; expected six eligible). It compares historical/default/default/historical through fresh-worker creation, persistence and result notification; no UI/GPU or cold-cache timing. It changes test-global controls: run it alone without concurrent builds. Consult STATUS and its retained fixture/source archive before repeating.

For JPEG reader specialization, run `cargo test -p towavue-runtime-windows --lib --release --locked --offline image::jpeg_static::tests::exact_memory_reader_reports_decode_cost -- --exact --ignored --nocapture --test-threads=1` with `TOWAVUE_JPEG_READER_SOURCE` set to one bounded, valid YCbCr/grayscale JPEG. Both modes decode the same already-read bytes into RGBA; five-sample generic/specialized/specialized/generic batches check full pixels and source identity outside timing. This isolates the input adapter, not file I/O, EXIF transformation, navigation or GPU work. Run ordinary `image::jpeg_static` regressions and its explicit Pillow-fixture control before a changed reader's reference comparison.

For high-depth video export, run `cargo test -p towavue-runtime-windows --lib --release --locked --offline export::video_quality_tests:: -- --nocapture --test-threads=1`, then the affected `export::` aggregate. Generated controls cover supported containers, edits, color tags, small/odd and chroma/depth layouts, guarded rejection/cancellation and PSNR against the prior H.264 route. The opt-in `reference_high_depth_public_export_preserves_precision_and_reports_quality` requires `TOWAVUE_EXPORT_DEPTH_REFERENCE` naming an explicitly authorized short 10-bit clip; add `--ignored`. It reads that clip and writes only uniquely owned temporary output, reporting PSNR/size, not physical visual quality or a latency benchmark. Reuse the approved short staged reference instead of repeatedly decoding the long source.

For a real-source minute trim, set `TOWAVUE_EXPORT_TRIM_REFERENCE` to an explicitly authorized absolute source path and `TOWAVUE_EXPORT_TRIM_START_SECONDS` to its whole-second start. Run `cargo test -p towavue-runtime-windows --lib --release --locked --offline export::video_quality_tests::reference_high_depth_minute_trim_matches_independent_frames_and_reports_quality -- --exact --ignored --nocapture --test-threads=1`. This 10-bit control uses the public export API with a 60-second trim and requested hardware encoding. Its independent FFV1 reference keeps source timestamps across a ten-second-earlier seek; every normalized video timestamp, geometry, precision and known color tag must match. It retains a unique temporary directory (reported as `EXPORT_MINUTE_ROOT`) with reference/AV1/H.264 files and per-frame PSNR logs for review. H.264 is a newly generated QP20 video-only control; it is not an earlier owner-supplied export. Container sizes include different audio contents and are not a video-compression ratio. This is not physical playback, audio fidelity, HDR colorimetry or proof that an unspecified earlier trim used the same source interval. Ordinary tests do not read private references.

For first-request Shell correctness, run `cargo test -p towavue-runtime-windows --lib --release --locked --offline shell:: -- --nocapture --test-threads=1`. The ordinary generated control covers empty/small/large mixed folders, fresh hidden views, cancellation and reuse. The ignored `reference_first_requests_preserve_shell_order` requires `TOWAVUE_SHELL_REFERENCE_DIR`; `TOWAVUE_SHELL_FORCE_HIDDEN=1` exercises the production hidden-view path without closing Explorer. Add `TOWAVUE_SHELL_COMPARE_LIVE=1` to compare every captured path and sort column against a current matching live Explorer baseline. Optional `TOWAVUE_SHELL_EXPECTED_FIRST` is a basename assertion; omit an obsolete expected name if the reference directory has changed. `TOWAVUE_SHELL_LEGACY_CAPTURE=1` with forced hidden mode retains the old premature capture only as a failing comparison; never enable it for product validation. Neither reference mode writes media or live view settings. In the app, run `fresh_direct_and_gallery_opens_preserve_complete_shell_order`, or its ignored sibling `reference_direct_and_gallery_opens_preserve_complete_shell_order` with the reference directory. They exercise first/middle/last image opens through direct and Gallery action handlers, real completion events and shared card positions in fresh app instances with isolated settings. These are offscreen handler/worker controls, not physical Gallery clicks, cold process-start timing or visible-window certification.

For isolated Shell enumeration comparisons, run `cargo test -p towavue-runtime-windows --lib --release --locked --offline shell::enumeration_tests::hidden_shell_enumeration_reports_batch_cost -- --exact --ignored --nocapture --test-threads=1`. It creates owned empty-file folders and a hidden view. The sibling `live_shell_enumeration_reports_batch_cost` instead requires `TOWAVUE_SHELL_REFERENCE_DIR` naming an already open Explorer folder; it only reads that view and fails explicitly if none matches. Neither measures full refresh/navigation or changes product enumeration. Indexed and [batched enumeration](https://learn.microsoft.com/en-us/windows/win32/api/shobjidl_core/nf-shobjidl_core-ienumshellitems-next) must preserve exact order/path/kind/identity; run without concurrent builds, and consult STATUS before repeating an unchanged rejected candidate.

For experimental JPEG/WIC cost and pixel differences, run `cargo run -p towavue-runtime-windows --example jpeg-decoder-cost --release --features png-decoder-verification --locked --offline -- 'path/to/source.jpg'`. It reuses the existing WIC verification feature, not a product backend. The source must have no EXIF transform. Current/WIC/WIC/current batches report five-sample medians, RGB difference counts/maximum/mean and deterministic output; pixels and paths are not printed. Untimed header diagnostics report compressed bytes, coding mode, colorspace, sampling and retained ICC length; sampling `None` means maximum component factors 1×1, not missing metadata. Normal WIC timing excludes apartment/factory setup. Setting `TOWAVUE_JPEG_FRESH_WORKER=1` instead includes a new thread, its COM initialization, factory and teardown on every WIC call, as a conservative service-cost control, not an app-thread proposal. Both controls use warm files; pixel comparisons and source checks are untimed, with no GPU, navigation or whole-process memory claim. Do not adopt the candidate from timings alone: CMYK conversion, corruption/cancellation, orientation, source budgets and eligibility need their own evidence.

Set `TOWAVUE_COLOR_INTEGER_TRIAL=1` for current-packed/historical-lookup/egui/egui/historical-lookup/current-packed batches. The historical lookup control retains its opaque-row shortcut; production uses opaque blocks. The example's ordinary Release tests cover every component/alpha pair and generated row boundaries; full image equality is outside timed conversions.

Set `TOWAVUE_COLOR_PARALLEL_TRIAL=1` (without the integer option) for current/serial-initialized/two-way/two-way/serial-initialized/current batches instead of the default block-conversion comparison. Both candidates use the same row-opacity algorithm but retain historical lookup conversion; two-way splits disjoint output rows between one scoped worker and the caller. This is no longer a pure parallelism comparison against current production. Timings include output initialization, thread creation and joining. Calling-thread CPU diagnostics apply only to the current strategy, not the parallel worker. Run the example's ordinary Release tests without `--ignored` for generated split/alpha boundary equality checks.

Set `TOWAVUE_COLOR_SCALAR_TRIAL=1` for current-packed/historical-integer-rows/historical-integer-rows/current-packed batches. Production converts four RGBA pixels at a time on x64, with scalar tail/fallback and no extra worker or image-sized scratch buffer. Run its exhaustive component/alpha and unaligned-tail tests with `cargo test -p towavue-runtime-windows --release --locked --offline image_color` before timing; the example tests also check row boundaries and the instrumented app entry.

The reference navigation test accepts `TOWAVUE_NAV_LOOKUP_COLOR=1` for the former serial lookup conversion, `TOWAVUE_NAV_PARALLEL_COLOR=1` for the historical two-way control at every size, or `TOWAVUE_NAV_SCALAR_COLOR=1` for historical integer rows. All default to `0` (current packed conversion); enabling more than one is rejected. The former `TOWAVUE_COLOR_SSE2_TRIAL` and `TOWAVUE_NAV_SSE2_COLOR` options are obsolete: packed conversion is now the default, and the new scalar controls select the previous implementation. Compare modes on the same Release test binary, with other settings fixed and no concurrent build. These opt-ins are compiled out of the product. App color wall time includes allocation and, for parallel mode, worker creation/join; calling-thread CPU excludes the worker. Preserve order/source-stamp gates and compare whole-process memory and navigation latency, not just the selected conversion stage. Pixel equality belongs in the shared helper/entry tests and isolated example, not inside timed navigation.

Use `cargo test -p towavue-app --example image-color-cost --release --locked --offline -- --ignored --nocapture` to include the app harness's `cfg(test)` diagnostics. With an explicit static reference, `TOWAVUE_COLOR_CONCURRENT_DECODE=1` adds one looping same-source decoder; this is a stress control, not the real neighbor sequence. The reference navigation harness also accepts `TOWAVUE_NAV_IDLE_FRAME_MS` (default 0; 16 is the measured comparison): only event-free redraws are delayed, never commands or completion-event draws. Neither setting represents physical input or the native redraw scheduler.

For opt-in PNG decoder comparisons, run `cargo run -p towavue-runtime-windows --example png-decoder-cost --release --features png-decoder-verification --locked --offline` with an explicit read-only static `TOWAVUE_PNG_REFERENCE_PATH`. `TOWAVUE_PNG_BACKEND` selects `wic` (default), `wic-bgra`, or `libpng`; the latter requires `TOWAVUE_LIBPNG_DLL`, an absolute path to a trusted x64 libpng 1.6.58 DLL with its dependencies beside it. Loading a DLL executes native code: use the audited development copy, not an arbitrary download. Generated RGB/RGBA controls and the reference must match full pixels without printing them; timings exclude equality and decoder-service setup. These experimental backends are not used by the app and are not certified for its full format/cancellation/color contracts. Results belong only in STATUS.

The ignored `image_navigation::performance_tests::reference::reference_folder_reports_unpaced_completion_under_fixed_rate_commands` test requires `TOWAVUE_NAV_REFERENCE_DIR` and Release/hardware D3D11. It reports exclusive whole-loop phase totals and each phase's longest interval/start/progress, including loading and held frames, plus first-original presentation time. Window/device/app construction precedes its command clock; this is not end-to-end process startup or CPU-active time. Select one loader trace with `TOWAVUE_NAV_TRACE_INDEX` (zero-based lexical source index, even for reverse trials) or `TOWAVUE_NAV_TRACE_LARGEST_PNG`, never both. No reference pixels or filenames are emitted; measurement does not flush system caches or simulate physical input. `TOWAVUE_NAV_DECODE_CACHE_MIB` compares decoded-cache budgets (1–512 MiB, default 384) only before this test's first load; it does not change production defaults, entry/neighbor counts, texture limits or the per-canvas limit. Compare latency and memory together before considering a product-policy change. `TOWAVUE_NAV_DIRECTIONAL_PREFETCH` accepts `1` (default, production eight-ahead/one-back policy) or `0` (historical balanced comparison), without changing the nine-entry count or queued-step priority; combine with an explicit decoded budget to isolate lookahead versus capacity. This override exists only in the app test binary. Set `TOWAVUE_NAV_NATIVE_ORDER=1` to omit the synthetic snapshot, accept real Shell order and deliver all worker events through app handlers; unset or `0` retains the historical lexical/image-events-only control. Native mode reports accepted folder completions/order source and rejects changed membership or order. Scheduled latency and the selected trace's command index follow the traversal from the initial source, including reverse/wrapped order. This still uses a fixed command schedule and manual hidden-surface draws, not physical input or the native redraw scheduler; do not compare different orders as a decoder-only effect.

The ignored `image_navigation::performance_tests::held_reference::held_reference_reaches_release_destination_from_middle_and_late_positions` uses the same explicit `TOWAVUE_NAV_REFERENCE_DIR`, a Release app test binary and hardware D3D11. Run it alone with `--exact --ignored --nocapture --test-threads=1`, after compilation finishes. It requires more than 400 images and real Shell order, starts at half/three-quarter positions, and sends one ordinary press plus 90 scripted repeats at 33 ms before release. It checks monotonic full-original submissions, exact final destination, retained originals without preview/blank substitution, 500 ms without later navigation, and final full-frame equality with a fresh decode outside timing. Source lengths/mtimes are audited before/after; no source writes, private path/pixel output, screenshots or GPU readback. The two runs have separate app/cache state, but OS caches and desktop activity are uncontrolled. Report release-to-settled time separately from input duration; this is manual hidden-surface presentation, not physical keys, compositor latency, a cold benchmark or a paired IrfanView comparison. The same control records the final source with the bounded existing loader trace and up to 8,192 frame-phase samples. `HELD_TAIL_CLOCK` aligns input/release/settled times with loader events using the setup origin; release-dispatch time is separate from subsequent event handling, layout/inspection, GPU submit/Present and sequence advancement. Tail phase totals exclude sleep and out-of-frame observation, and GPU values are CPU wall time, not GPU timestamps. Final dimensions/frame count/encoded length reveal source scale without paths or pixels; full decode validation runs after these measurements. Trace overflow fails the control instead of silently accepting incomplete timing evidence.

The selected navigation trace also reports managed texture counts and RGBA8 dimension estimates before/after submission, existing resources on that frame's free list, and whole-upload payloads (including replacements). Snapshots are outside GPU submission timing and collect no native handles or pixels. Existing retirement excludes newly created-and-freed resources; these estimates are not physical VRAM, driver staging allocations or proof of immediate GPU reclamation.

For a generated, isolated GPU history control, run `cargo test -p egui-directx11 --lib --release --locked --offline large_texture_upload_reports_settled_allocation_history -- --ignored --nocapture --test-threads=1`. It compares 12/468/468/12 prior creations, retaining the same final twelve textures before uploading 8706×5949 pixels. Each batch has three fresh-device samples with app device flags; GPU work is drained before timing. Set `TOWAVUE_TEXTURE_SOURCE_CONTROL=1` instead to hold history at 468 and compare persistent/fresh/fresh/persistent source storage; both conditions allocate and retain the same extra CPU copy. Source allocation/destruction and exact readback are untimed. No swap chain, decoder or app workers participate; neither control proves full-app causality or physical VRAM use. Do not run it concurrently with builds or another timing workload.

The same example's `stages`, `stages-sse2` and `stages-serial-paeth` modes isolate inflation/parsing, row reconstruction and packing for noninterlaced RGB/RGBA8 on x64, using png's opt-in benchmark APIs. They retain an additional full filtered canvas for measurement; do not substitute them for the production row decoder. Run the kernel's exhaustive/generated checks with `cargo test -p towavue-runtime-windows --example png-decoder-cost --release --features png-decoder-verification --locked --offline` before reference timing. Never print pixel buffers on equality failure. `stages-sse2` uses the current runtime kernel; `stages-serial-paeth` retains the historical per-pixel SSE2 control. The ignored `repeated_pixels_report_row_cost` test compares flat, mixed-run and noisy generated upper rows with resets and exact equality outside timing. These are row-kernel controls, not navigation or whole-process memory measurements.

User settings live under `%APPDATA%\towavue` (shortcuts.conf, grid.conf, recent-files.txt). Preview disk cache is under `%LOCALAPPDATA%\towavue\preview-cache`. Build output, `tests/generated`, and local FFmpeg are ignored. Never clear user settings or media to make a test pass.

For bug reports retain reproduction, expected/actual result, build, Windows/GPU/driver/DPI, and codec/dimensions/duration. Use disposable generated media, not private files. For Explorer ordering include Sort By, whether Explorer was open, and reported snapshot source.

Selected reference traces and the opt-in color-example test also report calling-thread kernel+user CPU accounting from [GetThreadTimes](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-getthreadtimes). These counters exclude other workers and GPU execution; their accounting granularity does not support exact wall-minus-CPU wait durations. CPU sampling is verification-only; ordinary color conversion leaves it disabled.

The reference navigation harness accepts `TOWAVUE_NAV_PARALLEL_PREFETCH=0` for the historical serial policy; unset or `1` uses production's bounded one-image lookahead. Compare disabled/default/default/disabled on the same Release binary, with cache/directional policy fixed and no concurrent builds. Include order, blanks/previews, memory, cancellation and source-identity checks; isolated paired-decode throughput does not prove navigation latency. This override is verification-only, not a user preference; the runtime contract is in ARCHITECTURE.

For initial-open ordering with real Shell notifications and large-image workers, run `cargo test -p towavue-app --bin towavue --locked --offline native_folder_notifications_preserve_initial_large_image_burst -- --ignored --nocapture --test-threads=1`. It generates 100 owned 4096×2304 JPEGs using the configured FFmpeg, queues 100 Right shortcuts before presentation, handles real worker events and derives expected navigation from the returned Shell order. It requires hidden-window hardware D3D11; inspect PASS/SKIP and the reported order source, not just the test exit code. Debug is sufficient for correctness, not latency comparisons; elapsed time includes fixture generation. The separate `native_initial_large_images_preserve_every_accepted_step` opt-in retains deliberately delayed scripted order as a control; tiny real/scripted-order variants run normally. Successful runs remove their isolated fixtures; failures retain them.

For non-stopping burst instrumentation, build with `presentation-verification` and set `TOWAVUE_BURST_TRACE=1` before launch. Resolve the matching EXE/PDB and verify exported `TOWAVUE_BURST_CAPACITY` (**65,536**) before reading records; reject incompatible binaries instead of reading past an older array. `TOWAVUE_BURST_NEXT` is the reservation count; `TOWAVUE_BURST_RECORDS` contains eight u64 words per record: committed sequence, QPC tick, kind, generation, source token, a/b/c. Read only the owned child, reject zero ticks, wait for sequence `index+1`, and verify it around the payload read; reservation is not publication. Never overwrite/reset; counts above capacity mean loss, not a complete recording. Event IDs/payload call sites are in [burst_verification.rs](../crates/towavue-runtime-windows/src/burst_verification.rs) and app/loader hooks. Tokens use UTF-16LE FNV-1a with slash and ASCII-case normalization, not anonymization; check source collisions/order locally and do not publish private paths. `Application::trace_burst` b/c encode queue depth and loading/awaiting/error/reading/folder/image/held bits; a is event-specific (Key: code in low byte, pressed/repeat/synthetic in bits 8/9/10). Geometry a/b pack original/client width and height in high/low u32; c packs physical display f32 bits. A frame's source is the current request, not proof that its pixels were drawn; only the validated OriginalSubmitted gate certifies that request. Worker a/b encode elapsed nanoseconds and outcome (completed/failed/unsupported/budget/superseded = 1–5). Mailbox, media and folder generations are separate domains. This mode suppresses old per-request stdout; remove the variable for existing submission tools. Redirect both diagnostic children's standard handles consistently to avoid unrelated library logging/pipe backpressure. Current manual evidence and remaining limits are in STATUS.

Frame-phase burst records are enabled only when loading, awaiting image presentation, holding queued directions or waiting for folder order at frame entry; ordinary idle frames do not consume the fixed buffer. OriginalSubmitted can occur repeatedly for one source/generation while awaiting folder order: do not count every submission as another image. Sort by QPC for concurrent event analysis rather than assuming reservation order is time order. The current local reader validates only a complete committed prefix; separately verify final queue drain, source order, physical inputs and instrumentation overhead before claiming a complete comparison.

For a non-input folder-application control, additionally set `TOWAVUE_BURST_FOLDER_REFRESH_CONTROL=1`: after the initial snapshot the diagnostic app requests exactly eight fresh Shell snapshots of the same current source. Event 33 (FolderApplyPhase) marks phases 1–7: comparison, audio/reading handoff, preview refresh, identity remap, snapshot replacement, reading restoration, prefetch submission. Its generation/state are the app helper's media-domain values; the preceding FolderApplied carries folder generation. `TOWAVUE_BURST_IDLE_FRAMES=1` explicitly includes otherwise-idle redraws; size/timing limits still apply. Remove both variables for ordinary manual navigation. This control does not inject keys or establish input latency. Run the metadata regression with `cargo test -p towavue-app --bin towavue --release --locked --offline filmstrip::snapshot_comparison_reuse_preserves_sequence_and_preview_refresh_decisions -- --exact --test-threads=1`, plus affected filmstrip/sequence/retained-reading regressions.

## Documentation maintenance

STATUS is the only current-work/handoff record. Update rows in place rather than appending a chronology. Retain new evidence when it changes the next decision or prevents repeated work; include a commit/test reference and its limits. Small documentation corrections need not create status entries.

ARCHITECTURE describes current durable contracts, not every implementation step. DEVELOPMENT describes repeatable procedures, not dated trial reports. Source/license records retain their own exact provenance; do not refresh them for unrelated UX work.

Pre-consolidation evidence remains in Git at [2d72e75](https://github.com/sheetau/towavue/tree/2d72e75001375f31130e027d8d1532f04f99bf80). Search only the relevant investigation:

```powershell
git show 2d72e75:SESSION_LOG.md | Select-String -Pattern 'APNG|PREVIOUS' -Context 2,5
git show 2d72e75:docs/DEVELOPMENT.md | Select-String -Pattern 'long GOP' -Context 2,5
```

The old README, architecture, roadmap, UX ledger, and known-gaps snapshots are recoverable at the same commit. They are historical evidence, not current instructions. There is no duplicate archive tree to read or maintain.

## Packaging references — only when needed

Publication is deferred; this index is not authorization to resume it.

- [DISTRIBUTION](DISTRIBUTION.md): packaging decisions and audit entry point.
- [LOCAL_SETUP](LOCAL_SETUP.md): per-user install/update/rollback/uninstall contracts and lifecycle evidence.
- [NATIVE_FFMPEG_BUILD](NATIVE_FFMPEG_BUILD.md), [FFMPEG_REBUILD](FFMPEG_REBUILD.md), [MABS_BUILD](MABS_BUILD.md): native Windows FFmpeg options, reproducibility, rejected inputs.
- [CANDIDATE_MATERIALS](CANDIDATE_MATERIALS.md), [APP_MATERIALS](APP_MATERIALS.md), [NATIVE_MATERIAL_CATALOG](NATIVE_MATERIAL_CATALOG.md): candidate/source/notice correspondence and specialized audits.
- [VC_REDIST](VC_REDIST.md), [INSTALLER_FIXTURE](INSTALLER_FIXTURE.md): prerequisite handling and isolated installer checks.

These records describe particular candidates. Rebind source, binaries, notices, and evidence before using them for a new release; their presence does not certify the current executable.
