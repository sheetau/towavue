# Current work and handoff

Last consolidated: 2026-09-15. This is the only current-status and handoff document. Update rows in place; detailed history stays in Git. [Development](DEVELOPMENT.md) provides setup, checks, and historical lookup; [Architecture](ARCHITECTURE.md) provides accepted contracts.

## Scope and next action

The active goal is faster image switching, loading/display and thumbnails across tab hover, recently opened, filmstrip, image/reading/video seek hover and other media paths. Adapt beneficial mechanisms from the owner's IrfanView analysis only after demonstrating improvement. Completion requires measured superiority to the installed IrfanView in actual switching/loading/display, not merely faster isolated kernels. The goal is not complete: manual instrumented navigation now favors towavue for large generated PNG/JPEG images, but tiny-image results are mixed, native startup remains behind and full input-to-display/cold/broader coverage is missing. The functionality/UI rows below remain a backlog, not the current priority.

Current priority: remaining thumbnail/media-loading costs while preserving the completed `.2Ds` Fit navigation result. Uncached native duration probing is now adopted; its endpoint measurements and limits are recorded below. After reporting severe loading delays with plain `cargo run`, the owner independently tried **Release and confirmed the problem was absent**; they accept the rapid-press improvement, conditional on the next held-key comparison. The observed owner process used this repository's `target/release/towavue.exe` with the setup-script BtbN DLLs, not the separate diagnostic archive. Plain Cargo run uses unoptimized Debug; DEVELOPMENT now distinguishes performance launches. **Held-key comparison is now complete at the recording level:** 303/303 towavue inputs became presentations, with bounded queue/drain; the matched first 100 transitions completed sooner than IrfanView. Details and limitations are in the held-key checkpoint below. Owner confirmation of the formal trial's perceived latency remains useful; this is not a final-goal or broader-thumbnail pass. Continue only from remaining demonstrated costs, not by repeating this unchanged comparison. Do not repeat unchanged startup or slow four-image/100%-zoom trials. JPEG/WIC remains verification-only, not permission-blocked. Reference media remains read-only and nonvisual to the agent; no appearance/screenshots, installed-app/settings changes or publication.

Publication, signing, and release recertification are deferred. An eventual assisted per-user installer is the accepted format, but not authorization to publish now. Windows 10 physical testing is optional; no installation into the owner's environment for that purpose. The owner permits visible-window interaction and clipboard use for scoped work, not unrelated writes or input.

## Handoff

- Latest caption checkpoint: a new hidden-window regression reproduced a 75 px reservation instead of the existing 154 px default at 96 DPI. Hidden/minimized windows now use the DPI-scaled default rather than nonempty but unusable native bounds; visible windows retain actual OS placement. Caption/resize regressions, visible state-transition geometry on three 100%/150% monitors, shared hardware video surfaces and workspace Clippy/fmt/diff pass. This does not reproduce the owner's entire new-window/resize trigger or fix maximized button height. The owner confirmed that the height change occurs with the upper-right Maximize button, not the app's separate fullscreen mode. The owner has been asked whether fixed height should take priority over the no-custom-drawing contract; that decision is pending, and automatic continuation is not approval. See Native caption geometry below.
- Accepted prefetch policy: The owner-approved **384 MiB decoded cache, eight neighbors in the current direction and one opposite** remains unchanged, with queued-step priority, 30 decoded entries, ten-path batches, 256 MiB texture cache and reading-spread selection. Bounded paired decoding is now enabled in normal construction; current adoption checks and measurements are in the Paired original-decode concurrency row. Earlier capacity/directional comparisons remain below and in Git. Installed executables were not replaced. Cold/physical/Shell-order/IrfanView, outliers, Shell lifetime and broader UI/feature work remain open; do not repeat unchanged capacity or steady-direction trials.
- Previous resize checkpoint (`087b9c7`): disable implicit DXGI stretch between window resize and redraw; standalone/shared-caption descriptors, four-size presentation and opaque background pass. WARP/hardware fixed-left text pixels match byte-for-byte at widths 624–680 and densities 1/1.25/1.5/2; logical origins were already stable. Shared video/UI surface recreation and native caption image/video recovery preserve pixels/state with zero playback CPU transfers. Full workspace **912 passed / 43 ignored / two known exclusions**, vendor **5**. The native test runs in a subprocess because winit allows only one event loop per process. Not proof that the owner's physical right-edge shimmer is resolved; exposed-edge appearance and native caption geometry remain open.
- Album Artist investigation: audio/PNG already expose it. The [Adobe Dynamic Media schema](https://developer.adobe.com/xmp/docs/xmp-namespaces/xmp-dm/) defines artist, not a distinct Album Artist property. Do not silently map AlbumArtist to xmpDM:artist or invent a standard property; JPEG/WebP retain their nine-field contract pending a justified interoperable mapping. Further descriptive-XMP retention and broader metadata transfer remain open; omission of unknown technical XMP after raster edits is intentional.
- No build or owned media job was left running at that checkpoint. No distribution work should be resumed merely because old logs list it as next.
- Manual input is now demonstrated for the independent control and the navigation trial below. Other physical-operation audits remain open; automated delivery has not recovered. Do not bypass supported tools through private injection or relabel synthetic egui input as OS verification.
- Build setup and alternate FFmpeg prefixes belong in DEVELOPMENT/environment, not machine-specific paths here.

## Remaining work

IDs retain traceability to the old UX ledger. “Implemented” describes the main path, not a claim that every device/input combination was tested. The shared validation row applies to all relevant entries; do not duplicate it as a new requirement on every checkpoint.

| ID | Current state | Remaining work / completion evidence |
|---|---|---|
| U01 | Native caption, borders, fullscreen/monitor restoration; fixed 32-point visible toolbar, tight left reservation/full button cutout; DPI default reservation while hidden/minimized; cutout refreshed before presentation when bounds change without another resize; 96/144 DPI visible state-transition geometry/hit queries tested (earlier 192 DPI evidence retained in Git) | Reproduce the owner's exact resize/new-window clipping trigger after the synchronization fix; reported maximized button height, OS-owned normal right margin and button-height mismatch with fixed toolbar; quantify window-drag latency; remaining native glyph/background/system-menu/Snap and DPI combinations |
| U02 | Native file picker and ordinary unsaved confirmation; stateful forms remain egui; image/video resize and audio-export content is height-bounded, scrollable and reveals newly focused controls | Other long-content/focus/scale/UIA edge cases, without rewriting all dialogs |
| U03 | Figtree/Codicon/Japanese fallback and tabular numbers; Welcome font fixed; solid play/pause counterparts with unchanged shared controls | Final font/icon coverage as UI changes |
| U04 | Grayscale styling, tab insets, left-anchored clock, status notices, async file details, shadows and #0c0c0c floating backgrounds; shared pill scroll handles and pointer feedback; shared Play/Pause availability; 2px dashed whole-client drop guide; unscaled presentation during resize; hover-only fullscreen status for all media, with stable audio timeline geometry and owned seek gestures retained through release | `fullscreen_status_is_hover_only_for_every_media_kind` covers three media kinds at 1/1.25/2x, notices, Tab, hidden-focus cleanup, normal-window seek focus and stable audio bounds. Related selection/saved-focus and ordinary/batched seek tests pass. **24 distinct affected app tests and three example tests pass**, plus app all-target Clippy/fmt/diff. The earlier full Debug app run passed 499, failed two superseded auto-reveal expectations and one new fixture missing a seek control, with 21 ignored and two known exclusions; all three were corrected and rerun in the targeted set, without repeating unrelated passes. Synthetic egui/AccessKit, not physical input/native UIA; no Release/installer update. Prior drop-guide density/ownership evidence remains in Git. Physical resizing/drag jitter, shadow quality/GPU cost and layout audit remain |
| U05 | Export progress and delayed foreground image/media/folder activity retain the toolbar boundary, export priority and unknown-work semantics. Owner-approved waveform loading/refinement now uses clipped, accessible text inside the visible timeline, not the border or path notice. Refinement labels appear in their submission frame without a redundant border repaint. Timeline selection uses the normal pointer; directional gain/edge/seek cursors remain | Loading location/no-duplicate checks cover audio/video, two widths and 1/1.25/2x; native refinement/identity/completion, export priority and timeline gestures pass. Shared notification checkpoint: full Debug app run **501 passed / 1 stale expectation failed / 21 ignored / two known exclusions**. The unchanged prior Release binary reproduces the palette/logo expectation failure introduced by `9f9f349`'s availability fix. Corrected only that test: palette still disables the logo after guard cancellation, closing it restores availability. **Four targeted app and three example tests pass** afterward; reuse the unchanged 501 passes, do not call this a fresh green aggregate. App all-target Clippy/fmt/diff pass. No native crash or leftover build/test process; no physical input, Release/installer update or publication. Physical/native-UIA and mixed-monitor confirmation remain |
| U06 | Welcome, recent 40 paths with bounded visible-first/offscreen thumbnail preparation, picker focus return and arrow-focus scrolling across recent-grid rows; inset scrollbar with gray handle hover/drag and lighter track, without restyling content | `welcome_scrollbar_is_inset_muted_and_keeps_gutter_wheel_input` reproduced the edge-to-edge track; 1/1.25/2x checks now cover 8-point insets, hover colors, four gutter wheel/click regions, handle dragging and unchanged content/sibling styles. 17 affected Welcome/recent-grid/scroll-style tests and app Clippy/fmt/diff pass. Physical input/accessibility remain; no session/unsaved-backup feature implied |
| U07 | Tab image/transport/focus/scroll preservation, background audio with clickable speaker/mute title indicators, retained video, finite image-animation playback state, content-aware dirty state; crop/flip/quarter-turn comparisons borrow pixels and accepted results survive activation/window transfer; pointer tabs, media controls/audio rows, tab-strip/playlist scrollbars and view/resize gestures release widget/saved numeric focus without removing explicit keyboard/accessibility focus | `batched_cancelled_media_button_press_releases_prior_numeric_focus` confirms existing behavior for Play, Shuffle and Reading at 1/1.25/2x: press plus outside release in one frame cancels activation, clears late-registered numeric focus and its saved role, preserves hover focus and other tabs\' roles, and leaves the numeric value unchanged. Test-only addition; no reproduction requiring another pointer implementation. **24 targeted tab-drag/focus/playlist/view-selection tests** and app all-target Clippy/fmt/diff pass. `c2a210b` covers middle-button cancellation, disabled/clipped/covered origins and dirty guards; `8aed4d4` covers close buttons; `e7e4251` tab labels; `99bc443` speaker focus/mute with separate generated-silence WASAPI evidence. Existing explicit keyboard/accessibility and role restoration controls pass. Synthetic egui/accesskit input, not physical mouse/keys or native UIA. Do not repeat the unchanged batched media-button cancellation probe. No full-app aggregate; Shell abort/exit-wait issues remain unchanged. Other focus paths, expensive edit comparisons, all-tab budgets and recovery combinations remain |
| U08 | Same-host forwarding, live tab split/merge; whole-client tab/filmstrip insertion; independent original-file opening; one highlighted card shared with keyboard activation; centered two-row title and virtualized folder-ordered traversal; open same-folder refresh retains ready thumbnails and follows relocated focus by path | `filmstrip_highlights_one_target_and_centers_two_line_names_above_the_preview` now also reproduces and fixes batched Tab+Enter opening the previous card. Resolve the requested focus before any card handles activation, retaining pointer behavior and existing reveal/virtualization rules. Thirty forward/reverse/wrap cases at three densities, with/without discarded passes, verify exactly one correct open action, matching highlight and visible bounds in a 320-point viewport. **11 affected app tests pass**, including 100-item keyboard traversal, idle-order relocation, refreshed-order Tab, accessibility, manual scrolling, overlay rejection and focus restoration; app all-target Clippy/fmt/diff pass. Earlier `913c87f` retains twelve idle relocations and `f28359e`'s 48 real-handler order+Tab cases, retained/deleted textures and closed-strip reset. Its unchanged-order GPU control remains 36 media-region / 18 completed whole-surface comparisons and 54 worker replacements; not repeated here. Synthetic egui/AccessKit, not physical input, live Explorer sorting or proof of the wider flash report. Different-folder/closed resets and native drag-effect design are unchanged. No full-app aggregate, Release/installer update or publication |
| U09 | Context menu, close groups, path copy/Explorer, path-only reopen; pointer/keyboard modality and natural root/direct/nested sizing; direct menus remeasure both dimensions; menu dismissal retains media-view input ownership for the frame; logo availability is shared across pointer/keyboard/accessibility | `filmstrip_blocks_logo_menu_equally_for_pointer_keyboard_and_accessibility` reproduced accessible logo activation opening a menu while filmstrip's pointer guard rejected the same operation. Disable the actual widget using the existing availability rule and expose its actual enabled state to AccessKit. Six open/closed-filmstrip cases at 1/1.25/2x cover pointer, accessible activation, focused Enter and restored availability, without dispatching commands or dismissing filmstrip. **14 affected logo/menu/focus tests** and app all-target Clippy/fmt/diff pass. The initial outside-click hypothesis could not reach a menu through ordinary pointer input during filmstrip; do not report it as a reproduced dismissal leak or add another frame guard for that path. `d90e584` retains the separate reproduced batched image-selection zoom fix and its 12-case discard/density regression. Synthetic egui/AccessKit input, not physical mouse/native UIA or proof of remaining flashes. Other menu/tooltip lifetimes remain; no new full-app aggregate, Release or installer update |
| U10 | Shared/deduplicated previews even after disk-cache failure, reduced JPEG and row-streamed PNG thumbnails, native first-composited GIF/APNG/WebP thumbnails, video-sheet UV reuse, same-frame cached hover cells and loaded image-tab reuse; Welcome offscreen and closed-filmstrip nearest-texture preparation followed by cache-only warming of remaining neighbors; ready textures survive temporary overlays and smaller strip/seek-preview working sets within the existing 64-slot bound; visible requests precede retained textures, with latest-generation completion and source validation; closed preparation retains completed textures without redrawing | Shared ordinary/explicit-large preparation trials now cover 96 generated 32x24 and 4096x2304 PNGs through real worker/AppEvent completions. A fresh, non-sharing cache retrieves all 96 thumbnails with source dimensions intact and unchanged source bytes; only the nearest 64 UI textures remain, with identical IDs and zero additional texture uploads/frees during the tail. Palette/held-pointer pauses cancel the pending item without advancing progress; resuming continues, a completed pass stays idle. The actual foreground request clears preparation before a redraw and returns the full original at the correct size. An exhaustive rank-order test covers every origin in folders of 1–99 entries. Both strengthened ordinary and explicit large tests pass; retain the preceding **19 affected app passes** from `742851f` rather than rerunning unchanged product paths. App all-target Clippy/fmt/diff pass. Large-trial Debug diagnostics: nearest 64 **39.641 s**, all 96 **59.733 s**, longest completion handler **1.407 ms**, full-original wait after request submission **629.102 ms**. Source files were freshly generated/read, the preview cache was empty, and pause/resume is included; generation and later cache/source checks are untimed. No concurrent compiler; the owner's separate running application was left untouched, so this is not a controlled comparative benchmark. Reuses the existing single worker and cache limits, with one tail request at a time; no UI upload, new thread or polling loop. Plain BMP remains memory-only within the nearest working set. Warming is best-effort: disk failure, capacity eviction and changed sources can still require regeneration. Existing `0298c46` GPU first-open/refresh evidence is unchanged and not repeated. Synthetic UI/egui texture deltas plus real worker/cache checks, not new GPU pixels, physical input, Release-speed, OS-cold or peak-memory measurements. The current large PNG case keeps completion dispatch short, but does not establish responsiveness under other workloads or justify repeating unchanged decoder/GPU comparisons. No full-suite, Release/installer update or publication; remaining preview costs and wider flash reports remain open |
| U11 | Image inversion outline; accessible edge focus/status; time selection has its own updated style | Remaining physical/DPI/focus combinations; preserve hit areas and keyboard adjustments |
| U12 | Full-width compact seek track/progress in idle/hover states, inset handle travel; release paint survives discarded passes without jumping to the old transport position; CTI triangle-only drag | Remaining physical mixed-DPI verification |
| I01 | Fit/Cover implemented and tested | Retain regressions; no standalone implementation left |
| I02 | Joined reading pages, first-spread offset, count/axis/order controls, dirty/edit guards; three-spread decoded/texture entry capacity within existing byte budgets, including completed animation frames with independent presentation state; previous completed spread held through asynchronous/partial loading; active/background tab previews compose loaded page textures; viewing zoom/Actual/Fit/Cover, bounded pan and scrollbars with joined page geometry; dirty status button's gray strokes, disabled UIA state and rejected click/drag tested at three densities; generated-file loader/GPU pressure integration verified below | Remaining physical mixed-DPI continuous navigation |
| I03 | Cancellable decode and useful-neighbor prefetch adoption, queued-step priority within nine neighbors, non-consuming cache hits, sequential full-image presentation including initial opens/pending order; bounded JPEG work and measured PNG/JPEG/navigation improvements. `6d67378` retargets an outstanding same-folder Shell refresh during known-item navigation instead of restarting it; unlisted items, different folders and explicit refresh still invalidate old work | New `native_folder_notifications_preserve_initial_navigation_burst` and its large-image opt-in queue 100 Right shortcuts before initial presentation, deliver real worker notifications through app handlers and verify every production render_frame/Present acknowledgement against the returned Shell order, not injected lexical order. **101 original presentations** pass for 100 generated 2×1 BMPs (**2** accepted folder completions) and 100 generated **4096×2304 JPEGs (95 completions)**; both report **PersistedShellView**, not natural-name fallback or a live Explorer window. Later completions retain the fixture's order and no accepted direction is lost. **10 ordinary sequence tests pass / two large opt-ins ignored**, then the new large opt-in passes explicitly; app all-target Clippy/fmt/diff pass. Its **45.01 s includes JPEG generation**, not navigation latency. Test-only checkpoint; reuse `6d67378`'s production/full-app evidence rather than rerunning unchanged checks. The older real-worker/delayed-scripted-order control remains (`1a8c725`). No physical keys, pixel readback, cold-cache, IrfanView or Shell-lifetime fix claim; owned fixtures are removed after success. The opt-in native reference mode now covers real Shell order and app-event handling; current timing and limits are in Reference-folder navigation below. Only the Release test executable was updated, not the installed app or installer. Decode/outlier cost, command backlog, unexplained initial-run delay and peak/latency reduction remain |
| I04 | Image aliases, bounded numbered jumps, Shell order and reading navigation | Keyboard layout/IME/custom binding physical checks |
| I05 | Edited-pixel clipboard, resize/resample with animation-wide preflight/dialog limits, and display-only interpolation | `resize::gpu::resized_animations_recover_gpu_pixels_and_keep_undo_redo_at_three_densities` passes **24 cases**: four resamplers, two viewing samplers and 1/1.25/2x. Real asynchronous resizing of generated mixed-alpha finite animations preserves current-frame pixels, deadline, remaining plays, history and view through same-device renderer recreation; **72 full-surface recovery/Undo/Redo comparisons** and 24 wrong-frame negative controls pass. **11 related tests** and app all-target Clippy/fmt/diff pass. Test-only change; read back before Present and compare Fit with Fit because edits/Undo/Redo deliberately reset the view. Remaining: injected device loss, physical mixed-monitor transitions, full-format/large-animation cases. No physical input, Release/installer update or claim that viewing interpolation is a saved edit |
| I06 | Aspect presets, quarter/free rotation, image/video UI and reading aliases | Full-format/HDR quality and large-animation performance; native rotation/selection interactions |
| I07 | Immediate ordered image wheel zoom/pan; old full image retained during navigation; last-tab cleanup; texture cache no longer pins unused originals and renderer retains no CPU upload shadows; hidden/minimized windows suspend image-animation updates/timers | Physical latency/continuous load and wider browsing measurements; no full-process memory cap or full window-occlusion detection |
| I08 | Bounded pan, inset image scrollbar paint/hit regions, full-range dragging without selection changes, vertical/Shift horizontal wheel, licensed Chromium Grabbing cursor (see Media cursor glyphs); image/reading scrollbar pointer use releases numeric selection focus and its saved tab role | Extended `image_pan_wheel_and_bars_share_bounded_offsets_without_editing_pixels` reproduces numeric focus surviving a bar press. Both axes at 1/1.25/2x now cover held drags and batched stationary thumb clicks: hover preserves explicit focus, press releases it even before movement, saved-role removal prevents tab-return restoration, and later ArrowRight leaves selection/history unchanged. Inspect actual egui bar responses via the pinned ScrollArea/axis IDs, not rounded offset changes; disabled/held bars do not take focus. **19 targeted tests pass / two opt-in GPU trials remain ignored**, including reading navigation, display-only handoff, focus/selection and shared bar styling. The affected test was rerun after correcting nested input-lock use and separating held movement from release in its drag fixture; no full-app aggregate. App all-target Clippy/fmt/diff pass. Synthetic UI/handler evidence, not physical window-edge/seek interactions, OS wheel delivery, native UIA or complete mixed-DPI input certification |
| I09 | Corner/ratio resize, selection movement, outside clear; selection zoom fits/clears on primary press with the licensed Chromium ZoomIn cursor (see Media cursor glyphs) and held-gesture ownership, without edits, drag/released pixel metrics follow edited or retained display geometry | Native gesture combinations, compact status readability and video-corner quality |
| V01 | Up-drag opens video timeline; dedicated button and timeline hover thumbnail removed | Preserve gesture/context regressions; further timeline work is V03 |
| V02 | Shared video sheets, single fallback and main-view scrub with commit/cancel | Long-GOP/all-codec quality, first-response distributions, mixed-DPI and peak load |
| V03 | Time selection with press-seek/held CTI, shared snapped marker/stem and selection endpoint axes, preview-matched endpoint and adjustment readouts/accessibility values, owner-approved full-height gain travel, edge-to-edge track contents, start-inside range playback, video/selected-range repeat, Delete/Keep, local-band gain dragging and stretch with texture-reusing live gain preview and asynchronous pixel-column post-edit refinement, Undo/Redo and export connected; panel-height cancellation preserves media; coarse-timestamp scalar-PCM/FLAC/Vorbis/Opus and guarded AAC-LC seeks preserve the sequential sample axis; PCM/FLAC reuse verified counted/played positions and count deleted packets on trusted or sample-precise axes; compressed-audio warm-up, AAC rewind priming and cached Ogg/Vorbis/Opus EOF anchors are covered; short stretched spans retain signal with bounded EOF context and exact join lengths | Expanded gain travel now places 100% at center, 300% at the top edge and 0% one quarter above bottom. An independent six-rectangle geometry test fails on the old 22-point margin; the waveform-preview fixture now drags to the actual top instead of its old inset and retains 3x preview / one release commit / cancellation / no texture reload at three densities. The readout regression also reproduced an Out 12.500s preview reporting 10.0 to AccessKit: endpoint bounds now include the preview while numerical controls remain read-only until the model owns the extended range. **60 selection and 30 gain cases** cover discarded passes, release/cancel, displayed/accessibility values, no premature numeric actions and single commits. **29 distinct affected app tests pass**, including seekbar/fullscreen/native-session waveform controls; app all-target Clippy/fmt/diff pass. Synthetic egui/AccessKit and the existing native-session regression, not physical input/native UIA or all timeline flashes. No full-suite, Release or installer update. `af85cb7` retains the preceding duplicate gain/missing Length caption fix; `3d80df5` the In/Out preview fix. Remaining: physical CTI/selection and mixed-DPI interaction; compressed-audio seek phase, cold/interleaved prefix-scan cost, other-codec deleted-span decode cost and remaining packet reads, atempo seams/tail, exact final UI/export and real-media quality |
| V04 | Viewing/edit context, J/K/L, actual-PTS video stepping, 10ms audio stepping, temporary 2× hold; short media clicks toggle playback/replay while the video timeline is hidden; same-device seek/rate/timeline restart holds the last frame. Five-second seek notices now accumulate signed actual movement while the same four-second notice is current | `keyboard_seek_from_eof_uses_the_paused_preview_path` covers aliases, +5/+10/net reversal, expiry/replacement/absolute-seek reset, fractional boundary clamps, unchanged history and retained fault/trim diagnostics. A generated 20-second video tests accumulation; a separate original two-second video preserves real EOF/terminal-frame checks. Hidden video-only native session with audio/video command contexts, not live audio output or physical keys. Related frame-step/hold/scrub checks: **8 passed / 2 opt-ins ignored**; broader checkpoint in U05. Long-GOP responsiveness, real-media fine seek/hold and physical confirmation remain |
| V05 | Centered/bounded video pan and per-event Ctrl-wheel zoom; Zoom/Fit/Actual/Cover available with the timeline hidden; timeline-gated selection fit-and-clear with SAR/rotation and same-frame UV, in-bounds 2px right-drag movement and edited-pixel position/size status, resize/resample, same-device raster and export connected | All-format/HDR resampling quality, sustained GPU cost and native/mixed-DPI checks |
| A01 | Auto-next, repeat/shuffle, white active row, async visible durations, 0–300% playback-only volume and saved timeline gain, volume HUD; audio/video wheel preserves boundary reversals and per-event modifiers | real-media duration/row/HUD verification and OS/DPI wheel behavior; gapless and restart-persistent modes are not implemented |
| A02 | Always-visible audio timeline and shared selection/gain/stretch/range playback | Final operation audit and V03 audio-quality work |
| E01 | Audio-only output, normalization/channel options, metadata UI, PNG/JPEG/WebP fields and qualified rdf:value text, dc:subject keywords, contributor/publisher credits and four standard XMP rights properties retained after JPEG/WebP edits/conversion, unselected XMP retention for unedited same-format saves and static JPEG↔WebP supported-XMP transfer, unedited PNG/APNG/JPEG/WebP saves without recompression or other payload loss, byte-exact unedited GIF/AVIF saves, APNG poster/shared compositing, bidirectional APNG/GIF/WebP/AVIF conversions within destination limits, GIF exact-fit palettes/transparent-frame clearing and strict LZW-tail validation, WebP lossless edited animations, AVIF color/alpha and timing/loop-preserving sequence export including one frame; still/animated AVIF clean aperture/orientation, premultiplied RGBA8 restoration and static RGBA-preserving saves | AVIF fractional-pixel apertures/derived-image limitations; long-animation cost and GIF over-budget quantization/extension/metadata fidelity; full display/edit 16-bit/ICC; additional XMP fields (AlbumArtist needs an interoperable mapping), EXIF/IPTC/COM synchronization, other qualified forms or Extended XMP and other cross-format metadata; ancillary retention after raster edits/conversion |
| M01 | Direct fixed-position File/Edit/View menus and directional logo gesture | Maximized/edge/DPI/focus cases; preserve normal keyboard/menu access |
| G01 | Shared command/guard/Shell infrastructure and non-active wheel gates; lazy Shell STA initialization and current-request checks across native phases/enumeration | Overlapping Shell COM lifetimes, native-worker shutdown/observed exit wait; targeted real OS keyboard/mouse/wheel/IME/UIA, correct cursors, overlay/hover ownership and failure recovery across the changed paths |

No marker/text/color-correction feature is added by this backlog. The draft's entire ideas-under-consideration section (file-search/command palette, reading/transport-button drags, gallery and grid filmstrip), plus `-#` proposals, is explicitly thought-only; do not implement it without a new decision. Do not substitute CUDA/QSV, default Exclusive audio, registry Bags, telemetry, plugins, or a new framework.

## Evidence worth reusing

Keep results that prevent redoing an investigation. Find exact test names/conditions in the cited commit or the historical lookup described in DEVELOPMENT. Do not rerun old long benchmarks without a relevant change or new question.

IrfanView decoder-call instrumentation (2026-09-15): both sides now use fresh processes, the same System32 DbgEng/QPC boundary timing and disabled debug heap. The new `decode-call` example invokes towavue's production static decoder without warm-up; the hash-matched IrfanView 4.73 probe matches the requested path at PNG RVA `0x5fa20` and times through its return address. Skip its internal 304×16 toolbar PNG. Warm-file I/T/T/I batches, three samples per batch, report these median ranges:

| PNG case | IrfanView ms | towavue ms |
|---|---|---|
| Generated RGB 32×24 | 0.303–0.305 | 0.407–0.421 |
| Generated RGB 502×316 | 1.632–1.729 | 1.248–1.274 |
| Generated RGB 4096×2304 | 82.704–82.930 | 45.074–45.321 |
| Reference largest RGB8, 4152×3008 | 84.805–84.821 | 34.171–34.194 |
| Reference largest RGBA8, 8706×5949 | 666.718–667.696 | 210.678–212.311 |

Every sample has identical full RGBA SHA-256 across the two decoders; hashes are computed after timing by normalizing IrfanView's bottom-up BGR(A) DIB. Source hashes/mtimes remain unchanged. Reference selection inspects PNG headers only (13 RGB8 and 57 RGBA8 candidates); no image appearance, screenshot or input injection. The Release example build, three ordinary generated-PNG invocations, example Clippy, fmt and diff checks pass; no production-library path changed. No compiler overlapped timing. This replaces the earlier asymmetric warm-runtime/fresh-Irfan comparison. **Decoder evidence only, not goal completion:** small-image debugger overhead is significant; IrfanView's format sniffing/internal-resource warm-up precedes its timed PNG call, whereas towavue includes format selection and orientation. Neither includes color-image preparation, display resampling, GPU/GDI transfer, compositor presentation, physical navigation or OS-cold I/O. Other desktop processes are uncontrolled. JPEG findings follow below; broader source/format and post-decode display evidence remain open. Do not repeat these unchanged large-PNG calls. The local experimental driver, exact results and scripts remain in `irfan-info-probe-e7e32c4ee1ed4ee598c39678bb015fdf`; no installed executable was changed and no owned process remains. LLDB step-out/script/resume and `/info` did not produce usable timing; do not retry them unchanged.

JPEG comparison after exact-memory-reader specialization: fresh-process DbgEng, unchanged heap/timing/source-matching method (Irfan JPEG entry RVA `0x58db0`), I/T/T/I batches of three. Median ms: generated 4096×2304 **21.118/17.332/17.614/20.861**; largest compressed references, 4961×3508 **121.473/124.207/124.267/121.360**, 2000×4000 **74.311/80.015/79.866/74.083**, 5000×6000 **105.078/90.048/90.311/105.196**. Towavue formerly needed ~150/99/106 ms on those references (`d70591d`); it still trails on the first two, so this is not goal completion. Source hashes/mtimes, dimensions and each decoder's repeated output are stable; cross-decoder RGBA hashes still differ on all four cases. Towavue's own pixel equivalence is independently checked below, not inferred from IrfanView. New decoder-example SHA-256 `BE7C3013F2EEE3354910D79237010CA2C4285B3D45A425ECC983B19880F9AFC2`; no compiler overlap, image appearance, cold-state, GPU/presentation or installed-app update.

JPEG/WIC decision after the owner's delegated review: **retain the current decoder; do not add a partial dispatcher on present evidence.** The analysis revision at 13:33 (57,372 bytes, sections 8.8–8.12 and 11.5) identifies IrfanView's WIC branch as CMYK/YCCK with no retained usable ICC, not a size-based ordinary-JPEG optimization. Its normal RGB/YCbCr decoder retains full resolution; “same quality” does not promise cross-backend pixel identity. It neither compares backend output nor defines a rounding tolerance. A legitimate bounded JPEG reconstruction difference need not automatically disqualify a faster backend, but observed differences alone do not establish correctness or a universal bound. CMYK/profile conversion differences are a separate color-policy question, not small rounding. Preserve the existing full-resolution/orientation/editing contract; no new color-management claim.

Rebuilt `jpeg-decoder-cost` against `217982b`; old **12–25% WIC gains are superseded**, not evidence for today's dispatcher. Untimed coding metadata now reports source bytes, SOF, colorspace, sampling and retained ICC length. Warm current/WIC/WIC/current, five samples per batch, including a fresh WIC thread/COM/factory/teardown: 12 references selected across compressed-size ranks 1/2/3/4/8/16/32/64/128/232/348/461. Eight baseline YCbCr sources all have no chroma subsampling (`None`, max factors 1×1), yet benefits reverse: rank 2 **80.697/74.050/73.528/81.671 ms**, rank 3 **90.778/98.198/95.206/90.745**, rank 8 **58.959/61.433/62.813/58.743**, rank 16 **20.909/18.486/18.757/20.712**. Four progressive sources are **~1.6–2.8× slower** with WIC. RGB maximum differences span **3–5/255**; 3/255 was not a universal ceiling. Sources with and without ICC occur on both sides of the speed comparison; profile presence is not a demonstrated routing rule. All 240 reference decodes preserve geometry/opaque alpha and repeat exactly within each backend; source hashes/mtimes are unchanged. An additional generated control passes, but its relative speed varies between batches, not a robust gain. The initial wrapper completed eleven cases then rejected its mistaken rank-463 assumption (461 direct-folder JPEGs); the actual last case passed separately and selection now uses the enumerated count. No Rust/native failure or unchanged-case rerun. Retain the earlier nine owned Pillow controls (`d70591d`): simple RGB/grayscale/progressive regressions and CMYK differences up to **111/255**, without inferring which CMYK conversion is colorimetrically correct. Example Release build, Clippy/fmt/diff pass; no production change, compiler overlap, private pixels/appearance, GPU/presentation, OS-cold, memory or installed-app evidence. Revisit WIC only with a justified eligible class and orientation/corruption/cancellation/budget/COM evidence, not another permission question or a threshold fitted to a few files. Exact scripts/results remain with the local probe above.

Rejected direct APNG pipeline: `apng_edited_export_reports_sequence_cost_and_pixel_digest`, Release, generated warm files, no concurrent build/test during measurement; three samples per case, baseline→direct Balanced→direct Fast→restored baseline. Median export ms for 240×640×360: **1072.640 / 5868.660 / 3285.575 / 1097.317**; 48×1920×1080: **2165.680 / 11073.108 / 6324.022 / 2174.173**. Output bytes respectively **12212490 / 12249500 / 14127468 / 12212490** and **23676439 / 23764852 / 24351244 / 23676439**. All runs retain dimensions, frame count and 1/30 delays, with matching decoded-RGBA CRC32 digests 61239103 / 8aeea9e3; small edited-frame/metadata/poster regressions passed on the trial. This rejects that whole sequential pipeline, not proof that intermediate I/O is free or that compression alone caused the regression: threading, filtering and encoding changed together. Timing includes export/probe/staging/publication, excludes fixture creation and readback; no OS-cold, physical UI, all-media or peak-memory claim. Baseline exe/PDB is retained locally under tvbuild/towavue-apng-edit-benchmark-20260913. That trial's production changes were not retained.

Piped APNG input: same Release benchmark, generated warm files, no concurrent build/test, staged→piped→piped→staged using the retained baseline executable. Three-sample median export ms: 240×640×360 **1079.006 / 852.780 / 833.492 / 1088.890**; 48×1920×1080 **2163.626 / 1685.971 / 1711.526 / 2190.402**. Output lengths stay 12212490 / 23676439 bytes; decoded RGBA CRCs and exact dimensions/frame counts/delays match the baseline above. Removes disk write/read of the unedited PNG sequence and overlaps its generation with the unchanged FFmpeg filters/encoding; PNG compression and edited staging still cost work. Not proof of lower total peak memory: producer/compositor and helper now overlap, and no whole-process measurement was made. Existing exact small-frame/metadata/poster/conversion regressions pass. Pipe cancellation kills/reaps the child before joining workers; producer errors reject otherwise successful prefix output.

| Topic | Evidence / location | Limit or next decision |
|---|---|---|
| JPEG header preparation | `7283758` removes whole-file reads from pair-size planning: generated 512×512 read accounting falls from 642,694 to 24,576 bytes (including buffer refills). Two generated 4096×2304 JPEGs, 22,803,690 compressed bytes each, warm-file old/new/new/old, seven-sample median preflight **7.692/0.044/0.043/6.903 ms**. That checkpoint passed 39 distinct Debug loader and five Release tests, plus the hidden native initial-100-JPEG burst (101 original presentations, 84 PersistedShellView completions; 45.24 s includes fixture generation). The separate preview header now borrows its existing bounded buffer and obtains dimensions/orientation in one parse, eliminating a second compressed copy and repeat parsing. `borrowed_preview_headers_report_adapter_cost`, one generated JPEG of the same size already in memory, 15-sample adapter/borrowed/borrowed/adapter medians: **2.3635/0.0044/0.0044/2.3610 ms**. **14 distinct affected Release tests pass**, including both explicit Pillow controls and the timing opt-in; workspace all-target Clippy/fmt/diff pass | Full thumbnail pixels match the adapter path for eight orientations at four resolutions and nine independently encoded RGB/grayscale/CMYK/progressive/direct-RGB cases. Invalid EXIF values, every truncated-header prefix, budget/cancellation, disk reuse, geometry and held-original preview suppression remain covered. Backend/options, FFmpeg reduction, cache/input limits and source pixels are unchanged. Measurements isolate header preparation, not total preview generation, navigation, peak memory or cold-disk/physical-input/IrfanView performance. Large metadata can still require longer parsing; no fixed header cap or whole-process memory claim. No repeated private-reference/native-navigation benchmark or installed-app update for the preview-only change |
| Paired original-decode concurrency | Paired decoding is now the normal default; the serial override remains verification-only. Post-promotion same Release binary, serial/default/default/serial, 540 references, 33 ms commands, hidden hardware D3D11: forward p95 **46.225/18.103/17.821/55.979 ms**, peak commit **1451.5/1448.5/1438.6/1411.8 MiB**, queue **5/3/3/5**; reverse p95 **21.346/17.377/17.312/21.994 ms**, peak commit **1291.7/1311.8/1382.5/1326.5 MiB**, queue **6/4/4/6**. All eight runs preserve 540 originals in lexical order, zero blanks/previews and unchanged source stamps. Prefetch waits fall **28–35→5–8** forward, **16–18→2** reverse. Largest PNG Present: forward ~14258–14269 ms; reverse serial ~3895–3906 versus default ~3829–3836 ms, so outliers remain. OS caches were not flushed; no in-test predecode, compiler overlap, private readback/screenshots, physical keys, cold-disk or IrfanView claim. Reverse process peak commit can increase; retained cache limits are unchanged, not a whole-process memory bound. Pre-promotion comparisons are in `263b1e0`, isolated full-result equality in `4ac5461` | Normal construction (without an enabling override) now drives the six PNG/APNG spreads: full frames/delays/plays, static Arc reuse and four-frame shared budget pass. The ten-case gated matrix includes first-publication reassignment, either/both-image adoption, changed sources/tails, clear/close, nonblocking UI cancellation and retirement of both decoders. `e12659e` fixes unwanted helper ownership after first-image-only adoption. **Release workspace: 976 passed / 61 ignored / two known exclusions**, plus workspace all-target Clippy/fmt/diff; 36 affected Debug loader tests also pass. No native failure in this aggregate, not a Shell root-cause fix. Eight navigation opt-ins pass separately. No test executable remains running; installed app/installer unchanged. Continue remaining UI/feature and non-steady/cold-input work; do not repeat unchanged steady-direction or isolated throughput trials. Cache 384 MiB and ten-path plans remain unchanged |
| Media cursor glyphs | Owner-approved Chromium ZoomIn/Grabbing artwork is embedded as exact text-encoded RGBA from pinned Windows CUR resources, with original license, source hashes, conversion details and hotspots in [asset provenance](../crates/towavue-app/assets/cursors/README.md). Two native winit cursor objects per window replace Windows arrow/move fallbacks only for these media roles; DPI changes rebuild them, ordinary redraws reuse them. Normal output respects pointer presence, idle hiding and restoration; tab/filmstrip Move/NoDrop and native caption drawing are unchanged | **15 affected cursor/image/reading/video/host-feedback tests pass; three unrelated reading GPU tests remain ignored.** Pixel/hotspot checks cover 100/125/150/200%; an isolated real Windows event loop creates distinct native cursor objects for both assets at all four scales and verifies selection of custom versus unchanged ordinary mappings, without setting the desktop cursor or opening a visible window. App all-target Clippy/fmt/diff pass. This is native allocation plus scripted ownership evidence, not native glyph readback, physical pointer restoration or mixed-monitor appearance certification; supported native input remains unavailable. No new decoder/dependency, Release/installer update or publication. Old candidate notice inventories were not refreshed; any later distribution containing these assets must include their Chromium license. |
| Image-color conversion | Adopted `23c70e6`'s packed conversion as the normal app entry, replacing `17740c9`'s integer rows. The x64 helper processes four pixels with an opaque-block shortcut, scalar tail/fallback, one output allocation and no worker/scratch canvas; its algorithm is unchanged from the measured candidate. Pinned Color32 layout and initialization/bounds invariants stay behind the safe runtime interface. Historical scalar/lookup/parallel controls remain verification-only. Adoption targets the largest measured color stage's **34–38 to 25–26 ms** reduction in contiguous UI-thread work, not a guaranteed whole-app speedup or memory reduction. Reuse the candidate's Release evidence: isolated 8706×5949 scalar/packed/packed/scalar medians **30.188/21.739/22.159/30.700 ms**, full equality outside timing. Same Release app, 540 references, 539 commands at 33 ms, 16 ms event-free redraws and accepted cache/paired-prefetch policy: forward p95 **27.225/24.109/24.535/34.688 ms**, reverse **16.582/16.368/15.473/16.530 ms**. All eight hidden hardware-GPU runs preserve 540 originals in lexical order, zero blanks/previews and source stamps | Historical forward peak commit **1415.7/1485.9/1454.8/1456.2 MiB**, reverse **1347.6/1384.8/1380.0/1346.8 MiB**: reverse packed peaks are about 33–38 MiB higher despite unchanged output allocation. Completion-event totals fall from **1154–1201 to 1032–1033 ms** forward and **1164–1176 to 1012 ms** reverse, but global reverse latency differences are small. Largest-PNG scheduled-command-to-Present delay remains **131–175 ms** forward and **186–199 ms** reverse. Promotion checks: full Debug workspace/all-target tests exit 0 with the two existing exclusions; workspace all-target Clippy/fmt/diff pass. Current Release kernel **1 pass** (65,536 component/alpha pairs and 1,728 unaligned/tail cases), example **3 pass / 1 timing opt-in ignored**, plus Debug app normal-entry equality. Explicit hidden hardware handoff **PASS** covers 3 densities × 3 alpha patterns, 12 supersessions each, central pixel equality, preview suppression, shared-device renderer recreation, replacement and held decoded release. No repeated reference benchmark, cold-cache, physical-key, Shell-order, private-image readback/screenshots or IrfanView claim. Release runtime/example were rebuilt, not the full Release app or installed executable. Preserve outlier/memory caveats and do not repeat unchanged comparisons |
| Two-way image-color experiment | Shared verification-only helper, one scoped worker plus caller, initialized output; product stays sequential. Earlier isolated full-pixel controls in `e4ec87b`: reference PNG (8706×5949) ~35→27 ms, dense-alpha benefits, opaque 9.4/51.8 MP no meaningful gain. `588be1a` same-Release-binary app lookup/two-way/two-way/lookup, 540 warm sources, 33 ms forward commands, accepted cache policy and unthrottled event-free redraws: navigation p95 **57.837/60.902/64.503/68.995 ms**; median **5.638/6.079/6.234/5.687 ms**, max queue **5** throughout. Selected PNG color **44.898/31.004/34.639/39.911 ms**, native texture creation **45.942/47.728/47.432/48.149 ms**. Completion-event preparation totals **1095.515/1282.483/1291.394/1088.032 ms**. Process peak working set **741.2/751.4/754.3/752.6 MiB**, peak commit **1453.7/1471.0/1404.8/1450.3 MiB**. Every run preserves all 540 originals in order, zero blanks/previews and source stamps. Shared 72-case equality and instrumented-entry tests pass (two example checks, three targeted app checks including existing alpha/phase coverage); app all-target Release Clippy/fmt/diff pass | Reject all-size adoption: selected conversion improves, but global p95 ranges overlap and ordinary preparation costs more. Do not repeat unchanged all-size or isolated trials; any future selective use must avoid small-image overhead and prove whole-app benefit, not just one outlier. No size threshold/pool, cache-policy or installed-executable change. Timings include initialization and worker creation/join; calling-thread CPU excludes the worker. Warm lexical hidden-GPU evidence, not physical keys, cold/Shell-order/IrfanView comparison or whole-process bounds. Pixel equality is checked separately, not during timed navigation; no reference screenshots/readback. No new full-app aggregate or resolution of the historical multi-second outlier; continue remaining UX/function work |
| Calling-thread CPU attribution | New verification-only CPU accounting, Release app/isolated-color/app, largest reference PNG (8706×5949), no compiler overlap. App color wall/CPU ms **36.249/31.250** and **53.766/46.875**; GPU submission wall/CPU **54.270/46.875** and **62.446/62.500**, native texture-creation wall **47.248/53.597**. Isolated first conversion **35.047/31.250**; current five-sample wall medians **32.748/32.778 ms**, CPU totals **187.500/140.625 ms**. Both 540-image app trials preserve order, zero blanks/previews and source stamps; p95 **169.621/164.393 ms**, max queue 7. Native-counter, conversion equality, phase-accounting and example tests plus workspace Clippy/fmt/diff pass | OS accounting is visibly quantized and can exceed a short wall interval; do not subtract it as exact waiting time. CPU time increases in the slower color trial, and submission CPU remains substantial: these observations do not support treating pure waiting as the sole cause. CPU execution/memory/resource-history attribution remains next, not another unchanged idle-redraw trial. No isolated native-creation CPU, frequency/cache-miss counters, CPU-versus-memory-stall separation, production speedup, cold/physical-input or IrfanView claim; earlier multi-second outlier remains unresolved |
| Image / reading Fit transitions | Normal images also reproduced rounded-up Fit overflow: **113×92 → 480.00003** in a 640×480 viewport; **113×94 at 125% DPI** still overflowed after correcting the physical scale. Shared `fit_scale` lowers its uniform scale by one floating-point step only when needed, and normal-image drawing/preview/held/hover geometry uses logical-unit Fit without a physical-DPI round trip. Actual/Custom/Cover retain their physical-scale conversion. `fit_does_not_create_subpixel_overflow_for_integer_image_sizes`, `logical_scale_preserves_non_fit_physical_zoom`, `fitted_image_scale_does_not_overflow_after_dpi_conversion` and `fitted_image_and_handoff_do_not_paint_phantom_scrollbars` cover bounded edges through 16384, 1/1.25/1.5/2x, no painted phantom bars and identical held image meshes. Full Debug workspace tests pass with the two existing exclusions; workspace Clippy/fmt/diff pass. Earlier **8761d18** corrected fractional reading Fit: joined height 10496.001 → 480.000031, **928 changed GPU pixels** on handoff. Its real-file hidden GPU test passed **90 pending frames (57 partial completions)**, **36 completion/next-frame pairs**, **252 interior/animation/cache/recovery checks**, and **27 foreground decodes / 81 cache hits**, with source stamps and close-time release preserved | Current normal-image evidence is math and egui shape geometry, not physical input/compositor capture. Earlier reading evidence is hardware readback of the whole 640×480-logical image surface, not window chrome; it used owned mixed-orientation PNGs/64-frame APNGs, fixed order and stepped animations. That opt-in real-file GPU trial was not repeated for the normal-image fix. No cold-disk, latency, peak-memory, Explorer ordering or monitor-movement claim; cache budgets and texture limits are unchanged. The wider reading/filmstrip report remains open. No Release executable, installer update or publication |
| Reading cache pressure | `fd93137` fixes three decodes instead of one for a partially cached three-page request; normal `image_loader::tests::pressure` covers identity, changed sources, budgets and cancellation. The opt-in `large_reading_spreads_preserve_pixels_and_cache_hits_under_byte_pressure` now uses eight 4096×2304 PNGs and four 512×512/128-frame APNGs: 200 MiB/spread, production 384 MiB cache, 36 traversals/108 pages, 11 partial-cache requests, exactly 86 decodes and 22 original reuses. Full-frame CRCs, delays/plays, weak identities, cache accounting, unchanged source stamps and complete close-time release pass. Three pressure tests and affected-runtime Clippy/fmt/diff pass; production code is unchanged since the **936-pass** workspace checkpoint | Generated warm-file runtime stress with the preceding spread retained, not GPU presentation, physical input, codec-cost or whole-process memory measurements. The first trial passed page checks but exposed an incorrect expected partial-request count (12, actually 11 with the cold first traversal); the corrected run passes through cleanup. Borrowing adds no pixel copy or cache capacity, but extends evicted originals' lifetimes within the request. Single-image navigation is unchanged |
| Status file details | `right_status_resize_bounds_gpu_color_drift_and_detects_pixel_displacement` adds hidden hardware readback of actual status text: three media kinds, 1/1.25/1.5/2x, 960-point base plus forward/back 0–39 physical pixels. **948 fixed-right-edge glyph-region comparisons** have at most one 8-bit color-code difference; 822 differ bytewise, at most 96 RGB bytes per frame, with opaque alpha unchanged. Twelve one-pixel-shift controls exceed that bound. Initial exact-equality assertions failed; this is explicitly a bounded color-drift diagnostic, not a byte-exact pass or another product fix. The opt-in GPU test, **five ordinary status/resize tests**, app all-target Clippy/fmt/diff pass. Earlier `right_status_anchor_tracks_each_physical_resize_pixel` fixed 7px↔8px inset drift at 125% using the parent edge and rounded inset; its 1,920 anchor / 960 glyph-mesh checks remain. Single-tooltip, cached/read-once metadata, stale/close and held-image checks remain | Owned hidden D3D11 surface before Present, fixed synthetic metadata, unelided text; not physical right-edge dragging, DWM composition, clipped-text transitions, or proof the owner\'s shimmer is resolved. No private media, screenshots, renderer/production changes or installed-executable update. Do not repeat unchanged anchor or fixed-label GPU sweeps; a different native/compositor trigger is needed. Existing UTC/Tokyo/Pacific and historical DST evidence remains; automatic refresh after an OS time-zone change is unverified |
| Tab audio controls | The native playback-volume regression reproduced retained mute-button focus after a cancelled pointer drag, then exposed a saved active-control role surviving an unfocused cancellation. Pointer press now clears the active media's saved role; release clears button focus. Background-tab roles, activation, edits, playback generation and mute/drag separation remain unchanged. Both mute states at 1/1.25/2x also preserve explicit accessibility focus/activation. The earlier 5-point/15-point spacing imbalance remains fixed without changing the 24-point hit region | Expanded native test passes with hidden D3D11 and generated silent WASAPI sessions; input/layout assertions are synthetic, not physical mouse/native UIA. App rerun **479 passed / 18 ignored / two known exclusions**, Clippy/fmt/diff pass. Initial aggregate hit Shell abort 38704, preserved below; passing reruns do not fix it. No installation |
| Playback availability | `status_play_button_and_commands_follow_playback_availability` covers audio/video, all five states, unknown/zero/restored duration and 1/1.25/2x: disabled semantics, dimmed paint and rejected synthetic pointer/AccessKit clicks. Negative controls reproduced enabled Loading and empty-timeline commands. The native timeline round-trip additionally checks full deletion, tab restoration, Undo/Redo and restored playback; normal video and opt-in muted shared-audio sessions pass. Latest affected checks: **475 app passes**, one additional native-audio pass, app Clippy/fmt/diff; the normal app run retains 16 opt-in tests plus one example ignored and two existing exclusions | Scripted egui semantics/input and native media sessions, not physical input or native UIA delivery. Known-zero timeline duration now disables Play/Pause with an empty-timeline hint; unknown duration and repeat/shuffle/viewing preferences remain available. No installation or Release certification |
| Image loading handoff | `reading_layout_reload_preserves_matching_animation_presentations` reproduced frame 1 restarting at 0 on a page-count reload. Exact shared decode identity now reuses the held animation's texture, frame, deadline and remaining plays for primary/neighbor pages; running and finished finite animations survive, while new decode ownership and errors replace old results. Expanded `reading_layout_changes_preserve_the_complete_gpu_surface_until_ready` preserves all four presentations across same-source reloads: **24 whole-surface comparisons and six restart negative controls**, both axes at 1/1.25/2x. The existing **42 held comparisons**, six partial-layout controls, six stable layouts and six animation resumptions still pass. **36 affected normal tests passed / 4 opt-ins ignored** (the expanded GPU test passed separately), plus app all-target Clippy/fmt/diff. `c642b31` fixes held mutable-texture animation updates (131128-pixel reproduction); `c9404f5` fixes count-change reflow | Hidden hardware D3D11 readback of the complete 640x480-logical image surface with generated decoded frames and scripted chunks/deadlines; no physical input, chrome, arbitrary animation-format decoding or performance claim. Keep native-only DPI setup/effective-density assertion. Existing supersession, graphics recovery, error/close release, hidden scheduling, finite-loop/catch-up, sampling and static-cache/device-limit regressions pass. Reuse is local to the held spread, not an animation cache; capacities/workers and source pixels are unchanged. Broader filmstrip flashes and physical reading vibration remain open. No new full-workspace/Release build, installer update or publication |
| Direct JPEG RGBA and exact memory reads | The direct YCbCr/grayscale path now uses a safe local `JpegReader` to replace zune's std exact-read loop with a single bounded slice copy; failed reads retain partial-buffer writes and leave position unchanged. Other operations delegate to the pinned ZCursor. No decoder arithmetic/options, orientation, cancellation, fallback or dependency changes. **28 affected Release tests and three explicit opt-ins pass**: exact/short/past-EOF reads, RGB/gray, odd/tiny sizes, EXIF transforms, limits/cancellation, truncation, sniffing, paired prefetch/export, nine Pillow color/progressive fixtures and all **463 reference JPEGs** with exact RGBA/dimensions/delay. Runtime all-target Clippy/fmt/diff pass. Isolated reader, already-read bytes, generic/specialized/specialized/generic five-sample medians: generated **16.672/15.342/15.310/16.558 ms**; compressed reference ranks 1–3 **137.245/115.545/115.729/137.189**, **89.159/74.816/75.220/89.603**, **98.533/84.944/83.928/97.901 ms** | The 463-file check compares the older RGB/Luma adapter with current direct RGBA plus reader, not reader alone: 926 samples each, median **14.778/12.863 ms**, p95 **64.473/57.095 ms**, total **20.340/17.568 s**. Sources unchanged; no pixel output, compiler overlap, GPU or worker/cache timing. Owned-process main-thread RIP sampling motivated this change: 677/983 largest-JPEG samples resolved near `decode_mcu_block`; hot-address disassembly confirmed coefficient lookup and four-byte refill loops. This includes waits/suspension and nearest-public-symbol ambiguity, not CPU-active attribution or speed evidence; no global trace was started. Keep direct-RGB/CMYK/YCCK and strict-failure lenient adapter fallbacks. Rotations may allocate another canvas; no whole-process-memory, all-corruption, cold-state or full-app navigation claim. Profiler artifacts remain in the local decoder probe directory; first-call results are above |
| Compressed audio seeks | b7ab02a: 250 ms decoder preroll, negative-time AAC rewind priming and prior-page Ogg/Vorbis cached EOS anchors. `compressed_audio_preroll_keeps_seek_samples_and_rewinds_reused_inputs` compares seeded four-second native MP3 (44.1/48 kHz), M4A/AAC, Ogg/Opus and Ogg/Vorbis across both directions, zero, EOF and cancellation. Fixed AAC rewind 11792/12000 samples and Vorbis EOS 12000/3984 samples; local FFmpeg `oggparsevorbis.c`/`seek.c` explains the cached page boundary | Ignored `compressed_audio_seek_reports_sequential_alignment`, 48 kHz, 250 ms windows at 17/617/1017/2917 ms: MP3/FLAC/Vorbis in native/MKV and native Opus match sequential decoding exactly. AAC noise/phase controls are separated below; baseline MKV Opus max 0.000422 (expanded controls/fixes below); former MKV Vorbis ±8-sample sparse lag now resolves to zero with byte-exact output. Sparse probes are not complete phase proofs. No listening, all-codec, cold-seek or latency guarantee |
| AAC noise versus timestamp phase | `aac_seek_without_noise_substitution_matches_sequential_samples`: four-second seeded stereo PCM, encoder PNS on/off, 44.1/48 kHz M4A plus same-bitstream MKV. Now covers both directions, zero/EOF, cancellation/reuse, exact PNS-off samples, matching counts and deterministic seeks with either setting; less than one second is decoded per 250 ms window. Baseline 04dd6de MKV/44.1 kHz offsets 0/+1/-4/-1 and 11024/11025 frames now resolve to zero/exact counts | `aac_prefix_requires_lc_frame_length_and_matching_packet_duration` guards profile, declared frame length, precise time bases, initial decode, duration and metadata-element fallbacks. Decode initial priming rather than simulating it: the new decoder inherits delay from initial_padding. Prefix counting still reads the file and does not validate skipped payloads. PNS random state explains remaining noise-byte differences (local FFmpeg `aacdec.c`/`aacdec_proc_template.c`); do not disable PNS or require whole-prefix decoding for its byte identity. No HE-AAC/other-profile, listening or cold/long-file latency certification |
| Opus sample phase and priming | `opus_seek_preserves_sample_phase_across_packet_durations`: four-second seeded 48 kHz stereo PCM, 2.5/5/10/20/40/60 ms packets (128k/audio), plus 20/60 ms (24k/voip), native Ogg and same-bitstream MKV. Seven reused/fresh positions and cancellation verify exact required counts, zero full-overlap offset, bit-exact native Ogg and 2.5/5 ms MKV. Baseline MKV/2.5 ms offset +24 is fixed. Missing decoder pkt_timebase caused 40/60 ms initial windows to return 11688/11640 instead of 12000 frames; now exact. Reused Ogg/60 ms at 3.917 s returned 0/3984; prior-page anchoring fixes it | `opus_prefix_counts_match_decoder_priming_gaps_and_invalid_header_fallback` covers the decoded/count-only sample axis, initial skip, a 100 ms gap, malformed headers and subsequent side data. FFmpeg decode.c needs pkt_timebase for skip-sample timestamp correction; oggparseopus.c cannot reconstruct the first EOS-page timestamp, like Vorbis. Remaining MKV warm-up residual reaches 0.00007403, not a proven listening defect or bit-exact output. Coarse seeks still read prefix payloads; MKV windows decode <1 s, Ogg may need an extra page (<3 s in this fixture). No cold/long/interleaved cost or all-mode/multichannel certification |
| Timeline atempo EOF | `short_tempo_spans_retain_signal_with_bounded_eof_context` retains signal/counts. Opt-in `tempo_eof_context_reports_signal_coverage` compares no/current/doubled zero context: 48/480/2400/9600/48000-frame 440 Hz antiphase stereo tones, 48 kHz, 127-frame pushes, 0.25/0.5/1.5/4x. Current/doubled output is bit-identical in all 20 comparisons. At 0.25x, 480 input frames have 1220 trailing quiet output frames (~25.42 ms); 48000 have 782 (~16.29 ms). The new opt-in `reflected_tempo_eof_context_reports_signal_and_silence_coverage` uses the same sizes/rates, both uninterrupted tones and a silent final input quarter, comparing current zeros with 4096 frames reflected from the last min(input,4096) frames. Forty pairs retain exact output length, finite samples and antiphase stereo | Reject both doubled zeros and this reflected-tail substitution. Reflection removes the 782-frame quiet tail on the one-second tone, but a 2400-frame input with 600 silent frames at 0.25x produces RMS **0.105726** in its final output quarter (zero context: **0**), with no trailing quiet samples instead of 3213. The diagnostic asserts this counterexample: more nonzero output is not proof of better fidelity. Quiet threshold is abs <=0.0001, not a perceptual rating. Evaluate a different boundary-aware time-stretch strategy against intentional silence as well as tone coverage, shared playback/export and exact joins; no tail synthesis or dependency change has been accepted. Existing finite context still prevents short input disappearing entirely. Five normal tempo tests plus the reflection diagnostic pass; no new listening, arbitrary-signal, ordinary-chain or universal pitch-quality evidence |
| Ordinary playback tempo EOF | 3080d52: `ordinary_tempo_keeps_short_input_and_exact_duration_after_queue_consumption` covers 105 cases: 44.1/48/96 kHz, 0/48/480/2400/one-second frames, 0.25/0.5/1/1.1/1.5/2/4x, 127-frame pushes and immediate queue consumption. Exact output ceilings and non-silent short input pass; 1x is byte-identical and invalid rates fail. Existing tone pitch/stereo checks pass. `live_drain_keeps_pause_and_resume_valid` separately passes muted default-WASAPI drain/wake and post-drain pause/resume for 4800 frames at 1/0.25/4x and 48 frames at 0.25/4x | Same two-stage tempo algorithm, finite context shared with timeline processing. Pending output is not discarded before EOF or published beyond the received-sample budget. Native testing used silence/mute and does not prove listening quality, hot endpoint changes or all hardware; no whole-process memory/latency guarantee. Ordinary export uses a different existing chain and is not covered by these playback assertions |
| Ordinary export tempo EOF | `ordinary_rate_export_preserves_short_audio_counts_and_channels`: 36 cases at 44.1/48/96 kHz, mono/four channels, 0.25/1.1/4x and fractional-sample trim boundaries. A 480-frame 44.1 kHz/0.25x export previously failed with no audio frames; it now retains signal and the exact sample-count/rate ceiling. Longer-video/longer-silent-track fixtures verify selection of short default audio, mono duplication, video/audio-only equivalence and normalized -1 dBFS peak. Twelve cancellation/source-change combinations retain existing targets; integer-statistics validation and app indeterminate/cancel/save/tab-switch/UIA checks pass | One extra read/decode pass for ordinary rate-changed exports with audio; normalization therefore takes three passes. No whole-audio PCM buffer/intermediate file, but no cold/long-file cost measurement. Lossless PCM output counts are verified; compressed output framing, arbitrary seam quality and audible tails are not certified. 1x and timeline paths retain their existing processing |
| Compressed export timing | `compressed_rate_export_matches_timestamped_pcm_through_trim_and_eof`: MP3/AAC/Vorbis/Opus/FLAC in native containers and same-bitstream MKV, stereo 44.1/48 kHz, whole/interior/EOF trims and 1/1.1x (60 cases). Timestamped float PCM in NUT retains a zero-time video origin anchor; packet sample extents independently bound output counts. Export matches within one 16-bit quantization step. `short_rate_exports_keep_signal_and_gapless_container_lengths`: 480 mono frames at 48 kHz, 0.25/4x, WAV/FLAC/MP3/M4A/Ogg-Opus/Opus (12 cases), exact decoded lengths and nonzero signal | A raw WAV reference wrongly erased Vorbis/MKV's 3 ms initial timestamp and MP3/MKV's small FFmpeg timestamp adjustments; those apparent trim failures do not justify discarding timestamps. Local FFmpeg `ffmpeg_dec.c::audio_ts_process` uses sample-aware rescaling and gap resets. Not all codecs/bitrates, raw ADTS padding, arbitrary discontinuities, playback equivalence, listening quality or long/cold performance |
| FLAC/Vorbis prefix counting | `coarse_audio_seek_preserves_sequential_samples_without_decoding_the_prefix` covers both codecs at 32/44.1/48/96 kHz, both directions, zero/EOF and cancellation/reuse; less than one second is decoded per 250 ms window. Baselines: FLAC 7997/8000 requested frames at 32 kHz; Vorbis 11024/11025 at 44.1 kHz. FLAC header and Vorbis priming tests compare counts with actual decoder output, variable block/overlap lengths and a 100 ms gap; invalid headers never reuse stale duration | FFmpeg's existing header parsers are shared, not new bitstream implementations. Vorbis counts no samples for initial priming; a fresh decoder's discarded warm-up packet still advances the reconstructed axis. Only exact full-first-packet Vorbis SkipSamples is accepted by counting; other side data/unavailable or invalid parsing falls back. Still reads prefix packets without validating skipped payloads; no cold/interleaved/long-file latency guarantee |
| Native thumbnail persistence | All native image thumbnails now transfer their generated RGBA allocation instead of decoding their new cache PNG again; extends the earlier fast-JPEG path to streamed PNG and native static/animation fallbacks. Encoded-only helper results and existing disk entries still use PNG validation. **69 preview-related Release tests pass / 13 opt-ins excluded**, including exact alpha/sampling, first animation frames, geometry, disk repair/reuse, cancellation, source changes, coalescing and occupied-path fallback. The new timing opt-in passes separately after its Debug-only guard was changed to satisfy Clippy; runtime all-target Clippy/fmt/diff pass. `native_thumbnail_publication_reports_round_trip_cost`: generated noisy RGBA PNGs, warm files, independent empty caches, same-binary re-decode/ready/ready/re-decode batches, 15-sample medians: 32×24 **0.957/0.703/0.710/0.895 ms**; 240×160 **1.461/1.248/1.241/1.459 ms**; 1920×1080 **3.852/3.766/4.978/3.858 ms**. All 180 outputs match full-decode nearest-resize pixels and fresh-memory disk geometry/pixels; source bytes/mtime unchanged | Small-source improvement only; the 1920×1080 result is variable, not a speedup claim. Timing includes decode/sampling, PNG encoding, persistence and publication, excludes fixture/cache construction, correctness readback and GPU/UI; no concurrent build/test, unrelated desktop work uncontrolled. No peak-memory, OS-cold, populated-cache, IrfanView or whole-app latency claim; no installed-app update. Cache keys, PNG encoding/geometry, cancellation and 64 MiB disk / 64-entry/16 MiB memory limits remain unchanged. Earlier pruning controls remain in Git at `f7f4c1a`: enumeration still costs ~3.3 ms at 8192 entries; under-limit sorting was removed. Foreground `remember_image` only seeds memory. Retain earlier ~27 ms generated 6000×4000 JPEG preparation / ~3.5–3.9 ms fresh-memory disk reuse evidence; BMP persistence remains rejected because it slowed ~1 ms preparation |
| Shell enumeration batching — not adopted | Release `shell::enumeration_tests` compares indexed `IShellItemArray::GetItemAt` with `EnumItems`/32-item `Next` batches, using a fresh view-ordered array and identical path/kind/PIDL resolution each time. Indexed/batched/batched/indexed, 15-sample medians: hidden owned 32-item folder **0.051/0.050/0.049/0.050 ms**; 1024 items **1.728/1.670/1.653/1.716 ms**; existing live Explorer reference view, 565 items **1.606/1.683/1.660/1.597 ms**. All three controls preserve exact supported-item order, paths, kinds and Shell identities. The two opt-ins pass; runtime all-target Clippy/fmt/diff pass | Tiny hidden-view gain does not carry to the live view; keep product enumeration and refresh policy unchanged. Retain the test-only comparator, not a product branch or cache. Warm view enumeration only, excluding folder parsing, view discovery/creation, sort metadata, UI delivery and equality checks; no compiler overlap, other desktop work uncontrolled. Live trial only reads an already open matching view, with no sort/window/input changes or image appearance/readback. Not an IrfanView, cold-state, shutdown, cancellation or whole-navigation benchmark. Next investigate another path rather than repeating unchanged batching trials |
| Animation thumbnail generation | Release `animation_thumbnail_native_generation_reports_helper_comparison`, generated two-frame RGBA fixtures, warm files, uncached generation, same-binary helper/native/native/helper with three samples per phase. Median ms: GIF 1920×1080 57.458/8.327/8.218/55.636; APNG 4000×2400 327.339/70.197/70.313/328.858; WebP 1920×1080 69.526/13.380/13.176/69.248. Every native result matches nearest-resized original first-frame pixels; both paths have the same output dimensions | Includes helper process startup, decode, resize and PNG encoding; excludes fixture generation, OS-cold reads, cache persistence, GPU/UI and memory measurement. Old helper uses its scaler, so this is not old/new byte identity or all-media quality certification. One fixture per format, not long-animation/all-format guarantees. JPEG/BMP already take optional fast preview paths; no duplicate implementation needed |
| Monapad caption reference | Local monapad uses titleBarOverlay height:36 with installed Electron 36.9.5. Matching [WinFrameView::LayoutCaptionButtons](https://github.com/electron/electron/blob/v36.9.5/shell/browser/ui/views/win_frame_view.cc) lays out the overlay and calls SetButtonSize; [WinCaptionButton](https://github.com/electron/electron/blob/v36.9.5/shell/browser/ui/views/win_caption_button.cc) paints backgrounds/symbols on a canvas, using WinIconPainter/Win11IconPainter | Electron's native-looking overlay is not OS/DWM-drawn caption controls. Matching its arbitrary height/position by copying that strategy would change the owner's no-custom-controls contract and add paint/input/accessibility maintenance. Keep the current DWM path unless the owner explicitly chooses otherwise; exact reference inspection does not prove all possible DWM approaches impossible |
| Native caption geometry | `caption::tests::native_caption_owns_subclass_and_preserves_winit_close_delivery` first fails on a new hidden 960×576 window: bounds x885..960/y0..31 (75 px) rather than the existing x806..960/y0..32 default. The visibility guard now passes. With TOWAVUE_CAPTION_VISIBLE_GEOMETRY=1, owned windows on three 100%/150% monitors cover normal/maximized/restored states, native center hit codes, exact left reservation, child-region exclusion at centers/bottoms, hidden/minimized fallback and cached clip, plus fullscreen monitor movement/restore and close delivery. Caption tests: 2 passed; visible opt-in: 1 passed; standalone/shared-caption resize: 1 passed; explicitly enabled shared hardware session: 1 passed with zero playback CPU transfers | [DWM documents undefined caption bounds while minimized or not visible](https://learn.microsoft.com/en-us/windows/win32/api/dwmapi/ne-dwmapi-dwmwindowattribute). This proves the nonempty hidden-bound failure, not the owner's entire visible clipping trigger. Native button-bound heights remain 30/23 px normal/maximized at 100%, 44/36 px at 150%; normal right gaps are 7/10 px, maximized Close reaches the edge. No custom controls or placement changes. Message-based hit queries and visible native geometry, not physical button input or a new full-app GPU/whole-workspace certification. The earlier same-size cutout synchronization fix is c5f5ccb; do not repeat unchanged geometry trials. The owner confirmed maximization via the upper-right button; the accepted native-button height contract is unchanged |
| Tab drag and scrolling | 9c01712 covers fixed-thin scrollbar ownership/wheel, release-only geometry, batched gestures, multi-density anchors and whole-strip edge scrolling. `drag_feedback_tracks_local_ownership_and_restores_after_release_or_cancel` checks local Move/invalid NoDrop, stale graphics, held capture after PointerGone and restore-once without a redraw; owner-switch/hidden-cursor restoration and all standard icon mappings have focused tests. The hidden native host trial checks valid merge/detach and blocked-target effects with existing live transfer/gap/dirty-state guards | Scripted UI/cursor application callbacks; hidden native trials inject OS hit selection and deliberately do not set the desktop cursor. Actual cursor visibility/restoration remains unverified. `egui-winit` skips outside updates, so the host reapplies native feedback after events; Move/Grab/Grabbing share Windows IDC_SIZEALL, NoDrop/NotAllowed share IDC_NO. Detach uses Move, not a distinct glyph. The native placement trial exposed an old half-pixel expectation error: quantize release and grab offset separately, then subtract; no placement behavior or tolerance changed. No OLE/helper window/custom cursor |
| Filmstrip interactions | New `gpu_filmstrip_refresh_completions_preserve_displayed_pixels` uses three owned 2×1 BMPs and actual image/preview workers, normal/two-page reading at 1/1.25/2x. **18 unchanged-list refreshes, 54 verified texture-ID replacements, 36 exact media-region comparisons and 18 completed whole-surface comparisons** pass; reading reloads finish with the handoff released and no image errors. App all-target Clippy/fmt/diff pass; no product change or full-app rerun. The first fixture compared before status metadata arrived (1944 changed pixels); waiting for it resolves that difference. The next difference was the intentionally disabled reading button (168 pixels at 100%); compare the complete dimmed media region during loading and the entire surface after completion, retaining geometry/length gates. Earlier closed-refresh and reopen fixes retain ready previews until actual source validation replaces them; `c80c63d` records 276 GPU frames/36 opens with refresh deliberately unresolved, and `b12fbca` fixes the delayed scrollbar reveal | Newly covers real refresh completions, not just held requests. Warm tiny fixtures, synthetic UI and hidden hardware readback, not the owner's physical flash, Explorer enumeration, cold/latency/peak-memory, arbitrary animations or changed-file behavior. Whole-surface equality is not required while controls correctly disable; media-region equality is. Existing changed/deleted-source, cache retention and UI regressions remain; do not repeat these unchanged refresh controls. Wider filmstrip/reading flash reports and native-input delivery remain open; next investigate a different trigger rather than marking the report resolved. No private image readback, Release/installer update or publication |
| Playback failure | `active_playback_failure_stops_the_clock_without_discarding_edits` reproduced the running faulted clock. `playback_tab_tests` verifies muted WASAPI stops before EOF and late first-frame/native Drained events leave the failed clock paused. Its `recover_failed_playback` also reproduced graphics restoration erasing the playback error; active/background restoration and tab return now preserve failure, position, history/view/selection and the persistent status notice. Native drain and explicit selection-end recovery pass; existing shared-window Retry/Cancel, background playback, stale-event and close checks remain green | Injected faults/recovery and controlled completion delivery with generated media; not actual device loss, codec failure, endpoint disconnection or physical input. Natural audio drain does not itself prove the runtime range has an exclusive end: the boundary case explicitly uses a selection. Preserve the original error if best-effort output pause itself fails |
| Unedited JPEG metadata | `unedited_jpeg_export_preserves_compressed_pixels_and_non_xmp_markers` reproduced recompression/marker loss and now checks byte-exact non-XMP retention, EXIF orientation/opaque ICC/IPTC/COM, Keep/Set/Remove, resave, source preservation and stage cleanup. The undecodable-samples regression demonstrates marker validation and `-xerror` alone accept missing entropy; `-err_detect explode` rejects it and malformed Huffman tables before publication. Existing progress cancellation/source-mutation guards and edited/conversion regressions pass. App lifecycle now covers both empty and rotated histories; UI explains preservation and Remove limits. Opt-in `unedited_jpeg_export_preserves_progressive_and_color_variants` covers nine existing Pillow-generated RGB/gray/CMYK/direct-RGB baseline/progressive fixtures | No raster recompression only for JPEG→JPEG with an empty operation list, not a lossless-edit or speed claim. Still decodes for validation. Unknown/technical XMP remains excluded; non-XMP metadata is copied, not interpreted or synchronized, so Remove is not a privacy scrub. Scripted app dialogs, not physical input; no benchmark or broader format certification |
| Image raster preparation | 65dc8eb: six equal-sized edited frames need one graph; four resamplers, crop/free/quarter rotation, size changes, comparison sides, late differences and cancellation preserve fresh-graph results. Empty edit lists now need zero graphs; `unedited_frames_copy_exact_pixels_without_native_filters_and_keep_validation` checks exact independent RGBA/delays, invalid dimensions/byte lengths and mid-chunk cancellation. WebP save/APNG conversion checks both graph counts. Release benchmarks, nine warm alternating-order runs: `animation_filter_reuse_benchmark` at 65dc8eb (Lanczos + flip), 96×320×240→240×180: 160.407→60.009 ms; 12×1920×1080→1280×720: 314.196→215.842 ms. `unedited_frame_copy_benchmark`, native copy filter→direct copy, 32×640×360: 8.009→3.061 ms; 4×3840×2160: 39.683→12.144 ms | Generated in-memory RGBA work, not decode, encoding, complete Save/UI latency or memory reduction. Every result is checked; validation, cancellation and budgets remain. No global cache; native graphs still have working-memory cost. Empty-list copying does not bypass actual raster operations or make encoded saves lossless. Do not repeat unchanged benchmarks |
| JPEG/WebP XMP transfer | Qualified text now also accepts `rdf:value` attributes on empty property elements and nested `rdf:Description`, plus namespaced Description qualifier attributes beside an element value. The new regression first reproduced rejection of attribute-valued Genre. **48** Keep/Set/Remove, edited/unedited and resave cases check plain/title values, CR/escaping and exact opaque qualifier retention; the earlier **216** element-form/nine-field cases still pass. Sixteen ambiguous/context inputs reject duplicate or simultaneous values, URI/identity/language controls, mixed/nested content and namespace lookalikes. Generated JPEG→JPEG/WebP edits, re-edit and WebP→JPEG retain an attribute-qualified Album and source bytes; app JPEG fixtures use directly attribute-qualified Genre. **46 distinct runtime checks pass / one existing opt-in ignored**, plus **three app metadata/UI/save/conversion checks**; app/runtime all-target Clippy/fmt/diff pass | Keep the selective descriptive-property contract and existing XML bounds, not arbitrary technical-XMP copying or general RDF conformance. Resource-valued text, RDF identity/URI controls, XML context on qualified wrappers, qualified list items and Extended XMP remain unsupported. No new editable fields, URI resolution or packet limits. Earlier `4825ed8` retains element-qualified transfer and broader metadata regressions; contributor/publisher/rights/keyword and static/animated WebP controls also pass here. ICC/EXIF/IPTC/COM synchronization and AlbumArtist in XMP remain open. Generated media and synthetic egui/AccessKit, not physical dialogs. No full-workspace rerun, Release/installed-executable update or publication |
| APNG frame loss | c17b0a3 reproduced three frames saved as one; independent-PNG assembly preserves controls/payload | Do not return to the old single-image export path or assume native APNG encoder disposal is equivalent |
| APNG PREVIOUS | 994f0a9: explicit 2×1 BACKGROUND→PREVIOUS golden exposes stale canvas restoration in image 0.25.10; runtime compositor fixes it; one-frame/Adam7 tests pass | Existing `export_png_metadata_tests.rs` covers first/consecutive PREVIOUS, palette/gray/16→8 conversion, preview/budgets/cancel; full color fidelity remains open |
| Separate APNG poster | 2d72e75: FFmpeg cannot demux the valid partial-first-animation-frame fixture directly; stream runtime-composited poster + animation through one FFmpeg job | Actual edits, text Keep/Set/Remove, exact pixels/controls, resave, corruption/I/O/cancel/source-change target protection tested; extra disk/encoding cost |
| APNG OVER rounding | `apng_export_preserves_all_edited_frames_delays_and_loop_count` reproduced RGBA 193 vs displayed 192; all APNGs now use the shared compositor and the regression requires exact edited frames, not tolerance or FFmpeg's own reference | Preparation failure/streaming tests cover both poster layouts; extra intermediate encoding/disk cost now also applies without a poster. Not proof of 16-bit/ICC fidelity or identical resampling for every filter |
| GIF animation export | 7ddc452: `export_gif.rs` reproduced three frames saved as one. Tests cover exact source delays/repeat, edited pixels, partial frames/disposal/duplicates/resave, static conversion, count/canvas boundaries, cancellation/source-change/corruption/occupied-stage target protection | Invalid LZW with intact frame controls was accepted by FFmpeg even with `-xerror`; retain strict indexed source decoding. Unknown extensions/metadata and other conversion directions remain; GIF→APNG is covered below |
| Animation conversions | GIF→APNG/WebP and APNG↔WebP regressions in `export_gif.rs`, `export_png_metadata_tests.rs` and `export_webp_animation_tests.rs` verify exact edited frames/resave plus independent raw timing/loop controls. Cases include single-frame animations, partial/duplicate/transparent frames, disposal, crop/flip/rotation/resize, zero/sub-10 ms delays and finite/infinite count limits. GIF tests cover all four disposals and raw 65535 cs; WebP→APNG covers reducible 16777000 ms holds and rejects irreducible 65537/0xffffff ms; APNG→WebP covers denominator-zero/65535, 16383750 ms, and pre-encoding rejection of separate posters, fractional milliseconds, oversized holds and 65536 total plays. Failure tests preserve targets on cancellation, source mutation, corrupted pixels/controls, invalid edits and occupied stages. The app's shared `animation_conversion_lifecycle` covers all four directions: chooser cancellation, Save As/resave/reopen, retained source/history/target and a later unsupported destination. 695f412 fixed missing .apng/uppercase classification | Reuse existing compositors, PNG staging/assembly and lossless WebP writer. Free-rotation/resampling comparisons between staged APNG and WebP are not proof of every display/export filter's byte identity. Cross-format metadata/ICC and poster removal are not implied. PNG staging/full snapshots add disk/encoding cost; no long-animation or physical-dialog evidence. WebP's 16384 output dimension limit does not apply to APNG (16385-wide regression passes) |
| GIF palette and transparency | `gif_opaque_palette_keeps_all_256_colors_through_edits_and_resave` reproduced unnecessary 256→255 reduction; 255 visible colors plus transparency also merged two colors in FFmpeg palettegen. RGBA PNG staging + exact local palettes passes both boundaries, a reversed local palette, edits/resave, and rotation alpha. The 32 disposal/overlay combinations reproduced stale green behind transparent pixels; BACKGROUND snapshots fix it | Four added tests also cover >256-color alpha masks/all-transparent pixels and malformed/truncated/oversized/mismatched PNGs, extra frames, cancellation and target preservation. NeuQuant replaces FFmpeg quantization beyond the palette budget: broader visual quality/performance remain unverified. One RGBA snapshot at a time, additional PNG disk/encoding cost, and non-interruptible one-frame quantization; not lossless for arbitrary color counts/alpha |
| WebP animation saving | `export_webp_animation_tests.rs`: partial/duplicate frames, blend/disposal, RGB/alpha lossy and lossless input, exact 0/1/0xffffff ms controls, 0/1/3/65535 loops, one-frame animation, all resize filters/free rotation, resave and XMP Keep/Set/Remove. Displayed edited RGBA matches saved frames exactly in these fixtures. App `animated_webp_metadata_save_resave_and_guard_preserve_all_frames` covers export/save/metadata/dirty/exit-guard lifecycle | Four runtime tests also cover damaged bitstreams, invalid edits, source changes, mid-export cancel, occupied stages, cross-format target protection, 65536/65537 frames, geometry/subchunk/padding and size limits. Same-format saves retain no whole decoded animation or PNG staging; full lossless snapshots can enlarge files. Long-animation latency/memory, per-frame cancellation latency, ICC/EXIF/unknown metadata and conversion beyond APNG remain open |
| AVIF display/alpha | Existing tests cover auxiliary alpha, first/progressive previews, budgets, callback cancellation and 16/32-bit item references. `avif_premultiplied_alpha_matches_display_preview_and_edited_saves` adds independently marked still/three-frame RGB fixtures spanning every valid 8-bit component/alpha pair: exact restored pixels, first preview, quarter-turn save/resave, alpha/timing/loops and invalid-reference target protection pass | Sequence unpremultiply first failed format negotiation; adding planar formats alone lost low-alpha values (128→0). The pinned FFmpeg verbose graph confirmed auto_premultiply_dynamic insertion after alphamerge. Explicit geq restoration matches display rounding/zero-alpha behavior without that extra conversion. Generated RGBA8 codec tests, not physical UI, HDR/ICC, derived-grid, combined aperture/premultiplication, long-animation performance or playback loop-count proof |
| AVIF transforms | `export_avif_orientation_tests.rs` covers quarter turns/reflections, crop/flip and resaves. The aperture regression reproduced 7×5 instead of the declared 4×2; new tests verify signed half-pixel center offsets yielding integer bounds, odd crop origins/sizes, still/one-/three-frame alpha, omitted alpha transforms, timing, previews/persisted thumbnails and exact saved/resaved pixels. Malformed/conflicting apertures reject display/export without replacing targets; pure inset tests cover truncation/overflow/empty bounds | Apply aperture then orientation to merged RGBA. FFmpeg autorotation/container cropping are disabled for sequence encoding; clear inherited matrices or saved pixels get transformed twice (caught by resave tests). AVIF-only preview revision v2 discards uncropped cached pixels; other formats stay warm. No physical UI, fractional-pixel-boundary, derived-grid or HDR/premultiplied coverage claim |
| Static AVIF saving | The static golden test reproduced alpha 0/64/128 becoming 255 and changed RGB on save. Shared display edits + one RGBA PNG stage + lossless color/alpha encoding now require exact decoded output RGBA before publication. `avif_static_edits_and_format_conversion_preserve_displayed_pixels` covers PNG→AVIF, opaque/alpha inputs, crop/flip/quarter/free rotation, four resize filters and AVIF/PNG resaves; WebP resaves retain alpha but use its existing lossy color encoding | `avif_static_save_protects_targets_and_owned_staging` covers pre/mid cancellation and source changes for PNG/AVIF inputs, occupied output/intermediate files, corruption and cleanup. No native 16-bit/HDR/ICC/metadata retention claim. PNG disk/encoding and verification-decode memory cost remain; individual codec calls are not interruptible |
| AVIF display timing | `animated_format_previews_match_the_original_first_frame` reproduced expected [500,500] ms as [500,100]. AVIF plane decoding retains source duration for terminal/first-only frames and positive next-PTS intervals otherwise. `avif_variable_frame_timing_keeps_the_tail_and_first_preview_duration` checks [125,750,500] ms with loop settings 0/1/3 | Timing evidence only; finite playback is now covered separately by `finite_animation_stops_on_the_last_frame_after_declared_plays` |
| AVIF derived grid | `generated_avif_color_alpha_grids_and_mixed_planes_preserve_pixels` builds 2×2 RGB/gray grids, clipped from 128×128 to 125×123, and all four single/grid plane combinations. It also covers per-tile alpha without a separate alpha grid: source RGBA, first/persisted previews, primary crop/rotation, edited saves/resaves, all/partly premultiplied tiles, missing/duplicate/mismatched alpha, cancellation/budget and target protection. Mixed planes previously failed synthetic-duration comparison; per-tile alpha previously became opaque. Still timing is normalized; tile associations are read once, then each pair is merged before placement. AVIF-only cache v4 excludes old opaque previews | Regular row-major uniform grids only; whole-image alpha links take precedence. Incomplete tile-alpha coverage fails explicitly. No overlays, nested derived items, tile transforms or fractional pixel boundaries. Not HDR/ICC, all encoder layouts or large-grid performance proof. The opt-in external grid/crop test uses TOWAVUE_AVIF_GRID_FIXTURE and --ignored: [pinned libavif fixture](https://github.com/AOMediaCodec/libavif/blob/ab708ca88689210b770005198c88f0c2e4159a6a/tests/data/sofa_grid1x5_420.avif), SHA256 c9e04ff9d90d7093454750fa33b7543ee5479e0cfb151e2c3d2ce6a16c1651c1; binary stays untracked |
| AVIF YUV conversion | The grid reference exposed first-component 199 versus expected 187 even with identical bilinear scaling. Passing frame range and matrix to swscale resolves it. `avif_yuv_range_and_matrix_match_explicit_ffmpeg_conversion` matches explicit FFmpeg RGBA for generated full/limited range × BT.709/SMPTE170M stills; existing alpha/premultiplied/orientation/save regressions pass | Refresh scaler color metadata per frame. AVIF filmstrip cache v4 excludes thumbnails produced with old range/matrix handling; other formats are unchanged. Not HDR tone mapping, ICC color management, all matrices/chroma positions or monitor-color proof |
| AVIF sequence export | `export_avif_tests.rs`: exact edited RGBA across three frames, opaque/alpha sources, crop/flip/quarter/free rotation and all four resize filters; VFR [125,750,500] ms and absent/infinite/finite loops survive resave. Tests also cover cancellation/source change/occupied staging/corruption/cross-format target protection, invalid geometry/auxiliary type/edit lists, box bounds/64-bit sizes/count limits and wide timing arithmetic. Transform and premultiplied evidence is above | FFmpeg exposes cover items and timed streams with duplicate IDs: match track ID, time base and media duration. Loop controls live in elst/tkhd, not metadata. Encode RGB losslessly plus monochrome alpha; alpha frames must not retain the RGB identity matrix. The test helper independently reads raw alpha through FFmpeg. Complex edit lists fail explicitly; 16-bit/HDR/ICC/metadata remain open. FFmpeg buffers encoded AVIF in memory; no whole-process memory cap or long-animation performance claim |
| One-frame AVIF sequences | `avif_single_frame_sequence_keeps_pixels_duration_and_repetition` reproduced the explicit export rejection, then verified edited pixels, alpha, 375 ms, absent/0/1/3 loops and resaves from an independently reduced FFmpeg sequence. Two further tests cover 1/10001/UINT_MAX ticks, rotation-created alpha, decoded long holds, cancellation/source changes/occupied stages/cross-format target protection and malformed generated tables/layouts without writes | FFmpeg AVIF omits moov for one frame: specialize its single-sample MP4 tables, not duplicated frames. Generated outputs have timed tracks without a still cover. UINT_MAX needs version-one mdhd (version zero reserves it for unknown duration); disable MOV's stts correction heuristic and use temporary packet timing to avoid MP4 rescale overflow. Export retains raw ticks; display retains its 10 ms floor. Not external-reader compatibility or physical-UI evidence |
| Dirty image comparison | e3080fb / `tab_transfer_tests.rs`: accepted current/saved results survive activation/transfer; interrupted/stale/changed-export snapshots restart without losing history/materialized pixels. `image_edits::tests::orthogonal_comparison_*` adds 156 crop/flip/quarter-turn histories compared pairwise across two RGBA frames against FFmpeg renders, both source and materialized-current paths, with zero render calls. Long rows, partial edge tiles, late alpha differences, invalid geometry/pixels and cancellation are covered. Alternating-order Release medians (9 warm uniform-frame runs): 4096×2304 flip 20.847→5.484ms, opposite quarter-turns 44.563→5.571ms, offset crops 28.600→0.588ms; 4096² quarter-turn versus original 41.767→29.525ms | Views allocate no pixel buffer and keep all-frame/delay equality; mixed-axis comparisons use 32² tiles after linear scanning regressed the square case to ~56ms. Resampling/free rotation retain the rendered fallback and materialized-current reuse. Measurements exclude decoding, UI/physical input and whole-process memory; no long-animation latency claim |
| Window scheduling | `image_visibility_tests.rs`: 3a499d1 stopped hidden/minimized animation work while retaining phase/source; d8454c7 prevented distant video frames postponing UI expiry. `image_animation_suspends_until_resampling_finishes_or_undo_recovers` additionally reproduced advancement during resampling: pending/failed edits now leave deadlines/textures untouched, while status expiry survives. Completion/Undo catch up to the original phase with Nearest sampling and correct copy dimensions; a failed first reading page does not freeze valid pages | Real winit visibility states plus scripted scheduler/egui texture checks; resampling uses real edited pixels and injected failure. No physical input, hardware presentation or performance measurement. Visible/minimized trial stays ignored by default; run explicitly for scheduling/visibility changes. Audio polling is unchanged; lack of focus alone does not suspend images, and full occlusion remains unimplemented |
| Image preparation/recovery | `failed_reedit_rerenders_after_tab_return_or_window_transfer`: failed re-edits rerender after activation/transfer; successful retry stays dirty, repeated failure blocks copy, and Undo restores saved pixels/history/source. `edited_image_install_converts_only_the_selected_frame_with_current_sampling` reduced non-first Nearest replacement from three CPU conversions to one, retaining frame/deadline/copy identity and invalid-input protection. `image_loading_uses_current_sampling_once_and_reuses_warm_textures` reduced Nearest load misses from two conversions to one; unchanged static hits use zero. Mixed static/animated reading chunks and repeated Nearest/Smooth changes preserve exact pixels/sampling and static texture IDs; animations stay independent | Scripted failures/loaded results, real rendering/comparison and model transfer; not physical UI or memory-exhaustion evidence. egui already coalesces full texture updates: counters prove less CPU conversion work, not fewer GPU uploads, measured latency or a process-memory reduction. Cache budgets/weak ownership, completed-comparison reuse and stale-result guards remain unchanged |
| Timeline dirty state | a59f9c9 effective settings; 3d1449a exact duration-aware intervals | Preserve conservative behavior for unknown duration, integer rounding and different source ranges; Undo history stays |
| Timeline selection release order | `selection_release_and_cancellation_follow_event_order` reproduced a lost selection after release followed by focus loss. Both Escape/focus-loss orders pass with batched/separate presses and repeated layout passes. `queued_timeline_release_precedes_native_cancellation` additionally reproduced the loss through WindowEvent::Focused(false) before drawing: a hidden native window, egui-winit queue and normal D3D11 render/action path now pass audio/video, both orders and entirely queued gestures. Escape dismissal commits first and exits audio fullscreen after release, but cancels an unreleased gesture without exiting. It verifies exactly one immediate frame only with a queued release and unchanged dirty state | Shared timeline input honors cancellation before release, not after it. Native focus loss and Escape dismissal drain an existing primary release before canceling audio/video interaction; modal/overlay gates remain. Injected native focus and direct Escape-dismissal dispatch are not physical keyboard/mouse evidence. No codec/audio quality claim; existing cancellation/ownership and panel-resize regressions remain |
| Timeline resize cancellation | `timeline_resize_preserves_media_state_and_leaves_room_after_window_shrink` reproduced Escape persisting 246pt instead of restoring 186pt. Expanded rendered regression covers Escape, focus loss, native cancellation dispatch, modal interruption, both Escape/release orders, repeated layout passes, tab-local height/close cleanup and window shrink; history/position/generation stay unchanged | egui 0.35 retains pre-drag PanelState but stop_dragging also reports drag_stopped: suppress its release-size calculation on cancellation. The pinned internal resize-handle ID is guarded by this regression. Completed gestures do not consume a later native Escape. Synthetic rendered/dispatch evidence, not physical input or all-DPI proof |
| Waveform loading | Native source overviews and fresh retained-interval audio inputs now discard non-selected stream payloads; reusable general audio/video inputs are unchanged. Timeline workers apply discard **after seeking**: applying it before seeking changed the 333ms MOV audio result, while the original policy and post-seek implementation agree. `audio_only_waveforms_skip_unselected_video_and_audio_payloads` compares audio chunk bytes/frame counts/timestamps at 0/333/1500ms through 1900ms in owned MOV/MP4/MKV/TS with video and two audio tracks. Source stamps are unchanged. The raw-video MOV scan retains its prior evidence: FFmpeg AVIO bytes **7,526,270→2,088,318**, returned unwanted payload **7,296,000→119,296**, identical selected packets/timestamps and overview pixels at three sizes. Probe-buffered packets intentionally remain. Runtime all-target Debug tests: **397 passed / 36 ignored**, including timeline edits, selection/end rejection, cancellation, seek/sample-axis reuse, overview codec/layout/short-tail comparisons and four decode/export integrations. The TS-only test extension passes in its targeted rerun; the broad command was not repeated. Affected-runtime all-target Clippy with waveform verification, fmt and diff pass. Earlier **4977e75** replaced probe/CLI overview generation: warm Release CLI→native medians in ms, 1s PCM 49.237→6.986/AAC 44.687→2.522, 180s PCM 76.656→29.956/AAC 126.052→58.969. **b43657a** retains source textures across trim and prevents overview requests replacing pending detail | AVIO evidence is logical read bytes including probing, not physical disk traffic, elapsed latency, UI/GPU or peak memory. Savings depend on demuxer/container layout; the uncompressed-video fixture is not an all-format benchmark or a measured timeline speedup. The shared timeline path also feeds edited audio playback; tested PCM/timestamps remain unchanged, without a new physical endpoint certification. Previous Release timings were not repeated for this change. Cached overviews were ~0.1–0.2ms and separate 180s detail ~96–100ms in that earlier trial; detail still reprocesses retained audio. Cold/longer real-media and physical-input cost remain unverified. No cache-budget/raster/listening-volume change, installer update or publication |
| Initial audio seek phase | `coarse_timestamp_edited_audio_seek_matches_the_continuous_sample_axis` now passes as a normal regression: gain at 17–35ms, delete at 69–84ms and seven seeks match continuous PCM / PCM-copy WAV, fixing the previous -16/-8/+8 sample errors. `decode::audio_seek_tests` adds seven PCM conditions (8/16/24/32-bit integer, float32/64, LE/BE, 44.1/48/96kHz, 1/2/3 channels), exact NUT-remux comparisons, forward/back/reused seeks, fractional boundaries, EOF, cancellation/reuse, variable packet sizes, sequential gap rules, malformed-packet rejection and side-data fallback | Audio-only workers read/count the prefix for coarse scalar PCM, without prefix decoding/conversion in the ordinary case. Sample-precision and compressed formats retain existing seeks. Side data falls back to decoding the remaining prefix. No general compressed-audio, nonzero-origin, atempo or listening-quality claim; avoid a fixed packet-size/PTS-grid assumption |
| PCM seek traversal and reuse | `pcm_packet_scan_reports_exact_seek_anchor_cost`, standalone Release, generated 30min/48kHz/mono PCM16 Matroska, 1001-sample packets, warm local files. Same open owner/native indexes, discard/retain/retain/discard of only towavue's bookmarks, three trials per mode. Median ms at 0.617s: **0.433/0.342/0.364/0.356**; 60.017s: **2.416/0.473/0.444/2.284**; 900.017s: **32.018/0.425/0.393/30.747**; 1799.017s: **243.044/0.458/0.442/270.320**. Separately, ordinary unpaced decoding to each target learns bookmarks before the first seek: three-seek medians **0.479/0.444/0.451/0.451ms**, with two probe packets plus 29/48/6/10 worker packets in the final trials. All measured outputs match the independently sought PCM-copy NUT exactly | Initial fresh-input scans were 0.691/2.836/33.142/61.130ms; the late reused-owner discard control is substantially slower than the initial scan, so do not conflate them. An uncached seek still traverses the prefix (roughly 173MB at the end); played positions can avoid that first-seek traversal. No consistent improvement near the start, no OS-cold/interleaved performance, paced WASAPI/UI latency or whole-process memory claim; interleaved correctness is covered separately. This updates 51c23ac's measurements for ordinary-decoding integration; the benchmark remains opt-in, not a reason to rerun unchanged trials |
| Image resource cleanup | 494cd5e clears last-image-tab caches; 638d5db empty-host delayed Trim; 7fc0b7c visible-host 100-image run | Two generated 4096×2304 JPEG runs: ~1.02s idle, GPU ~303→17MiB, private ~959→132MiB, reopen ~44/50ms. Explicit navigation, not physical keys/cold reading; browsing peaks are a separate measurement below |
| Texture-cache original lifetime | `image_texture_cache_reuses_only_shared_static_decodes_within_its_limits` first failed the release assertion, then passed with weak decode identities; texture IDs, sampling state, LRU budgets and live-tab ownership remain shared | GPU cache must not retain a second set of original pixels after decoder eviction: those old textures could not serve a new decode identity anyway. Test-only `TOWAVUE_VERIFY_RETAIN_TEXTURE_ORIGINALS=1` restores strong ownership for A/B measurement; absent from normal builds |
| Reference-folder navigation | The selected reference trace now records managed RGBA8 dimensions before/after submission, existing free-list resources and whole-upload payloads, outside GPU timing. Release after `039bf34`'s after-paint retirement, 540 read-only references, **forward/reverse/reverse/forward**, 33 ms commands, 16 ms event-free draws, accepted 384 MiB/directional/paired-decode policy: all four preserve **540 originals in Shell order, zero blanks/previews after the first original and source stamps**. LiveExplorerView completions **527/533/532/524**; p95 **20.008/18.349/17.001/19.425 ms**, maximum queue **3/1/1/2**, peak commit **1466.6/1411.4/1393.7/1409.4 MiB**. First original **9.6–12.0 ms** after the command clock, excluding construction. Largest PNG (lexical 428, traversal 466 forward / 55 reverse) scheduled-to-Present **157.502/51.661/52.007/114.445 ms**; GPU submission **91.950/27.401/26.740/52.245 ms** | Both forward trials have identical **14 / 236.58 MiB** pre-submit managed resources and **10 / 186.33 MiB** existing resources slated for retirement; reverse has **26 / 223.43 MiB** and **19 / 172.92 MiB**. Each uploads one **197.57 MiB** texture and finishes near **248 MiB**, yet native creation is **77.857/21.634/21.061/44.268 ms**. Live pool counts/estimated bytes alone do not explain the forward variation; do not tune capacities from these observations. The isolated history/source-storage controls are recorded in Image preparation/upload investigation below. These are logical dimensions, not physical VRAM, immediate reclamation or a causal retirement-fix comparison. Three reference accounting/order tests pass in Release and final Debug, plus app all-target Clippy/fmt/diff; Release trials precede only an equivalent filter/map lint rewrite. No compiler overlap, native crash, physical keys, native redraw-scheduler/cold-disk/IrfanView evidence, private pixel readback/screenshots, full-suite repeat or installed-app update. No performance fix is claimed. Earlier native-order adoption/generated controls are in `08974a1`, capacity/directional comparisons in `f360890` and `da5a80b`/`b6c3361`/`aa55074`; do not repeat unchanged trials. Explicitly propagate native `$LASTEXITCODE` to avoid PowerShell stderr-wrapper false failures |
| Image preparation/upload investigation | Generated Release control `large_texture_upload_reports_settled_allocation_history`: three fresh-device samples per batch, app BGRA/VIDEO flags and multithread protection, identical final **12 textures / 240.89 MiB** logical GPU content, prior work drained, then one 8706×5949 upload. Short/long/long/short (**12/468/468/12** creations) submission medians **19.130/19.264/19.704/17.508 ms**. A separate same-history (**468**) persistent/fresh/fresh/persistent CPU-source control allocates and retains the same extra copy in both conditions: **18.170/19.671/18.003/19.551 ms**. GPU event-completion medians stay within **0.18 ms** of submission. All current/retained pixels, dimensions and source ownership match; allocation/destruction/readback are untimed. Both explicit Release controls pass; **seven Debug renderer tests pass / four timing opt-ins ignored**, renderer all-target Clippy/fmt/diff pass | Neither isolated history length nor freshly copied source storage reproduces the app's **44–78 ms native creation** from `94d11e1`. This does not rule out combined worker/memory pressure, driver state, source conversion/allocation history or the app/swap-chain context; actual app upload profiling remains next. System-wide CPU sampling (other process names/stacks included) is awaiting explicit owner permission; WPR/Xperf are available but no trace has started. Automatic continuation is not approval. Do not repeat unchanged controls or introduce a speculative cache/source-pool redesign. Test-only changes, no production speedup, app rebuild, private-media trial, physical input, full-suite or installed-app update. Preserve `2a8f51f`'s sampler-only empty updates (zero conversion/pixel upload, retained pending pixels/animation state and WARP/hardware output; 11 app tests) and `039bf34`'s after-paint retirement. `6826e80` rejected blanket same-size pixel reuse: largest GPU completion **12–13→32–33 ms** idle, **16–23→29–32 ms** with queued reads, despite similar submission; it also fixed invalid whole dispatch. DEFAULT initial-data creation and validated nonempty UpdateSubresource remain unchanged. Earlier immutable/separate-upload and device/decoder/idle-redraw controls remain in `53aea7d`/`b35e070`/`08974a1`; do not repeat them |
| Reference decode stages and alternatives | `87d1142` retains the full scan: 463 JPEG / 74 PNG / 3 WebP; median/p95 **13.080/60.335**, **13.475/47.424**, **10.930/48.947 ms**. Slowest repeats: JPEG ~151 ms, PNG ~316 ms, WebP ~53 ms. Largest PNG selected by signature/IHDR, not extension: **8706×5949**, noninterlaced RGBA8, **24.690 MiB compressed / 197.571 MiB RGBA**. On it, current/FFmpeg/FFmpeg/current was **315.354/1221.895/1217.517/313.630 ms**. New Release `png-decoder-cost`, current/candidate/candidate/current, three samples per batch: WIC converter medians **302.024/370.808/378.929/305.285 ms**; native BGRA plus channel swap **300.901/375.703/380.215/304.593 ms**; libpng simplified API **299.365/559.165/592.752/316.724 ms** | Reject all three substitutions for this outlier; not a claim about every configuration/file. Each new mode checks full reference RGBA and generated odd-width RGB/RGBA, including hidden RGB at alpha zero. Source length/mtime unchanged, no private paths/pixels emitted. Timings include file open/decode/output allocation/copy/decoder release, exclude equality and worker COM/factory/DLL setup; warm files, serial after compiler exit, no GPU/app navigation/physical-input/cold-disk or peak-memory claim. Libpng reads the compressed file into additional memory and has no cancellation/orientation/color-contract certification. Existing audited DLL version **1.6.58**, SHA256 `3737838EE8D6B893DF7BDABF7439775371A57DCE7E17EE6B35586023C64BD9AB`; no redistributable added. FFmpeg's comparison includes demux/scaling/copy, not pure codec time. WIC native mode falls back to its converter for non-BGRA input, including the generated RGB control. Do not repeat library substitutions without new internal cost evidence |
| Browsing-memory comparison | Release `hundred_large_images_report_gpu_navigation_and_memory`, same-binary strong/weak/weak/strong order: 100 generated 4096×2304 JPEGs, warmed files, synthetic Shell snapshot, hidden 960×576 GPU surface. At 33ms cadence, peak commit 1239.8–1241.9→1061.0–1061.3MiB; readiness median 11.414–11.543→10.155–10.262ms | Fastest cadence median 17.806–17.916→17.958–18.034ms, p95 21.345–21.920→21.800–22.165ms; 100-command burst 2.133–2.160→2.192–2.203s. All originals presented, no blank/preview targets; separate 64-pixel-per-image readback passed. GPU peak ~338MiB unchanged; fastest-cadence commit/terminal private bytes did not consistently improve. Not physical input, cold/other formats, IrfanView, a whole-process cap, or universal speed improvement |
| High-resolution PNG navigation | 5aefccb baseline versus c3d8e17 row-decoder Release builds, two trials each: 100 generated 4096×2304 RGB8 PNGs (86.3 MiB), warm files, hidden 960×576 D3D11 surface, same NAV100 harness. Fastest readiness median 54.502/54.783 → 49.974/49.451 ms; p95 55.727/56.233 → 55.009/50.887 ms; 100-command burst 5.411/5.469 → 5.033/4.907 s. Updated 33ms-cadence medians 51.711/49.438 ms. All originals, no blank/preview targets, separate 64-sample-per-image readback and cleanup passed | Fastest-phase process peak commit increased from 983.7/983.3 to 1001.6/1001.3 MiB: no whole-app memory improvement claim. Retained pixel/texture limits are unchanged; one removed RGB buffer is not a process cap. Accumulated GPU calls include repeated waiting draws, not just upload. Baseline JPEG control was 18.067 ms / 2.246 s. Not physical keys, cold disk, all PNG encodings or IrfanView comparison |
| Managed texture lifetime | WARP readback reproduced a missing green quad when its last texture handle was dropped after queuing the mesh: the renderer freed egui's after-paint IDs before drawing. Frees now run after the paint attempt, including empty frames and upload failures. Extended WARP/hardware tests verify exact whole-target output and pool removal for existing and same-frame-created textures; **seven Debug renderer tests pass / three timing opt-ins ignored**, with both sampling tests passing in Release. Three explicit hidden-hardware app tests pass: filmstrip first-open, actual refresh completions and reading layout/animation handoffs. Workspace/renderer all-target Clippy/fmt/diff pass | The full Debug app attempt passed **512**, failed one native Shell abort, ignored 23 and retained two exclusions. Its exact rerun passes; do not relabel the aggregate green. Remaining checks pass: Core 69, Runtime 407 / 42 ignored, four decode integrations, three app-example tests / one ignored, plus the one render-verification-only Runtime test absent from the feature-off run. Crash evidence is below. No physical-input, initial-upload/navigation latency, new memory measurement or installed-app update; this is a reproduced texture-lifetime defect, not proof of the owner's entire flash report. Earlier shadow-removal evidence remains at `1ef757a`: generated NAV100 PNG working-set peak 686.8→424.7/430.5 MiB, JPEG 704.1→440.1 MiB, with similar readiness. Commit and post-close driver reservations did not fall proportionally; same cache budgets, no whole-process cap. Do not repeat those unchanged comparisons |
| Static PNG rows and Paeth cost | Four-pixel wrapping prefix sums now replace serial Paeth only when all upper neighbors in the block equal the upper-left pixel; mixed blocks/tails retain the prior SSE2 recurrence. No new decoder, CPU requirement, worker, cache allowance or image-sized buffer. Generated 8706×512 row comparisons, seven samples per phase, old/new/new/old: flat **~14.6→3.2 ms**, 31-pixel runs **~14.6→4.7 ms**, noisy **~14.6–14.7 ms** for both. The read-only 8706×5949 reference still has **5909 Paeth rows**: isolated reconstruction falls **~169–170→~100 ms**. Separate-process Release production-decode old/new/new/old, three-sample medians: initial **279.044/211.588/213.070/279.016 ms**, later **295.873/225.371/226.437/297.210 ms**. Later samples follow the full-buffer diagnostic control in each process; that extra buffering is not deployed | Full RGBA/dimensions and source length/mtime match; generated odd-width RGB/RGBA includes hidden RGB at zero alpha. **Full Debug workspace: 995 passed / 66 ignored / two known exclusions**. The ordinary row regression was then strengthened with flat/mixed upper runs and prior reconstructed rows; both Release runtime kernel tests pass afterward, including exhaustive predictor triples/ties. Four explicit Release example checks (including generated timing) pass; workspace all-target Clippy with the PNG verification feature, fmt/diff pass. Existing depth/orientation, palette/gray/tRNS, Adam7, source/canvas/cancellation/tail checks and shared thumbnail/export paths remain covered. The optional vendor row hook and its allocation/metadata policies are unchanged; serial comparison is verification-only. Earlier predictor-formula/library rejections remain in `407870e` and the rows above; do not repeat them. Warm-file timing excludes pixel comparisons and concurrent compilers. No private image output, peak-memory measurement, new app-navigation/GPU/physical-key/cold-disk/IrfanView comparison or installed-app update; the full latency objective remains open |
| Prefetch handoff | The intermittent `foreground_adopts_the_matching_inflight_prefetch` failure led to `obsolete_foreground_lookup_preserves_pixels_for_the_new_request`: replace the request between its generation check and cache lookup, for an existing hit, an adopted in-flight decode and a smaller obsolete budget. All three originally returned fallback [99; 4] with one redundant decode; all now return [42; 4] with zero. A test-only per-cache hook fixes the ordering without sleeps or production callbacks | `ImageCache::get` clones shared ownership instead of removing valid entries. Cache/size limits remain, successful use still refreshes priority, changed/missing stamps still invalidate, and pointer-identity tests reject RGBA copying. This establishes the reproduced handoff loss fix, not all-files latency, lower whole-process memory or physical browsing throughput |
| Image transfer/order | Existing sequence tests cover old-image retention, each completed presentation, queue cancellation and three-scale pixel checks | Offscreen/batched commands are not evidence that 100 physical-key images were all visibly inspected |
| Long-GOP Seek | 2026-09-09 historical DEVELOPMENT: 120s 1080p single-keyframe fixture p95 ~873–979ms; FFmpeg-alone first frame ~836–936ms; 2s-keyframe control ~40–108ms | 300ms target missed on long GOP; not proof of a UI-only bug or absence of optimization opportunities |
| Long playback | Same historical Release: 30min 4K, 107771 frames, drop/CPU transfer 0, drift p95 4.812ms/max 17.349ms | One build/machine/fixture, not all-media stability or a current-build certificate |
| Image wheel event order | `image_wheel_reaches_the_rendered_view_without_requiring_window_focus` reproduced losing a 30-point reverse scroll after reaching a boundary in one frame. The expanded test compares batched versus separate frames for vertical/Shift-horizontal reversals, pan-before-zoom, mixed anchors/modifiers and zoom across disappearing scrollbars at 1/1.25/2x, focused/inactive. Immediate meshes and no delayed tail pass; allow <0.02 point for egui scroll-content layout rounding | One ordered stream replaces zoom-first/summed-pan processing; viewport bounds apply after each event. Existing wheel gates, image bar/selection and video-view tests pass. Synthetic rendered input, not OS delivery, physical latency or large-image browsing evidence |
| Volume wheel event order | `volume_wheel_preserves_boundary_reversals_and_event_modifiers` reproduced a lost reversal at the 200% limit. Audio/video, focused/inactive cases now keep both-limit reversals, accept the unmodified part of mixed Ctrl/Shift batches, ignore Start/End/Cancel phases and create no action for net-zero/idle input | Clamp per event and publish one final playback-only setting without editing history; preserve list exclusion, modal/pointer/focus-loss gates and overlapping-target deduplication. Synthetic input, not OS wheel delivery or listening evidence |
| Detailed timeline waveform | Extended `refined_waveform_renders_native_samples_and_rejects_stale_edits_and_sizes` reproduced accepting an obsolete result after width A→B→A. Every submission now has a distinct shared key identity, so matching parameters alone cannot accept a queued old success/failure. Generated one-second native audio at 1/1.25/2x covers width round trips and same-key restarts, retained loading/status until the current response, unchanged samples, and existing stale-generation/edit/size, listening-volume, height/recovery and cancellation controls. **11 targeted waveform/progress/reopen tests** and app all-target Clippy/fmt/diff pass. Earlier `8c37efe` batches ordered sample accumulation within columns: exact f64/f32 equality across 240 cases; Release three-minute stereo aggregation ~19→5.4 ms. Generated warm real-file detail medians, three samples per old/new/new/old process: PCM **109.699/73.704/75.602/108.196 ms**, AAC **124.972/82.025/81.963/128.142 ms**; all 48 per-fixture output fingerprints match. One-second PCM remains ~11.5–11.7 ms | Current ownership evidence uses real worker completions with scripted late-result/error delivery, not physical resizing or GPU/readout timing. No decoder, cache, settle-delay or sampling change, and no new performance comparison; retain the earlier 30–36% detail-generation improvement only for those warm three-minute fixtures. No unchanged benchmark reruns, installed-app/installer update or publication. Existing Delete/Keep/gain/stretch integration and 8192-column limits remain; new plans/widths still reprocess retained audio. Earlier 1.1x native-vs-CLI maximum column error 0.00016174465 remains DSP fidelity work, not PCM/phase identity; the fixture bound remains 1/4096. Cold/longer real-media, peak memory and physical UI remain unverified |
| External-drag hover ownership | `external_file_drag_suppresses_hover_content_until_it_leaves` reproduced an active background preview target during file hover. Tab/seek previews and help now disappear in that frame and resume after leave, focused or inactive. The dirty-tab hover test also verifies target/texture release, rejection of late results, no request/cancel churn and unchanged tabs/history | The drop overlay is painted without hit testing, so layer-only hover checks cannot detect its ownership. Existing tooltip delay/clipping/size-change tests pass. Synthetic RawInput/UI tests, not actual Explorer drag evidence |
| Preview reuse without disk | `unavailable_disk_cache_still_coalesces_and_reuses_generated_pixels` reproduced four generations for four simultaneous callers, then required one with identical output and cancellation preserved. Occupied-directory/locked-file tests retain original bytes and now verify shared memory reuse; persistence retries on a fresh cache rather than each memory hit. Release `large_image_preview_reuse_without_disk_reports_repeated_request_cost`, generated 6000×4000 PNG, same-binary discard/retain/retain/discard: four-request batch medians 498.237/126.184/124.509/500.989 ms, three batches per mode, exact output pixels | Clearing memory models the previous no-retention failure path. Only cache-unavailable repeated requests were measured, not faster initial generation, cold disk, healthy-cache browsing, GPU or physical UI. Existing 64-entry/16 MiB memory limits remain; the measurement is opt-in via TOWAVUE_PREVIEW_BENCH_IMAGE and was run separately from ignored tests |
| Native input delivery control | Manual delivery is demonstrated: plain native control **keys=1 / clicks=2 / wheels=7, 16.64 s**, followed by the 16 navigation steps and later held-key trial. Earlier supported-tool key/click/wheel delivery remained 0/0/0. Latest non-input probe: installed IrfanView's documented `/thumbs` plus a quoted native generated-image path and isolated INI opens **Thumbnails (5 Files)**. Computer Use returns only window/title-bar elements and explicitly reports that the target has higher integrity than its helper. The preliminary forward-slash folder launch showed 0 files and is not valid evidence | This identifies the current accessibility restriction, not every historical input failure. File count is not thumbnail readiness or presentation timing. No key retry, screenshot, security/permission change or private input workaround; both owned IrfanView processes exited and the installed INI hash stayed unchanged. Do not repeat unchanged UIA/input trials. Local `thumbnail-ui-control-20260915` records this probe; earlier `NATIVE-INPUT-RECONNECT-RESULTS.txt`, `NATIVE-READINESS-RESULTS.txt` and `MANUAL-SWITCH-RESULTS.txt` retain the historical evidence. The existing non-stopping readiness observer is separate from UIA |
| Medium JPEG thumbnails | `jpeg_thumbnails_reduce_medium_images_without_enabling_speculative_previews` covers 503×317, 1001×701 and 1920×1080, all eight orientations, bounded dimensions/opaque alpha and independent full-decode quadrant colors, unchanged sources, budget/cancellation and tiny-source fallback. `medium_jpeg_thumbnail_persists_without_starting_a_speculative_preview` checks exact reduced-pixel and source-geometry disk reuse. Release `medium_jpeg_thumbnail_reports_full_decode_comparison`, generated RGB fixtures, warm source files, empty thumbnail caches, same-binary full/reduced/reduced/full, five-sample medians: 503×317 **2.396/2.299/2.117/2.217 ms**; 1001×701 **3.758/2.646/2.591/3.601 ms**; 1920×1080 **7.405/3.933/3.931/7.156 ms**. Image regressions **27 passed / 6 ignored**; preview regressions **46 passed / 7 ignored** (overlapping filters), workspace Clippy/fmt/diff pass | Measures decode/resize/PNG encoding/persistence, excluding fixture/cache construction, disk readback and UI/GPU; no concurrent build. Full-decode control uses the existing native static-thumbnail fallback, not an older executable. JPEG reduced pixels intentionally differ from full-decode nearest sampling; its previous integration-test equality was updated, while the separate full-sampling test remains intact. Other formats keep their pixel contract. No original-display/export changes, cache invalidation, new memory allowance, universal JPEG quality, OS-cold/private-reference or physical-hover claim |
| PNG thumbnail rows | Replace the per-target-pixel sorted map with separable source-axis/destination-offset maps, adapting the analysis's axis-lookup idea without changing sampling. At most 400 coordinate entries replace up to 38,400; output, cache limits and decoder buffers are unchanged. Release generated RGB PNGs, warm files, separate-process old/new/new/old, five-sample medians: 32×24 **0.411/0.099/0.106/0.422 ms**; 502×316 **1.000/0.829/0.817/0.990 ms**; 4096×2304 **40.088/40.284/40.156/40.419 ms**. Large-image time is unchanged, not an acceleration claim. Source hashes match and no compiler overlapped; unrelated desktop applications were not controlled. The exact full-decode/nearest-resize regression now also covers portrait and single-column/single-row inputs across all eight orientations and gray/RGB/alpha at 8/16 bits; packed gray/palette/tRNS, Paeth, budget/cancellation and truncated-tail checks remain. **22 distinct affected Release regressions pass / 8 opt-ins excluded**, plus the timing opt-in explicitly; runtime all-target Clippy/fmt/diff pass | Measures open/decode/sampling, excluding PNG encoding/persistence, GPU, physical UI and process startup; no private-image appearance/readback or cold-disk/IrfanView comparison. Retain earlier Paeth evidence: generated 6000×4000 RGBA8 warm medians ~99.6→90.6–91.0 ms, RGB control unchanged ~99 ms. The earlier full-decode→stream control reduced generated RGB peak commit ~177.8→20.6 MiB; no new peak-memory measurement. APNG/interlaced fallbacks and original-image loading are unchanged. Reuse these checks rather than repeating unchanged decoder/memory trials |
| Intermittent process exit wait | Latest recurrence, 2026-09-14: `tab_transfer::tests::settled_image_comparison_survives_tab_switch_and_window_transfer`, child 48980 under aggregate 33460. Its captured test body reports success in **0.02 s**, but `.NET HasExited=True` and native exit code **0** coexist with `WaitForSingleObject(handle, 0)=258`. A new minidump shows only main in `LdrShutdownProcess → urlmon → InitOnceExecuteOnce/RtlRunOnceExecuteOnce → ZwWaitForAlertByThreadId`, the same signature as earlier selection/APNG-to-WebP cases. Matching EXE **205852160 bytes**, PDB **996126720 bytes**, dump **58296 bytes** and capture script are retained in `tvbuild/towavue-exit-wait-20260914-48980/` before further builds. Native TerminateProcess on the identity-checked child, with nonzero exit code, then a signaled wait confirmed cleanup; ordinary taskkill had incorrectly reported no running instance | The aggregate terminates **489 passed / one failed / 18 ignored / two existing exclusions**; the failed child is deliberate diagnostic termination, not an image assertion failure or a pass. Do not add a third exclusion or rerun the aggregate just for green. No test process remains. Earlier archives `towavue-exit-wait-20260913-10984`/`57952` and their evidence remain in Git; the existing two exclusions are unchanged. [Windows permits DLL-detach deadlock during process exit](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-exitprocess), but this does not identify the lost initialization owner or prove a Shell-worker cause. System32 DbgEng uses export-level Windows symbols. Investigate before teardown; never force successful production exit or join a blocked extension on the UI thread. Confirm termination by a signaled native handle, not exit code/HasExited alone |
| Isolated navigation-test crash | **26** recorded occurrences share `0xc0000409`, subcode 7 `FAST_FAIL_FATAL_APP_EXIT`. Latest **44464**, 2026-09-15 05:12 JST, in `audio_buttons_fit_small_windows_and_accessibility_invokes_shared_commands` during the texture-retirement aggregate (512 passed / one abort). Its own matching-symbol dump confirms ucrtbase abort/terminate after nested `e06d7363` in Windows.FileExplorer.Common, through `SHParseDisplayName → parse_path → shell_snapshot` on the audio-queue Shell worker. Exception object+0x0c is **0x80080008**, process uptime 4 s. Exact rerun passes in 0.17 s; this is not a lifetime fix or a new exclusion | EXE/PDB/dump preserved before rebuilding in `tvbuild/towavue-dump-diagnostic-20260915-texture-retirement-44464/`; EXE SHA-256 `FF0644626D68B5EB4D7FD752777DA080371539CD691B2F683E87E71BA411502E`. Nested record/object: `0xbf663fc2e0 / 0xbf663fc450`; fixture `towavue-app-test-31384-1789416736311881700-3`. Prior 25 occurrences, 40268/42444's archive and apartment evidence remain in Git at `1ef757a`, including references to earlier archives and 48592's activation evidence. Reader: `tvbuild/towavue-dump-diagnostic-20260912/dump.exe` (source alongside), System32 DbgEng with matching `_NT_SYMBOL_PATH`, `.exr -1; .ecxr; kb 32`, then nested `.exr` and object inspection. LLDB has crashed on these dumps. Never upload dumps or overwrite matching symbols before preservation; require each new failure's own stack evidence, not an STA redesign inferred from non-reproducing trials |
| Shell thrown activation error | Dump 48592, Windows.FileExplorer.Common 10.0.26100.8972: MSVC ThrowInfo/RTTI resolves `winrt::hresult_error`; constructor RVA 0x31148 stores HRESULT at object+0x0c, containing 0x80080008. The failed activation wrapper uses IActivationFactory and `Windows.Foundation.Collections.ValueSet` (literal RVA 0x92620). This is [CO_E_SERVER_STOPPING](https://learn.microsoft.com/ja-jp/windows/win32/com/com-error-codes-2), but does not identify the responsible lifecycle. Main is awaiting the test worker, not in ExitProcess | Dump/symbols indexed above. Type-name RVA 0xae378; thrown object 0x000000fb4c9fc580; caller RVAs 0x13222/0x13344. Offsets apply only to that DLL/version, never production logic. Restricted error-info heap/IAT contents are absent from the minidump. Investigate activation/apartment timing or capture first-chance evidence; do not patch Windows binaries, prewarm private Shell internals or assume a retry can catch an abort inside SHParseDisplayName |
| Shell cancellation/lifetime | `81c78b2` covers lazy initialization, reuse/unwind, cancellation and message-aware/nonblocking closure. Earlier overlap/restart stress passed (32 early/late, 32 parallel app, 48 lazy restart rounds). `83db33c` traces show headless main-STA retirement during another parse and test return with pending parses; hidden native-host workers are secondary STAs after winit window creation. New `ShellLifetimeTrial` counts providers before thread scheduling and drains their Shell work/apartment cleanup. The two actual failing workflows each pass with drain-only and with an independently message-pumping main STA that never parses a path: audio **0.86 / 0.84 s**, image transfer **0.09 / 0.09 s**. Drain-only audio PID 35060 replaces its main STA before all secondary cleanup; anchor PIDs 39820/45072 retain it until after every worker's cleanup. Eleven Shell regressions and feature-on/off app/runtime Clippy, fmt/diff pass; the prior 479-pass instrumented aggregate remains at `83db33c`, not rerun for this control | [COM requires the main STA to outlive its COM clients](https://learn.microsoft.com/en-us/windows/win32/com/single-threaded-apartments); headless tests bypass winit's OLE initialization. Both drained conditions pass, so this does not identify the crash cause or justify changing production to a shared/anchored STA. The control now distinguishes body completion from Shell cleanup without an arbitrary sleep or forced exit. No latency improvement, physical-input or OS-thread/DLL-shutdown claim; tracing alters timing and covers only instrumented providers. Keep the two existing exclusions and matching dumps. Do not repeat unchanged passing lifetime trials or enable anchors across ordinary tests to hide failures; use first-chance/activation evidence on a future recurrence while continuing remaining UI/function work. Trace/control usage and isolation limits are in DEVELOPMENT |
| FFmpeg provenance | BtbN development binary rejected for transitive GPL FFTW; native rebuild uses audited alternative/scoped ZVBI | Preserve source/license audit records in packaging references. LGPL labels or old candidate hashes do not certify a new distribution |

### Held image-navigation keys

Native repeats now resolve only standalone next/previous-image bindings and reuse the existing presentation-gated, 256-direction queue. Respect custom bindings, chord prefixes and blocked sequence states; filmstrip/reading and unrelated commands retain their previous repeat behavior. Releasing a key adds no input and does not discard accepted steps. The new regression covers 100 ordered requests with controlled unfinished loads, reverse/alias input, the queue limit, overlays/prefixes and custom/rebound commands. **41 affected Release tests pass / two large-image opt-ins remain ignored**, including existing hidden native Shell/GPU sequence controls. Feature-off/on app all-target Clippy, fmt/diff pass. The generated five-second diagnostic reader control retains 594/594 records with no overflow/errors/input and confirmed child exit. Candidate EXE `8523A3B3B65FEDE5174E4A855FED36CF4EA66DCCF89398B02BA768795CE5C9DA` and matching PDB are archived locally under `burst-fit-20260915/control-ab3eb5a11e944f85842a0222da81a700/`. No decoder/quality/cache or installed-app change.

The owner requested a fresh formal trial, superseding the preliminary held run. Formal `.2Ds`/Fit captures: towavue **303 raw/handler downs, 302 repeats, 303 targets / 304 original submissions**, seven inputs during loading/awaiting, queue maximum two, zero full/cancel, all 11,457 reserved records retained. IrfanView **318 raw downs / 317 repeats / 230 transitions / 231 sampled originals**, each with preceding null; 114 racing observations were rejected, zero reader errors/dropped observations. Both full-folder source-stamp audits and installed-INI hashes are unchanged; no compiler overlap or live owned process remains. The preliminary run's installed-INI check failed and its second key release was not captured; do not substitute it for this formal comparison.

The initial **118 sources match in order**, and all 231 common originals match dimensions; Fit geometry differs by at most **0.5 px**. Three later order swaps affect six positions, so use the exact prefix for milestones. From first observed press to the 100th transition: towavue Present **3743.588 ms**, IrfanView sampled-original-ready upper bound **4847.974 ms**; 117th **4315.458 / 5941.618 ms**. Held intervals **10.447 / 10.956 s**; final marker after observed release **1.770 / 96.722 ms**. Towavue's 303 observer-to-Present latencies median/p95/max **11.201 / 33.328 / 72.021 ms**; handler-to-Present **11.043 / 20.377 / 40.316 ms**. Irfan raw inputs do not map one-to-one to transitions: do not report ordinal per-input latency or infer an app/OS coalescing cause from this observer. Observer dispatch is not hardware time; Irfan readiness is not GDI/scanout. One trial each, OS caches uncontrolled, isolated settings, numerical evidence only. Current evidence favors towavue on this held-key case, not every format/cold/thumbnail path. Captures/scripts and full limits: local `burst-fit-20260915/HELD-PAIR-RESULTS.txt`. Both requested physical-input trials are finished; perceived-latency feedback is still pending.

### Folder snapshot application

The app now computes same-folder/same-items once per snapshot application. Identical items imply identical filtered image order; other changes retain the historical image-order check and preview/drag decisions. Every fresh snapshot is still installed, with identity remapping, reading restoration and prefetch unchanged. No persistent index/cache, worker, stale-snapshot reuse or disabled Shell refresh. The generated core-type predicate control saved ~0.9 ms but did not explain the physical dispatch tail, so actual application phases were measured before adoption.

Local `burst-fit-20260915/FOLDER-COMPARISON-RESULTS.txt` records an old/new/new/old actual-app control: one fixed read-only source in the original `.2Ds` folder, then eight fresh snapshots per process, **24,511 supported items / LiveExplorerView in all 36 applications**. Excluding initial application, comparison-phase medians **5.542/2.499/2.516/5.629 ms**; whole-application medians **8.814/6.288/7.855/8.996 ms**, with snapshot-disposal/other-phase variation. No compiler overlap, raw keys, reader errors/overflow, selected-source hash/mtime change or installed update. This is repeated real Shell refresh, not physical navigation or a new full-folder stamp audit. Candidate EXE `FC6A1E4939D0BD826DD9D9849C73B9B0FC4747CFFE120EEE11840C4DDDA341D0` and matching old/candidate PDBs are archived locally.

Event 33 and explicit eight-refresh/idle-frame controls separate comparison, audio/reading handoff, previews, remap, old-snapshot disposal, reading restoration and prefetch. The old all-frame control has UI-build max **4.587 ms**, renderer max **4.273 ms** and Present max **2.205 ms** over 115 frames; the 30–50 ms physical dispatch tail was not reproduced without navigation. Do not claim the deduplicated comparison explains or fixes that entire tail. **27 affected Release tests pass**, including the new 24,512-item/20-case metadata regression, filmstrip/sequence/retained-reading checks and an explicit hidden-GPU refresh check (36 media-surface / 18 whole-surface comparisons, 54 worker texture replacements). The ordinary set also passes real-Shell 100-input/101-presentation and late-scripted-order 100-step controls; the two large-image opt-ins and unrelated first-open GPU opt-in remain unrun. Diagnostic build, feature-on/off app all-target Clippy, fmt/diff/local links pass. No full-suite aggregate, native crash, remaining owned process or physical speed remeasurement after this change. Next: investigate remaining dispatch/thumbnail work with complete frame markers rather than repeating unchanged predicate/startup trials.

### Owner-operated rapid Fit navigation

Verification-only hooks capture four navigation keys (code, press/release, repeat, synthetic flag) before consumption; navigation/target selection; queue push/full/pop/restore and blocked cancellation; loader cache/wait/foreground/prefetch outcomes; UI completion/acceptance/texture preparation; frame/UI/render/Present boundaries, successful original submissions and geometry; and Shell request/completion/application. Records retain QPC time, source token and the relevant generation; UI events include queue depth and loading/awaiting/error/reading/folder/held state. Loader generation is mailbox generation, not necessarily the generation that started an overlapping decoder: correlate source/start/return/outcome instead of assigning old work to the newest UI target. Navigation/cache/cancellation policy is unchanged.

The local `burst-fit-20260915` observer reads only its owned child without stops, screenshots, pixel reads or input injection. It retains Raw Input only for Right/Left/Space/Backspace while that child is foreground; no device IDs/text/other-app input. QPC is observer dispatch time, not hardware arrival; millisecond message times and clock anchors are retained. IrfanView 4.73 is hash-pinned, uses an isolated copy of owner settings (FitWindowOption 2, UseResample 1, UseZoomResample 0), and reads only index/path-token/DIB headers. Its main-DIB null→ready transitions are sampled, **not GDI completion**. The `/display` argument unexpectedly selected 100%; it was rejected and only the isolated INI window dimensions were changed. Generated controls verify the resulting comparable Fit area.

The measured towavue EXE is `158448BF491CE52BEFDC27FF7A6C525069FEDD824E31326DC020DA55E5881053`: **271 raw key-downs**, but its 16,384-record buffer overflows at 17,768 reservations. The complete retained prefix includes **250 handler key-downs (zero repeat), 250 targets and 251 distinct source/generation submissions**; the first 100 inputs plus initial image match IrfanView's first 101 in order and original dimensions. All 101 Fit dimensions differ by at most **0.5 px**, towavue client 960×576 and IrfanView image child 960×514. IrfanView captures **174 raw key-downs / 175 original sources**, each with preceding null; 103 racing samples are discarded, zero read errors/observer loss. Folder source stamps and installed INI hash remain unchanged. No compiler overlap or private visual inspection.

First-100 observer-dispatch→towavue Present median/p95/max: **18.402/55.776/171.389 ms**; observer-dispatch→IrfanView sampled-original-ready upper bound: **29.198/72.799/737.890 ms**. Towavue's later-stage marker is earlier on **64/100** paired sources, not every image. Irfan readiness brackets median/p95 **1.559/1.638 ms**, maximum 3.263 ms. Handler→towavue Present alone is **14.972/32.289/172.336 ms**; it excludes dispatch delay and must not be compared as the same start clock. Input spans differ (~14.8/16.4 s), OS cache state/observer overhead are uncontrolled, and towavue inherited noisy FFmpeg stderr whereas Irfan used NUL. Overall towavue tail/drain is unverified after overflow; only one retained key arrived while loading/awaiting, maximum app queue one. Thus no cold, sustained-overload, GDI/scanout or final-goal claim. The owner explicitly reports the severe slowdown was not reproduced.

Follow-up instrumentation only: capacity is now **65,536 records / 4 MiB**, with an exported capacity checked by the reader before any records; old mismatched diagnostics are rejected before launch. Frame phases remain limited to active loading/queue/folder work, with no idle record consumption. Both owned children now inherit only NUL standard handles to remove diagnostic pipe backpressure. Three native publication/overflow/hole controls pass at the new capacity, as do the Release Rust layout/source/capacity test, diagnostic build, feature-on app all-target Clippy/fmt/diff and a five-second generated actual-app reader control (130 records, no read errors/overflow/input, no frame records after settlement). Final EXE `DD644C13360E85FEEE073A3C2CFC2DB9FBF89A5B161A3EB8B742D5762DF976C1` is archived with PDB; **no new physical speed measurement on that diagnostic revision**. Product navigation/decoder policy and installed executables are unchanged. Detailed local numeric captures and comparison scripts are retained; do not rerun the unchanged physical trial without a new question.

### Fixed startup shaders compiled at build time

Adopt build-time D3DCompile for the four fixed blit/egui shaders; retain the exact source, entry points, targets and flags. The former blit literal contents match the extracted HLSL exactly. Both new regressions compare complete generated bytecode with historical runtime compilation. Per-renderer device/resource ownership and the separate video-edit shader compiler are unchanged. Generated bytecode totals **3412 bytes**, kept in Cargo output and embedded, not committed or deployed separately; both measured EXEs remain **14,236,160 bytes**. Cargo.lock and dependency versions are unchanged.

**48 native actual-app initial submissions**, generated sources, old/new/new/old with three fresh isolated process/settings/cache samples per batch. Main-entry→original-submission batch medians:

| Original | Old 1 ms | New 1 ms | New 2 ms | Old 2 ms |
|---|---|---|---|---|
| 32×24 PNG | 198.280 | 179.661 | 173.211 | 180.222 |
| 502×316 PNG | 187.405 | 175.857 | 177.475 | 178.454 |
| 4096×2304 PNG | 196.989 | 185.366 | 183.895 | 185.623 |
| 4096×2304 JPEG | 187.776 | 183.950 | 183.542 | 190.240 |

The isolated shader-resource interval (phase 22→23) falls consistently from **5.078–5.605 to 0.818–0.970 ms** across these batches. Startup totals also favor the candidate, but baseline drift prevents attributing their entire difference to shaders. Device-creation medians remain **94.167–98.408 ms**, still the main startup cost. No compiler/test overlap during timing; warm sources and uncontrolled desktop. All 48 original/display geometries match at 100% in 960×576 clients, with one selected-path prefetch decode, no foreground original and zero dropped trace events. Source hashes/mtimes and owned-child termination pass. No debugger, input injection, private appearance/pixels or screenshots; excludes process-loader work, compositor completion, physical navigation and OS-cold state. Not a fresh IrfanView comparison or full-goal completion.

**Release workspace: 1019 passed / 77 ignored / two existing exclusions**; vendor **8 passed / 4 ignored**, both bytecode regressions and the explicit hidden shared-hardware video/UI surface trial pass. WARP/hardware readback covers filters, inversion, alpha, clipping, draw order and anchored text; the shared-device trial retains three surface sizes/recreation and zero playback CPU transfers. Workspace and diagnostic-feature app all-target Clippy, vendor Clippy, formatting/diff and local links pass. Final vendor-only formatting split two test tuple lines; its suite/Clippy were rerun, not the unchanged full workspace. Full-suite app test EXE SHA-256 **FA7A61CD75B5F1989C06BA0589C05F9FCE1DB1CC3DFA87BD56ACBCEDE05D7F6A**. No native crash or remaining owned process.

Local `shader-build-20260915`: `SHADER-BUILD-RESULTS.txt`, `SHADER-BUILD-VERIFICATION.txt`, `submission-matrix.ps1` and matched EXE/PDB copies in `bin-before` / `bin-after`. Old EXE SHA-256 **2C0994885F1FC7BA1555D0F00FF50B67F5038492ADABC4ED7C800F94261697B9**, measured candidate **2C1F46FAE4D5106E923E14A2E9D16B50F83B2197348309AC85D6393034B8145C**. Compiler identity is retained in raw evidence; equality is host-specific, not a cross-Windows compiler-version promise. No installed-app/settings changes or publication.

### Owner-operated actual navigation comparison

The owner supplied physical Space (IrfanView) / Right (towavue), four steps about two seconds apart per fresh process, in **Irfan/towavue/towavue/Irfan** order. All **16 transitions** completed; four startup seeds are excluded. Generated originals, isolated settings/caches, default prefetch and warm source files; no concurrent compiler, injected input, screenshots or private-image appearance. External QPC timing excludes stopped debugger inspection, but running debugger/event/GPU perturbation remains.

| Next original | Irfan 1 ms | towavue 1 ms | towavue 2 ms | Irfan 2 ms |
|---|---|---|---|---|
| 4096×2304 PNG | 91.570 | 16.878 | 25.996 | 91.799 |
| 502×316 PNG | 6.660 | 5.503 | 5.082 | 7.495 |
| 4096×2304 JPEG | 32.882 | 19.497 | 19.578 | 28.099 |
| 32×24 PNG | 5.930 | 5.919 | 6.365 | 5.414 |

Large-image advantages persist across run order; tiny results are mixed. Only two observations per app/case, not a latency distribution. **Load-request→original-submission, not full key→display:** Irfan starts at selected-index load RVA `0x7cdf0`, ends after successful primary-DIB GDI transfer at `0x113995/0x113c31`; towavue starts at presentation stage 1, ends after successful original Present. Earlier key dispatch/navigation/reset work is excluded differently. Both validate next-original geometry and 100% zoom; large originals are clipped, not fully visible. Irfan's large transfer destination is 939×504 (small-image child client 956×521); towavue whole-window client is 960×576. Not equal viewport, compositor completion, physical scanout, pixel-quality, cold/rapid navigation or thumbnail evidence; native startup remains behind.

The verification-only Actual override now applies to each fresh image load, so all eight towavue steps retain 100% instead of resetting to Fit. A separate diagnostic correction requires a selected-path Requested event in the current generation: otherwise cache/publication is unavailable and no unrelated INITIAL_DECODE is emitted. Old raw worker fields describe the traced seed, **not each next target**; child self-clocks include debugger pauses. Neither affects the independent external timing above, and neither changes ordinary rendering/decoding. The new generation regression, feature-on/off app all-target Clippy, Release diagnostic build and fmt/diff pass. Final native flag-absent smoke retains Fit (502×316→816.544×514). No new full-suite aggregate.

Local `manual-switch-b02375e07ad64ac7a7ae58a06bb839f5` holds `MANUAL-SWITCH-RESULTS.txt`, probe source, generated fixtures and matching EXE/PDB archives. Measured `bin/towavue.exe` SHA-256 **0D81380FB69FB9D1B6E15B018F43F083292E8E315EDDD2425E2E95C977BF0A6B**; final trace-fixed `bin-final/towavue.exe` **2C0994885F1FC7BA1555D0F00FF50B67F5038492ADABC4ED7C800F94261697B9**. Installed IrfanView hash remains **1DFFD4E64079FF3070511661195DDFF7C5769EAF278513FE8087452B384DF22E**. Source hashes match before/after all runs; independent mtime checks began after the first Irfan run, not before it. All four owned debugger children report signaled cleanup; manual work is finished. No native crash, installed-app/settings update or publication.

### Native original-readiness comparison and delayed-import trial

A read-only, non-stopping observer measures process creation→first original ready, avoiding debugger perturbation. IrfanView's hash-pinned main DIB slot is assigned after normal decode; verify its header/dimensions and stable pointer without reading pixels. towavue's opt-in aligned `TOWAVUE_FIRST_ORIGINAL_SIZE` publishes validated first-original dimensions before texture preparation. This is **not submission, scanout or navigation**, and does not replace the goal's full boundary.

Same external QPC clock, local 1 ms sampling timer, fresh isolated settings/caches, generated sources; Irfan/ordinary/delayed/delayed/ordinary/Irfan, three samples per batch. Upper-endpoint batch medians:

| Generated input | IrfanView ms | Ordinary towavue ms | Delayed-import trial ms |
|---|---|---|---|
| 32×24 PNG | 115.597 / 130.792 | 207.056 / 200.152 | 202.024 / 192.728 |
| 502×316 PNG | 141.597 / 127.220 | 205.262 / 208.273 | 211.376 / 185.412 |
| 4096×2304 PNG | 196.720 / 222.861 | 211.111 / 202.536 | 194.947 / 198.844 |
| 4096×2304 JPEG | 158.186 / 134.782 | 209.271 / 208.524 | 197.316 / 206.924 |

All **72 observations** have a preceding empty state, matching original geometry, unchanged source hash/mtime and signaled owned-process cleanup; no unreadable samples. Empty→ready brackets are **1.501–1.640 ms**. No concurrent compiler, debugger, input injection, screen/pixel capture or global trace. Source hashing warms content; OS-wide caches/desktop activity remain uncontrolled. Retain the first delayed tiny-PNG **323.713 ms** outlier. Actual-size options are set, but this pre-render endpoint does not validate final client/display geometry, full pixels, presentation quality or cold I/O. Small-image startup remains behind IrfanView.

**Delayed FFmpeg imports are not adopted.** Same six DLLs/headers; only temporary MSVC short import libraries/link arguments differ. GNU-style archives caused six ignored `/DELAYLOAD` warnings initially; that build was not timed. The corrected PE has six actual DelayImport records. Overlapping gains/drift do not justify unverified first-use, concurrency, DLL-search/failure and broader media behavior. Do not repeat unchanged. The preliminary ~140 ms module observation did not persist (matrix median ~6.2 ms); it is not an FFmpeg initialization measurement.

The marker's Release regression and feature-on/off app all-target Clippy/fmt/diff pass; the matrix exercises the real completion callback. No new full-suite aggregate: 1017 workspace passes belong to `ed124a2`. Local `irfan-uia-*` evidence: `NATIVE-READINESS-RESULTS.txt`, `native-ready.cpp`, `ready-matrix.ps1`, matched EXE/PDB in `bin-native-ready` (ordinary EXE **CD8628F22B4BF13F3AE918418134B3A977BFA7109AD42840C11D8EC580E0182F**) and `bin-delayed` (trial **89D49BA3B2C767FFA169775525BAB391DF63E6C423A854A34CDABD0F45D0D338**). The restored ordinary-link diagnostic at that checkpoint was EXE **9C66C02CCAA39B49FA652FDCEB99DEF79D38C4312458420362ED676AEB0D769A**; delayed imports remain only in the isolated trial copy. No native crash, remaining owned process, default product/build/packaging change, installed-app/settings update or publication.

### Native preview/media duration probing

Uncached `PreviewCache::duration` now uses the existing worker-side native FFmpeg input with its owned interrupt callback instead of spawning FFprobe. Keep source-keyed coalescing, 64 cached durations, Matroska/WebM origin subtraction and unknown/invalid-duration rejection. The old CLI route remains test-only; real helper-location tests still validate it, and now additionally require native duration success with missing helpers. No new worker, native-pointer exposure, cache/quality change, or installed-app update.

Release CLI/native/native/CLI, five fresh probes per batch, generated MP4/MKV/WebM and warm source files: final duration medians **33.25–35.00 ms CLI / 0.48–0.84 ms native**, all values exactly equal. Whole uncached video-filmstrip requests (duration, decode, PNG/persistence/publication) have batch medians **40.069 / 4.173 / 3.851 / 39.965 ms**; all 20 output images and durations match, source bytes/mtime unchanged. Cache construction, equality and cleanup are untimed; no agent-owned compiler overlap. Desktop activity, including the owner's separately running app, was uncontrolled; no actual-app input/UI/GPU, OS-cold or new IrfanView comparison. This removes a measured first-preview cost, not proof of every thumbnail surface's final latency. Detailed numeric evidence is local `burst-fit-20260915/NATIVE-DURATION-RESULTS.txt`.

**55 preview-related Release tests plus six preview-worker tests pass**; two explicit cost opt-ins also pass. The final three-test duration run includes the overlapping audio regression and both opt-ins, not three additional distinct tests. Coverage includes WAV/FLAC/ADTS exact duration, missing/malformed input, pre-cancellation, six MKV/MP4/TS origin fixtures, helper isolation, actual thumbnail pixels, source changes and cache/worker bounds. Fifteen other opt-ins remain unrun. Clippy initially rejected the two constant Debug guards; they now return an explicit error, with affected tests rerun. Runtime/app all-target Clippy, ordinary Release app build, fmt/diff pass. No full workspace/app-test aggregate or new physical trial; native interrupt cancellation remains cooperative, not a hard blocking-I/O deadline. All owned jobs are terminal.

### Preview generation, cache ownership and first-preview latency

Pruning now retains directory entries and forms full paths only for attempted deletions. Same 64 MiB limit, file filtering, age order and failed-delete continuation; no cached size estimate or deferred pruning. Same-binary eager/lazy/lazy/eager paths, nine Release samples per batch: 512 entries **0.245/0.216/0.212/0.242 ms**, 2048 **0.900/0.817/0.781/0.886**, 8192 **3.448/2.840/3.634/3.477**. Small-directory benefit; largest case mixed, not an end-to-end speedup. Counts/bytes unchanged. **26 Release sharing tests pass / 15 ignored**, including locked-oldest and nested-directory coverage; cost opt-in separately passed, runtime all-target Clippy/fmt/diff pass. Warm filesystem, no compiler overlap; no UI, cold, memory or IrfanView claim. Raw evidence and pre-regression timing-binary limitation: local `lazy-prune-paths-20260915/RESULTS.txt`. Do not repeat unchanged.

The shared preview worker now serves memory hits, then persisted image thumbnails, then generation. A real-worker regression first reproduced memory/uncached/persisted ordering; it now verifies memory/persisted/uncached, source equality and a fresh disk-name snapshot after another consumer writes new entries. Reuse source keys already calculated during memory checks; one advisory disk-name snapshot avoids opening every missing entry. Bounded PNG/source validation, all three stages' cancellation/readd/close checks, legacy/corrupt fallback, cache limits and one worker are preserved. Audio/video do not probe during the disk sweep. A direct per-image disk sweep doubled all-miss first-result latency and was rejected; a mere nonempty-cache hint was also insufficient.

Latest refinement: one remaining request bypasses disk prioritization, including when other requests were memory hits. Its ordinary load still checks disk; no full-directory scan is needed for one-at-a-time closed preparation. The new scheduling regression fails without this bypass and passes with it; disk-stage lifetime controls retain a second pending key so they still exercise actual disk cancellation. Preserved Release bypass-off/on/on/off, nine samples per state, 8192 unrelated generated cache files: singleton persisted-image medians **4.305 / 0.347 / 0.417 / 4.598 ms**; uncached **9.428 / 6.084 / 6.605 / 10.045 ms**. At zero unrelated entries, persisted **0.388 / 0.458 / 0.370 / 0.390 ms**, uncached ~1.1 ms: no empty-cache speedup claim. All 288 results retain exact seeded pixels/path/generation and unchanged source bytes/mtime identity. Fresh memory, OS-warm files; request-to-observer, including ordinary persistence/pruning on misses, excluding setup/equality/cleanup. No compiler overlap; desktop uncontrolled. Filename normalization also reuses its owned string: same-binary old/new/new/old enumeration at 8192 entries **3.987 / 3.669 / 3.552 / 4.114 ms**, exact mixed-case key sets at 512/2048/8192 and unchanged file bytes. **Nine affected Release regressions and two explicit cost opt-ins pass**, plus **15 Debug app filmstrip/recent/seek/preparation tests / one large opt-in ignored**, runtime/app all-target Clippy, ordinary Release build, fmt/diff. No full-suite or physical/GPU/IrfanView speed claim; remaining enumeration/pruning and real display costs are not eliminated. Local matching EXE/PDB pairs and results: `singleton-preview-priority-20260915/RESULTS.txt`. Do not repeat the unchanged batch comparison below.

Final preserved Release before/after/after/before, five samples each: one uncached generated 4096x2304 PNG followed by 63 persisted thumbnails yields second-requested-image medians **13.183 / 3.376 / 3.554 / 13.343 ms**, all-64 **22.984 / 23.012 / 23.261 / 23.327 ms**. With 64 uncached 32x24 PNGs and an unrelated older cache entry, first-result medians are **4.061 / 4.365 / 4.391 / 4.343 ms**, all-64 **61.806 / 66.692 / 74.183 / 72.311 ms**. This improves ready-thumbnail delivery, not whole-batch throughput: the uncached first image now follows persisted results. All 2560 returned previews match seeded pixels/geometry/duration; source bytes/mtime keys unchanged. Fresh memory, OS-warm files, request-to-observer completion; setup/equality/cleanup excluded, no compiler overlap, desktop uncontrolled. **75 affected Release runtime tests pass / 20 ignored**, plus **15 Debug app filmstrip/recent/seek/preparation tests / one large opt-in ignored**; benchmark opt-in, runtime/app all-target Clippy, ordinary Release build, fmt/diff pass. No full suite, UI/GPU timing, physical-input, OS-cold, large populated-cache scaling or new IrfanView evidence. Local EXE/PDB pairs and raw data: `persisted-preview-priority-20260915/RESULTS.txt`. Installed apps unchanged; the broader goal remains active.

Accepted tab-hover single-frame completions now request a native window redraw, including errors; stale generations/targets still do nothing. The old handler only requested egui repaint, which does not itself wake this app's idle native window. `first_preview_completion_wakes_an_idle_native_window` checks five same-binary controls: direct native request, historical egui-only completion, current success, current failure and stale completion yield **redraw / idle / redraw / redraw / idle**. It uses an isolated, visible nonactivating blank window, generated 2x1 data and no input or GPU presentation. Initial hidden-window trials failed even after the fix and were not causal evidence; the native positive control resolves that harness limitation. **10 affected Debug tab-preview tests pass / one cost opt-in remains ignored**, plus app all-target Clippy, ordinary Release build, fmt/diff. This proves native event delivery, not a measured end-to-end hover speedup, actual worker latency, physical input or a new IrfanView comparison. No cache/decoder/quality or installed-app change; no full-suite claim. Continue remaining thumbnail/display measurements without repeating unchanged navigation or decoder controls.

Preview results now share their original RGBA allocation across cache entries and queued consumers using `Arc<Vec<u8>>`. Previously each cache hit copied the complete buffer under the memory-cache mutex. Unique producers still transfer their Vec without copying; full editable originals are unchanged. Cache limits, keys, source validation, cancellation and delivery order are unchanged.

Preserved before/after Release binaries, copy/shared/shared/copy, five samples per case; six generated resident keys supply 64 retained results through one/four callers:

| Preview / callers | Copy batch medians ms | Shared batch medians ms |
|---|---|---|
| 240×160 / 1 | 1.6006 / 1.4722 | 0.0896 / 0.0858 |
| 240×160 / 4 | 1.5377 / 1.5327 | 0.2399 / 0.1830 |
| 960×640 / 1 | 14.6952 / 14.4153 | 0.1549 / 0.0957 |
| 960×640 / 4 | 14.8749 / 14.5933 | 0.2467 / 0.2143 |

All **5120 returned images** match independent full generated pixels. Distinct returned pixel lengths fall from **9,830,400→921,600 bytes** for thumbnails and **157,286,400→14,745,600** for sheets: 64 copied buffers become references to six resident buffers. These are neither additional-to-cache bytes nor whole-process/peak-memory measurements. Batch timing includes lookup and worker joining/collection, not thread creation/readiness or validation. Sub-microsecond request p95 approaches timer granularity; retain raw values without claiming corresponding speed ratios. No compiler overlap; desktop uncontrolled.

The existing native-publication opt-in also passes copy/shared/shared/copy: **720 timed generated outputs, encoded PNGs and fresh-memory disk readbacks** match, with source bytes/mtime unchanged. Ready-path median ranges (copy→shared): frame **2.668–2.821→2.697–2.759 ms**, sheet **8.733–9.027→8.316–8.537**, waveform **2.149–2.241→2.186–2.284**. No consistent generation regression in these small warm fixtures; not a general no-regression or waveform acceleration claim.

**Release workspace: 1017 passed / 77 ignored / two existing exclusions**, plus **54 targeted preview passes / 15 ignored** and explicit timing opt-ins; workspace check/all-target Clippy/fmt/diff pass. The new lifetime regression covers original allocation identity, shared hits, explicit copy-on-write isolation, actual LRU eviction, surviving queued results and final-owner retirement. Existing native allocation-transfer, alpha/hidden-RGB, PNG conversion/corruption, source-change, cancellation/coalescing, unavailable-storage and media fidelity checks pass. The automated native-input failure was separate from this aggregate; the later owner-operated pass is recorded above. This preview-cache trial supplies no actual-app/GPU/scanout, physical-input, cold-state or IrfanView superiority evidence.

Earlier controls remain adopted unless marked rejected; historical timings below do not measure current shared ownership:

| Checkpoint | Retained evidence / do not repeat unchanged |
|---|---|
| `362b9d7`: packed preview color conversion | Six UI consumers share the original-image kernel. Actual tab acceptance: 960×640 opaque ~0.575→0.24 ms, mixed alpha ~0.94→0.32; all 240 queued texture images exact. Acceptance/conversion/registration only; drain egui's built-in texture before timing. `PREVIEW-ACCEPTANCE-RESULTS.txt`. |
| `c379764`: indexed-GOP continuation | Same indexed MOV/MP4 interval only; reverse/different/unindexed targets retain seeking. Six-second 1080p MP4, 16 cells: short GOP 225–273→132–136 ms, long GOP 1037–1047→140; all 640 frames exact. Decode/filter, not PNG/cache/UI. The initial fixture required absent libx264; corrected to MPEG4 plus existing H264, without a dependency change. `INDEXED-PREVIEW-GOP-RESULTS.txt`. |
| `7d5757e`: native ready-pixel publication | Video/frame/sheet/waveform producers publish generated RGBA after PNG encoding; disk/encoded-only fallback still validates PNG. All 180 generated outputs, PNG byte sequences and disk readbacks exact. Old sheet generation timings predate GOP continuation. `NATIVE-MEDIA-PUBLICATION-RESULTS.txt`. |
| `7ef8e55`: owned PNG cache decode | Consume RGBA8 canvas; convert other layouts normally. Thumbnail ~0.36→0.33 ms, sheet ~4.46→4.18; all 200 outputs exact. Encoded-memory decode only. `PREVIEW-CACHE-COST-RESULTS.txt`. |
| Rejected disk-sheet-first | Persisted sheet arrives earlier, but first preview regresses from ~3.0 to ~7.4 ms. All 60 sheets exact; retain early single-frame fallback. Prototype source/tests removed. Evidence in the preceding cache-cost file and `DISK-SHEET-FIRST-TRIAL.patch`. |

Current raw evidence: local `SHARED-PREVIEW-PIXELS-RESULTS.txt`, including initial baseline variability and authoritative suite summaries. Before/after runtime EXE/PDB copies are in `bin-preview-cache-copy` / `bin-preview-cache-shared`, EXE SHA-256 **32043620C9795F581377C68FD1E6545AC742CF3779E58902EDE75E1DFED887D3** / **3CF31DBF4C7408FCD70F77FA26EF49E8108481007921BE17D7B21D9A750643E6**. That checkpoint's Release app test EXE SHA-256 was **9EF51BBD6DC8E295DACACCB3B33107DD18B94E9E51664C0C20F4E9301F45DAB6**. Current Release artifacts are identified in the fixed-shader checkpoint above; **the Debug app test binary still contains the rejected disk-sheet-first lookup until rebuilt**. No native crash, owned job, installed-app/settings update or publication. Goal remains active.


### Warm-process submission and IrfanView instance semantics

**Correction to the previous handoff:** the installed IrfanView's `/one` closes existing instances; it does not forward a file into them. Three owned CLI probes lose the original debuggee before target submission; the final probe records original exit code 0 during termination and a still-running sender, with both handles subsequently signaled. The matching decompilation at `i_view64.exe.c:16291` places `/one` in the close-old FindWindow/PostMessage(0x111,0x438)/Sleep loop; `FUN_140076a90` also applies it for `Others/OneInstance=1`. The bundled option description alone was insufficient. No user IrfanView instance was present or targeted, no private input was injected, and there is **no IrfanView timing or warm-navigation comparison** from this attempt. Do not retry this CLI as process reuse.

The towavue observer does reach the additional window's successful original submission and sender acknowledgment. It also reproduced repeated `INITIAL_DECODE` output on later redraws. That verification-only log now uses the existing per-request token, avoiding duplicate trace snapshots/output; ordinary product behavior is unchanged. A generated 32×24 seed→502×316 target smoke verifies two request records/two decode records, 100% geometry, 960×576 target client, unchanged source hashes/mtime, sender exit 0 and both owned handles signaled. One selected target debugger stop reports **139.682 ms** external / **135.052 ms** resumed; the preceding valid old-diagnostic smoke was **148.579 / 143.624 ms**. These single observations do **not** demonstrate a speed improvement: initial seed painting is not an idle-state guarantee, and debugger events, auto-continued seed repaints and output perturb timing. Earlier observer failures (hexadecimal width condition; timeout/ack handling) remain in the raw log, not a successful aggregate.

Feature-on app all-target Clippy, Release diagnostic build, fmt/diff pass; no full-suite rerun, pixel/cache/backend or installed-app change. Local `warm-submit.cpp` / `warm-submit.ps1` and `WARM-PROCESS-SUBMISSION-RESULTS.txt` preserve evidence; the script now tests towavue only. That checkpoint's build-directory Release app and isolated `bin-request-log` EXE/PDB have EXE SHA-256 **06DAD0B29731BBB209F4A02B110D04A1395AC3E72807378AAFD60497BA4FBEF0**. No owned process remains. Actual navigation/thumbnail superiority remains unproven.


### PNG direct row output: not adopted

The existing png 0.18.1 `read_row` API can write expanded noninterlaced RGBA8 directly into the retained canvas, bypassing `next_row`'s output scratch buffer and the subsequent row copy. A scoped prototype preserved cancellation, tail/CRC handling, orientation, budgets and other formats; no vendor or unsafe change. Same Release test binary, buffered/direct/direct/buffered, five samples per batch: generated 4096×2304 RGBA8 Paeth PNG **36.823 / 36.664 / 36.540 / 36.689 ms**; largest read-only RGBA8 reference 8706×5949 **203.368 / 201.339 / 201.605 / 215.698 ms**. All **40 timed outputs** match full historical RGBA/dimensions; reference bytes/mtime remain unchanged, without private appearance or pixel output. Already-read compressed input, no compiler overlap; desktop activity uncontrolled. Generated gains are negligible and reference drift prevents a robust acceleration claim; fewer copies alone do not justify adoption.

All prototype product/test changes were removed, returning source to **3065d6c**. No full regression suite, trial Clippy, GPU/navigation, cold-I/O or memory claim. Local `PNG-DIRECT-ROWS-TRIAL.patch` / `PNG-DIRECT-ROWS-RESULTS.txt` retain the exact experiment. The Release runtime test binary was subsequently rebuilt for the adopted preview-cache ownership change; the Release app retains the ready-launch behavior. No installed changes, native crash or owned job remains. Do not repeat this row-copy trial unchanged.


### Ready images in additional launch windows

A normal CLI launch into a running host creates another HWND, sharing its D3D11 device but not its Application/loader. Per-application timing found small originals published in **0.1–0.2 ms**, yet accepted in **40–52 ms**. After ordinary show/focus, a newly launched image window with no prior UI frame now consumes an already-ready original and may submit its first frame immediately through the existing presentation gate. It never waits for decoding. Hidden windows, pending/error/nonimage loads and already-drawn contexts keep ordinary redraw scheduling; codecs, pixels and budgets are unchanged.

Same Release trial binary, eager off/on/on/off, three warm hosts per batch, three generated images opened in order per host. External time is sender process creation→host submission-log receipt, including IPC/window/show/focus and observation, not scanout:

| Input | Off batch medians ms | On batch medians ms |
|---|---|---|
| 502×316 PNG | 112.364 / 116.315 | 88.584 / 89.152 |
| 4096×2304 PNG | 126.176 / 125.550 | 114.326 / 122.919 |
| 4096×2304 JPEG | 105.947 / 102.793 | 103.678 / 101.139 |

All **36 target opens plus 12 seeds** completed. Fresh isolated settings/preview caches per host, warm files, normal Fit, 960×576 clients, unchanged target hash/mtime and acknowledged/signaled senders/hosts; seed geometry only, no independent seed stamp check. No compiler overlap; desktop/folder/preview activity uncontrolled. Small-PNG gains are clear, large-PNG gains smaller; JPEG external improvement is not robust. No arrow/tab/physical-input, private-reference, cold-I/O or IrfanView superiority claim.

Promotion to normal behavior passes an independent hidden-native image test covering pending/error/nonimage guards, exact owned-BMP pixels, one completed presentation, no duplicate frame and unchanged hidden launch; **13 other affected app tests and 39 image-loader tests pass**, including native initial-order controls. Default app/runtime and presentation-feature app all-target Clippy, Release diagnostic build, fmt/diff pass. The broad native-host test instead fails earlier in unchanged filmstrip video opening with D3D11 E_INVALIDARG at window_open_tests.rs:598, before the new image check; it is not a pass or a new exclusion, and the cause remains unverified. No native crash was reported.

The final normal-default Release copy also passes **12 target opens plus four seeds**, skip/default/default/skip, one host per batch. Small PNG **107.646 / 100.466→87.920 / 81.667 ms** confirms the direction; retain the final skip large-PNG **227.731 ms** external outlier (request interval 49.246 ms), not a new aggregate speed claim. Local raw evidence is in `WARM-HOST-RESULTS.txt`; matching EXE/PDB in the existing isolated image-submit root's `bin-ready-launch`, EXE SHA-256 **0395ED2734DB1533F9938330BC928756D7FDDF5327921C228747E80230410A99**. This preserved copy is feature-enabled; the build directory's later diagnostic is identified in Warm-process submission above. Installed files/settings remain unchanged. No owned job remains. Goal stays active.


### Actual-app initial image submission

Initial image launches now submit the Shell-normalized path to the existing prefetch worker before native window/graphics initialization. Ordinary Open reuses the completed original or adopts its active decode; it still refreshes Shell order and uses the initial-original presentation gate. No new worker/cache budget, codec, pixel conversion or animation policy. Successful adoption also avoids an unnecessary foreground preview; the measured gain is not exclusively decoder/graphics overlap.

Same Release diagnostic binary, skip/default/default/skip, three samples per batch, main-entry→first-original successful Present:

| Input | Skip medians ms | Default medians ms |
|---|---|---|
| Generated 32×24 PNG | 184.655 / 172.204 | 171.454 / 172.824 |
| Generated 4096×2304 PNG | 211.935 / 214.664 | 181.834 / 188.515 |
| Generated 4096×2304 JPEG | 192.701 / 197.277 | 184.113 / 182.292 |
| Reference JPEG compressed-size rank 1, 4961×3508 | 442.811 / 433.386 | 197.464 / 197.464 |
| Reference JPEG rank 2, 2000×4000 | 351.148 / 346.772 | 196.066 / 182.948 |
| Reference JPEG rank 3, 5000×6000 | 350.366 / 352.231 | 193.659 / 193.340 |
| Largest reference RGBA8 PNG, 8706×5949 | 422.803 / 425.653 | 288.718 / 289.765 |

**84 retained native samples** verify original geometry, unchanged source hash/mtime and exactly one selected-path image-loader original decode (skip: foreground 1/prefetch 0; default: 0/1; trace dropped 0). These counts exclude independent thumbnail/preview workers. Fresh isolated settings/thumbnail caches per process, warm OS source files, 960×576 client, normal folder workers, no compiler overlap or appearance/screenshot inspection; other desktop applications were uncontrolled. Reference geometry is independently checked with ffprobe. Small-image benefit is not established; rank 3 has a **417.400 ms** default outlier. The original reference run's final 27 outputs were lost with its session handle; after confirming no owned child remained, only those missing batches were repeated. They replace missing evidence, not failures or a fresh uninterrupted sequence; rank 2's final control was later. No claim about unretained outputs. These timings exclude process loader work, compositor completion, physical input and cold I/O.

**17 distinct affected Debug app tests pass**, plus **39 Release image-loader tests / two opt-ins excluded**. Coverage includes completed reuse, changed-source revalidation, nonimage/error behavior, launch-path normalization, original ordering and existing in-flight adoption/cancellation/budget/animation fallbacks. The app runs include two explicitly passing hidden-native controls: 101 originals with one real Shell completion, and 100 originals with delayed scripted order; two large sequence opt-ins and one native host opt-in remain excluded. Feature-on/off app Clippy, Release diagnostic build, fmt/diff pass. No full-suite rerun or installed/distribution update. No native crash or remaining owned media job. Local scripts/raw results: `INITIAL-PREFETCH-RESULTS.txt` in the existing Irfan probe directory; candidate EXE/PDB in `image-submit-20260915-21f1914ab79b41469a047ab72c02be5d/bin-prefetch`. EXE SHA-256 **BD79FB10BD2FEB4AC5244021413DDEFE862D3205C22DB518304ABD82C849F2D6**. The build directory's Release app remains a feature-enabled diagnostic.

Prior checkpoint `4a37d95` identified late image requests (~145–158 ms) after D3D11 creation (~98–111 ms), motivating this overlap; its exact stages and nine baseline trials remain in Git/local `IMAGE-SUBMISSION-RESULTS.txt`. The installed hash-verified IrfanView's actual original-DIB drawing helper/GDI transfer is also observed locally, but debugger clocks, start boundaries, display dimensions and resampling differ from towavue's native clock. Debugger attachment materially changes towavue timing (~484 ms versus ~209 ms in that control). **No comparable end-to-end IrfanView speed result yet**, and neither probe measures compositor completion or physical switching. Do not equate the native startup gains above with goal completion.

The subsequent **minimal-stop 1:1 control** uses the same initial debugger-loader boundary and one selected stop at original submission in each app. IrfanView's official `/display` option sets 100% zoom; hash-pinned post-GDI call sites validate successful full-height transfer, the primary DIB and image-child HWND. towavue's verification-only actual-size option and display geometry validate the unedited original's 1:1 scale. Generated 32×24 / 502×316 PNGs, Irfan/towavue/towavue/Irfan, three samples per batch: **23 fixed-client debugger samples retained** (Irfan **75.275–87.272 ms**, towavue **381.808–409.906 ms**). One additional towavue sample changed client to 1920×1080 (**398.851 ms**) and is excluded, not replaced; cause unknown, future harness runs reject the mismatch. Images fit without clipping; Irfan's image-child client is 956×521, towavue's whole-window client 960×576, so UI/background work still differs. All 24 owned debug handles signaled after cleanup; source hashes/mtime and geometry remain unchanged. This is an instrumented startup deficit, **not a native speed ratio or full goal result**. [WaitForEvent also processes other debugger events](https://learn.microsoft.com/en-us/windows-hardware/drivers/ddi/dbgeng/nf-dbgeng-idebugcontrol-waitforevent); one selected stop does not eliminate debugger overhead.

**Six native actual-size controls** report **155.383–160.416 ms** main-entry→submission and one prefetch original/no foreground original; D3D11 creation still takes **91.314–98.730 ms**. Their start boundary also differs from the debugger clock. One separate native flag-absent control retains Fit (502×316→816.544×514); it is not a speed comparison. No compiler overlap, fresh isolated settings/caches, warm sources, no pixel/appearance inspection, uncontrolled desktop activity. Feature-on/off app Clippy, diagnostic Release build, fmt/diff and local links pass; no full-suite rerun, native crash, remaining owned job or installed app change. Product behavior is unchanged in this checkpoint. Raw results/call-site details are local `MINIMAL-SUBMISSION-RESULTS.txt`; current diagnostic EXE/PDB is in the existing isolated root's `bin-actual`, EXE SHA-256 **7509DDC5F710BB7525646EBA19EE3C3F2D6661DE64434544BD4FA67844BBBF32**. Do not use the older four-argument marker with the new minimal probe.

### Graphics-startup overlap: not adopted

UI-start diagnosis, three fresh generated 32x24/100% launches: font setup **2.3–2.5 ms**, style/egui-winit state **<0.02 ms**, AccessKit **1.7–4.0 ms**, showing **26.1–46.0 ms**. A separate three-launch control found no caption bounds/WM_PAINT work during Show; seven bounds queries elsewhere total **1.0–1.2 ms**. Rejected image-Open-before-Show prototype, same-binary old/new/new/old, three samples each: first-Present medians **191.566/200.896/177.014/182.748 ms**. Shown-to-accepted falls **12–13→<1 ms**, but Show grows/varies and total gain is not robust. All 18 captures preserve source stamps, geometry, one prefetch/no foreground decode and owned-child termination. Numerical visible-window evidence only; desktop/OS caches uncontrolled, no compiler overlap, physical input, pixels/scanout or IrfanView claim. Ordering and temporary caption probes removed; only UI phase markers remain. Two Release marker tests, feature-on app all-target Clippy, restored ordinary Release build/fmt/diff pass. Exact binaries, rejected patch and limits: local `ui-start-cost-20260915/RESULTS.txt`. Next: investigate ready-only first-window submission in `WindowHost::start_pending`, without moving Open before Show; additional windows already use it. Do not repeat unchanged ordering controls.

Device-flag control after `5f7fe7a`: 20 fresh Release processes, five per BGRA+VIDEO / BGRA / BGRA / BGRA+VIDEO batch. Creation medians **99.550 / 98.915 / 100.317 / 99.066 ms**: no meaningful gain, so **retain production flags; do not repeat unchanged**. Adapter, feature level, decoder-profile fingerprint and context protection matched, not proof of actual decoding or cross-driver compatibility ([flag contract](https://learn.microsoft.com/en-us/windows/win32/api/d3d11/ne-d3d11-d3d11_create_device_flag)). Windowless API timing only; no media, startup-to-image/scanout, OS-cold or memory evidence. No compiler overlap; desktop uncontrolled. Diagnostic Release build/Clippy/fmt/diff pass; product unchanged. Exact binary, matrix and limits: local `device-start-cost-20260915/RESULTS.txt`.

The owner permits **GDI prototyping only**, but prioritizes more effective changes within one-stage rendering; **defer GDI, not permission-blocked**. New 20-process default/explicit-adapter/explicit/default control, five per batch, total creation **101.845/104.723/102.740/98.612 ms**. Explicit factory medians **8.731/8.397 ms**, enumeration **0.087/0.086**, device-only **95.618/94.114**: discovery is a small part, no total gain. Same flags/LUID/capabilities, one device; no product adapter change. Diagnostic Release build/Clippy/fmt/diff pass, no compiler overlap. Same windowless/warm/uncontrolled limits as above; raw matrix and exact EXE/PDB: local `device-adapter-cost-20260915/RESULTS.txt`. Do not repeat this adapter-only comparison unchanged.

CPU diagnosis: 20 fresh default/explicit/explicit/default processes, five per batch; device wall medians **103.726/96.129/96.226/104.589 ms**, calling-thread CPU **93.750/78.125/78.125/109.375 ms**. Coarse 15.625 ms accounting does not measure exact waiting. A separate owned C++ device-only helper retained **409 instruction samples** across six fresh processes: imagehlp **144**, ntdll **140**, win32u **76**. Installed export/disassembly checks locate imagehlp samples in the scanning helper called by `IsBufferCleanOfInvalidMarkers`; syscall sites include GPU hardware-queue creation and file/registry access. Suspended-thread observations are not CPU-time shares or caller/scan-target identification. Plain/sample/sample/plain controls, all exit/error/capacity checks pass; no stack/media reads, system trace or OS/driver/security change. This narrows startup to substantial OS/driver work, not an app-level fix; do not bypass verification or repeat unchanged probes. Next: inspect remaining app-owned startup/preview costs before expanding rendering architecture. Rust example test/build/Clippy/fmt/diff and local MSVC strict build pass. Complete data, binary identities and limitations: local `device-cpu-cost-20260915/RESULTS.txt`.

A verification-only helper created the same hardware BGRA/video-capable D3D11 device while window/caption/fonts/UI initialization proceeded; swap-chain creation and rendering stayed on the UI thread. The existing image prefetch and one-device contract were retained. Same Release binary, off/on/on/off, three-sample medians: generated 32×24 PNG **172.965/166.460→159.784/154.814 ms**; 4096×2304 JPEG **180.375/178.249→172.066/169.122 ms**. The wider controls do not establish a worthwhile general improvement: generated large PNG **180.014/207.749→191.799/206.114**, reference JPEG rank 1 **203.881/196.850→182.696/213.627**, largest reference RGBA8 PNG **285.572/285.104→290.696/287.286 ms**. Keep every sample; no passing reruns or outlier removal. The UI still waits ~74–97 ms for the helper, and no cross-driver/lifetime/recovery/video/early-close certification was performed.

**60 native matrix calls plus two preliminary smoke calls** retain source hash/mtime, stable reported original/display geometry, one selected-path prefetch original/no foreground decode and signaled owned process handles. Fresh isolated settings/preview caches, warm OS sources, 960×576 client, no compiler overlap; other desktop activity uncontrolled. No private appearance/pixel inspection, cold-I/O, full-pixel comparison, compositor/physical-input or IrfanView speed claim. The trial Release build passes; no full suite or prototype Clippy. All product changes were removed, restoring **991b59a** exactly; no installed app change, native crash or remaining owned job. The existing pre-draw completion drain and pre-thumbnail original publication already address ready-image waiting; do not duplicate them. Local patch/results are `GRAPHICS-OVERLAP-TRIAL.patch` / `GRAPHICS-OVERLAP-RESULTS.txt`; prototype EXE/PDB remains under the isolated root's `bin-graphics-overlap`, SHA-256 **514CA720737A27D0E2B37366CE9241FBBBC38EE8D3CBCAC3D6D1A81C10D28A24**. The build-directory Release app was subsequently rebuilt for the adopted ready-launch checkpoint below; this rejected prototype survives only in its isolated archive. Do not repeat this startup parallelization unchanged.

### JPEG input buffering: rejected prototype

Following the analysis's streaming-stdio mechanism, compare full compressed-file reads with 8 KiB/64 KiB buffered input using the same strict zune RGBA decode. A specialized buffered exact-read path preserves partial output/position; a second prototype retains bytes across local seeks and caches logical position. Neither is adopted. Release, warm files, full/8K/64K/64K/8K/full, five samples per batch; generated 4096×2304 and compressed-size reference ranks 1/2/3/128. Ordinary buffered seeks regress every case (rank 1: full **126–136 ms**, 8K **239–260**, 64K **298–319**). Buffered local seeks remove much of that overhead but still do not justify replacing the current reader:

| Input | Full file ms | Buffered 8 KiB ms | Buffered 64 KiB ms |
|---|---|---|---|
| Generated 4096×2304 | 12.821–12.992 | 12.795–12.849 | 12.743–12.841 |
| Rank 1, 4961×3508 | 118.972–120.110 | 126.646–128.014 | 123.721–123.829 |
| Rank 2, 2000×4000 | 75.088–75.675 | 81.806–81.817 | 78.942–79.270 |
| Rank 3, 5000×6000 | 88.223–88.440 | 89.322–90.600 | 87.721–88.203 |
| Rank 128, progressive 1830×2048 | 24.723–25.181 | 31.869–32.838 | 31.713–32.652 |

Both prototypes pass all 300 timed full-pixel comparisons and source-byte/mtime checks without displaying/writing private pixels. Five ordinary JPEG/reader tests pass on the final prototype; the nine-fixture Pillow control passed before adding its test-only positioned reader. File open/read/decode/drop are timed; EXIF transformation, damaged-input fallback, presentation/GPU, physical input, OS-cold and process-memory measurements are not. Compiler work did not overlap timing; other desktop processes were uncontrolled. The prototype's Clippy check rejects twelve test-only `unwrap()` calls; it was discarded, not certified for adoption. All source changes were removed and the worktree's product code matches `26c8f0d`; no installed app changed. Exact trial patch/results are retained beside the local Irfan probe as `JPEG-STREAMING-TRIAL.patch` / `JPEG-STREAMING-RESULTS.txt`. The Release runtime test binary was subsequently rebuilt from current source for the 39 passing image-loader checks above; it no longer contains this rejected trial. Do not repeat these input variants unchanged. Prioritize post-decode display preparation and a comparable actual-app presentation boundary next, rather than another isolated JPEG backend substitution; the full goal remains unproven.

## Recent checkpoints

Keep at most a few meaningful handoffs here; older details are in Git.

- **2fb9a43 — APNG→GIF / strict GIF tails:** exact representable delays and up to 65536 total plays, shared compositing/snapshots and save lifecycle. A 32×24 quantized GIF reproduced valid LZW-tail rejection; drain one byte after reading pixels before advancing, reject excess pixels, retain strict end-code checks. 823 workspace passes / 36 ignored after affected reruns; Shell dump 62148 retained below.
- **8f5ca6a — WebP→GIF:** shared edited snapshots/GIF assembly, exact centisecond and repeat conversion, color/alpha and target-safety regressions, Save-as/resave/reopen/history test. 818 workspace passes / 36 ignored, then five app conversion tests including the new path.
- **b6e4e0d — video-tab texture reuse:** UV-only position updates within the installed sheet; unchanged texture ID/empty upload delta, owner/boundary replacement, hover-exit release and rejection of late/failed results. 434 app passes / 12 ignored, not a physical-hover latency measurement.
- **c83455f — JPEG persistence:** reduced JPEG previews persist source geometry and survive a fresh memory cache; BMP persistence rejected after measurement. Foreground speculation remains memory-only; occupied storage preserves bytes and falls back to shared memory. 815 workspace passes / 36 ignored; separate Release comparison below.
